# گزارش ممیزی کامل — ربات تلگرام «میراث» (Legacy Game)

> یادداشت: این سند گزارش تاریخی یک ممیزی است؛ در زمان نگارش آن، نام ربات «زندگی مجازی» بود.

تاریخ: ۱۴۰۴-۰۶-۱۷ (2026-09-08) · شاخه: `arena/01a080c5-virtuallife` · ممیز: Agent Mode (Arena.ai)

---

## ۱) خلاصهٔ مدیریتی (Executive Summary)

پروژه یک ربات بازی شبیه‌ساز زندگی فارسی است: بازیکن، اقتصاد، شغل، مسکن، بانک، شهر، انتخابات، بازار، مأموریت و… با TypeScript (strict)، grammy، Prisma 6 و PostgreSQL.

**وضعیت نهایی:**
- `tsc --noEmit` → پاک ✅
- `eslint src tests` → پاک ✅
- `jest` → **۵۰ سوئیت / ۴۸۱ تست، همه سبز** ✅ (قبل از اصلاحات: ۴۷۸ تست که ۷ مورد در ۴ سوئیت شکست داشت)

**نتیجهٔ ممیزی:** معماری پروژه قوی و الگوهای مالی عموماً اتمیک و شرطی هستند (`updateMany` + `count===1` در `$transaction`)، اما **۷ باگ واقعی (۲ بحرانی/پرخطر مالی، بقیه متوسط)** پیدا و همهٔ آن‌ها در ریشه اصلاح شدند. ۴ شکست تست نیز ریشه‌یابی شد: ۳ مورد «تستِ قدیمی نسبت به رفتار جدیدِ عمدی» بودند (به‌روزرسانی تست‌ها بدون تضعیف پوشش) و ۱ مورد باگ در خودِ اسکنر تست بود.

**مهم‌ترین یافته‌ها (قبل از اصلاح):**
1. 🔴 **پول‌سازی از رقابت در تسویهٔ انتخابات** — دو بازدید همزمان می‌توانست ودیهٔ نامزدها را دوبار برگرداند.
2. 🔴 **نابودی کالا در بازار بازیکنان** — کسر مضاعف انبار فروشنده هنگام خرید (یک‌بار موقع آگهی، یک‌بار موقع خرید) + امکان فروش همزمان یک آگهی به دو نفر.
3. 🟠 **جایزهٔ دوبل قرعه‌کشی** — رقابت در تسویهٔ تنبل هفتگی.
4. 🟠 **TOCTOU ثبت‌نام دانشگاه** — کسر شهریه و فعال‌سازی تحصیل غیراتمی.
5. 🟠 **چندهمسری رقابتی** — پذیرش پیشنهاد ازدواج بدون بررسی وضعیت فعلی خواستگار.
6. 🟠 **سوختن پول آگهی همگانی** — کسر هزینه جدا از ثبت خبر؛ آگهی دومِ روز پول می‌گرفت اما منتشر نمی‌شد.
7. 🟡 چندگانهٔ «کسب‌وکار»: مالکان بیش از یک شرکت به مدیریت شرکت دوم دسترسی نداشتند.

---

## ۲) فایل‌های بررسی‌شده (Files Inspected)

**پوشش: ۱۶۳ فایل TypeScript (~۳۴٬۶۰۰ خط) — همهٔ ماژول‌ها خوانده شدند.** دسته‌بندی:

| لایه | فایل‌ها |
|---|---|
| ورودی/پیکربندی | `src/app.ts`, `src/config/*` (env, economy, market.config, region.config) |
| بات | `bot.ts`, `handlers/{text(2856),expansion(1417),start,group}.handler.ts`, `keyboards/main.keyboard.ts(1092)`, `renders.ts(2120)`, `help-content.ts(886)`, `panel.ts`, `ui-kit.ts`, `handler-errors.ts`, middleware ها (خطا/نرخ), `member-info.provider.ts`, `profile-photo.ts` |
| DI و دیتابیس | `services/container.ts`, `database/client.ts`, ۱۵ ریپازیتوری در `database/repositories/` |
| ماژول‌ها (~۵۰ سرویس) | identity, registration, occupation (شامل `employment.ts` منبع حقیقت), banking, deposit, lending, housing, rental, city (projects/elections/lottery/region/policy/challenge), market, shop, quests, achievements, events, news+ad, rewards, residence, family, education, finance/credit, ranking/leaderboard, missions, statistics, activity, notification, maintenance, lifecycle, relationship, skills, health, gym, pets, stock, farm, crafting, branch, auction, admin |
| Prisma | `schema.prisma`, ۱۰ migration، `seed.ts` |
| تست‌ها | ۵۱ فایل تست + `setup.ts` |
| متا | `AGENTS.md`, `package.json`, `jest.config.js`, `tsconfig.json`, `.env.example`, `Dockerfile`, `railway.json` |

---

## ۳) باگ‌های پیداشده و اصلاح‌شده (Bugs Found)

| # | شدت | محل | شرح | وضعیت |
|---|---|---|---|---|
| 1 | **CRITICAL** | `city/elections.service.ts` → `settleExpiredElections` | بستن انتخابات بدون قفل وضعیت → رقابت دو بازدید = بازگشت **دوبار** ودیهٔ همهٔ نامزدها (پول‌سازی) | ✅ اصلاح: `updateMany({status:'OPEN'})` + `count===1` داخل تراکنش |
| 2 | **CRITICAL** | `market/trade.service.ts` → `buyListing` | ۱) کسر مضاعف انبار فروشنده (کالا هنگام آگهی رزرو می‌شود؛ خرید دوباره کسر می‌کرد → نابودی کالا یا شکست خرید قانونی) ۲) دو خریدار همزمان می‌توانستند یک آگهی را بخرند (فروش دوم پول+کالای تازه می‌ساخت) | ✅ اصلاح: حذف کسر مضاعف؛ ادعای انحصاری `ACTIVE→SOLD` پیش از جابه‌جایی پول |
| 3 | **HIGH** | `city/lottery.service.ts` → `settleLastWeek` | رقابت در تسویهٔ تنبل → پرداخت دوبل جایزه | ✅ اصلاح: ادعای شرطی روی `lotteryLastWeek` داخل تراکنش + علامت‌گذاری حالت بدون بلیت |
| 4 | **HIGH** | `education/education.service.ts` + `player.repository.ts` | TOCTOU: بررسی موجودی → `update` ساده؛ دو درخواست همزمان = شهریهٔ دوبل/ثبت‌نام دوبل | ✅ اصلاح: متد اتمیک `enrollStudent` با شرط `balance>=tuition AND isEnrolled=false` |
| 5 | **HIGH** | `news/ad.service.ts` → `publish` | کسر هزینه جدا از ثبت خبر (با خطای بلعیده‌شده) → آگهی دومِ روز یا خطای ثبت = **سوختن ۲ میلیون** بدون انتشار | ✅ اصلاح: کسر هزینه + ثبت خبر با کلید یکتا در **یک تراکنش**؛ نقض یکتایی = رولبک کامل + پیام «امروز آگهی داده‌ای» |
| 6 | **HIGH** | `family/marriage.service.ts` → `accept` | خواستگار می‌توانست بین پیشنهاد و پذیرش با کس دیگری ازدواج کند → چندزنی (دو Marriage فعال) | ✅ اصلاح: بررسی `maritalStatus` خواستگار داخل همان تراکنش |
| 7 | **MEDIUM** | `text.handler.ts` → `work:menu:my_biz` و بخش `my_biz` | کیبورد فقط برای `businesses[0]` ساخته می‌شد؛ مالکان چند شرکت، شرکت ۲+ را مدیریت نمی‌کردند | ✅ اصلاح: کیبورد انتخاب `biz:manage:<id>` برای حالت چندشرکتی + هندلر جدید با بررسی مالکیت |
| 8 | **MEDIUM** | `banking.repository.ts` → `buyProperty` | رقابت خرید یک ملک AVAILABLE توسط دو نفر (هر دو کسر پول، سند فقط یکی) | ✅ اصلاح: ادعای شرطی ملک پیش از کسر پول (ریپازیتوری؛ مسیر اصلی خرید از `acquirePropertyDirect` است و آن امن است) |
| 9 | **MEDIUM** | `banking/deposit.service.ts` → `settleMatured` | بستن سپرده خارج از تراکنشِ پرداخت → کرش بین این دو = «بسته اما بی‌پول» | ✅ اصلاح: هر دو در یک `$transaction` |
| 10 | **MEDIUM** | `text.handler.ts` | هندلر `house:main` **دوبار** ثبت شده بود (نسخهٔ دوم مرده؛ نسخهٔ اول کد مردهٔ `isResting` داشت) | ✅ اصلاح: حذف ثبت تکراری و کد مرده |
| 11 | **MEDIUM** | `registration.service.ts` → `start` | اجرای دوبارهٔ `/start` وسط ثبت‌نام، `stateData` را با `{}` بازنویسی می‌کرد → **پاک شدن جنسیت انتخاب‌شده** | ✅ اصلاح: ادغام با دادهٔ موجود؛ کد رفرال جدید فقط جایگزین می‌شود (+۲ تست رگرسیون) |
| 12 | **MEDIUM** | `text.handler.ts` → `bank:loan_repay` | نمایش هاردکد `money(1_000_000)` به‌عنوان قسط حتی در قسط آخر که کمتر است | ✅ اصلاح: `min(1_000_000, ماندهٔ قبل از پرداخت)` |
| 13 | **LOW** | `rate-limit.middleware.ts` | حذف بی‌صدای آپدیت در پنجرهٔ ۸۰۰ میلی‌ثانیه بدون پاسخ به `callback_query` → **اسپینر بی‌پایان تلگرام** | ✅ اصلاح: `answerCallbackQuery` با پیام «کمی صبر کن…» |
| 14 | **LOW** | `bot/profile-photo.ts` | کش عکس پروفایل: اگر هیچ ورودی منقضی‌ای نبود، هیچ‌گاه تخلیه نمی‌شد → رشد بی‌حد حافظه | ✅ اصلاح: تخلیهٔ قدیمی‌ترین‌ها تا زیر سقف |
| 15 | **LOW** | `leaderboard.service.ts` + کیبورد | دکمهٔ «بعدی ➡️» حتی در صفحهٔ آخر (دکمهٔ مرده) | ✅ اصلاح: `hasNextPage` در سرویس و گارد در کیبورد |
| 16 | **LOW** | `text.handler.ts` → هندلر `lb:` | همهٔ خطاها «این گروه ثبت نشده» پیام می‌گرفتند (گمراه‌کننده برای خطای شبکه در رتبهٔ جهانی) | ✅ اصلاح: پیام واقعی `AppError` و پیام عمومی برای بقیه |
| 17 | **LOW** | `elections.service.ts` → `castVote` | رأی‌دادن بعد از `endsAt` ممکن بود (تا تسویهٔ تنبل بعدی) | ✅ اصلاح: گارد مهلت |
| 18 | **LOW** | `elections.service.ts` → `registerCandidacy` | کسر ودیه و ایجاد نامزدی جدا بود؛ خطای غیرتکرار در ایجاد = سوختن ودیه | ✅ اصلاح: هر دو در یک تراکنش (رولبک خودکار) |
| 19 | **LOW** | `expansion.handler.ts` + کیبورد | پنل موفقیت «شکست سپرده» دکمهٔ «✖️ انصراف» داشت (معنای غلط) | ✅ اصلاح: کیبورد بازگشت به تختهٔ سپرده‌ها |
| 20 | **تست** | `tests/*` | ۷ تست شکست‌خورده — ریشه‌ها: ۳× قدیمی نسبت به قابلیت‌های جدید عمدی (گرید وام، بافر سیاست شهردار، قیمت‌گذاری اجاره با `purchasePrice`)، ۱× باگ اسکنر `ui-consistency` | ✅ اصلاح اصولی (نه حذف/تضعیف) |

---

## ۴) ممیزی امنیت (Security)

- **ساخته‌شدگی/تغییر `callback_data`:** همهٔ هندلرهای دارای شناسه (وام، سپرده، آگهی، ملک، کسب‌وکار…) **مالکیت را سمت سرور** از روی `ctx.from.id` بررسی می‌کنند (`ownerId === player.id`، `targetId === player.id` و…). هیچ مسیر IDOR فعالی یافت نشد. نمونه‌های تأییدشده: `trade.buyListing` (جلوگیری خرید آگهی خود)، `shop.useItem`، `rental.setRent/rent`, `business.*`, `playerLoan.*`, `identityPrivacy.getProfileForViewer`.
- **تزریق:** تنها SQL خام در `job-capacity.repository.ts` است و هر دو با **tagged template پارامتریزهٔ Prisma** هستند → امن. هیچ ورودی کاربری به کوئیرشته الحاق نمی‌شود.
- **ارتقای دسترسی:** `AdminService` در هر متد `assertAdmin` دارد؛ `ADMIN_USER_IDS` از env. دستورات ادمین فقط در چت خصوصی. اکشن‌های `adm:action:` بازبینی شدند — امن.
- **ورودی متن:** `parseAmountInput` + اعتبارسنجی‌های عددی (`Number.isSafeInteger`, ردّ NaN/Infinity/منفی/اعشاری) در همهٔ مبادی پول. طول بیو و آگهی محدود است.
- **نرخ/اسپم:** ریت‌لیمیتر ۸۰۰ میلی‌ثانیه‌ای + هزینه‌های سنگین طراحی‌شده (آگهی ۲ میلیون، ودیه‌ها، جریمه‌ها) + کلیدهای یکتای روزانه.
- **طول `callback_data`:** بلندترین دادهٔ تولیدی (`work:pt:cat:رستوران و پذیرایی`) = ۴۴ بایت < سقف ۶۴ بایت تلگرام ✅.
- **باقی‌مانده (اطلاعاتی):** مقادیر فارسی در `callback_data` از `safeCallbackSchema` عبور نمی‌کنند، اما این اسکیمای خاص در هیچ مسیر تولیدی استفاده نمی‌شود (فقط در تست)؛ اگر روزی به مسیر داغ اضافه شد باید الگو اصلاح شود.

---

## ۵) ممیزی دیتابیس (Database)

- **الگوی پولی:** مطابق `AGENTS.md` تقریباً همه‌جا رعایت شده: «کسر شرطی `updateMany({balance:{gte:x}})` + `count===1` در `$transaction`». تنها تخلف (ثبت‌نام دانشگاه) اصلاح شد.
- **تراکنش‌ها:** همهٔ جریان‌های چندنقطه‌ای پول داخل `$transaction` تعاملی هستند و با پرتاب، کامل رولبک می‌شوند (تأییدشده برای: بانک، وام، سپرده، اجاره، خرید ملک/مبله/نگهداری، بازار، بورس، مزرعه، کارگاه، شعبه، چالش، آگهی، ازدواج/طلاق/مهریه، قرعه‌کشی، حقوق و دستمزد).
- **مهارهای یکتایی کلیدی:** `Vote(election,voter)`, `ElectionCandidate(election,player)`, `LotteryTicket(group,player,week)`, `DailyFortune(player,day)`, `WeeklyChest(player,week)`, `AchievementGrant`, `Referral(referee)`, `GameEvent.dedupeKey` — همگی به‌عنوان «ضامن ایدمپوتنس» درست استفاده شده‌اند.
- **قفل خوش‌بینانه:** `lastInterestAt`, `lastPayrollAt`, `lotteryLastWeek`, `lastBonusDayIndex`, `remainingAmount` وام — الگو درست و مؤثر است.
- **تنها کمبود ساختاری (اطلاعاتی):** نبود ایندکس یکتای جزئی روی «یک انتخابات OPEN به‌ازای هر گروه» — در رقابت بسیار نادر ممکن است دو انتخابات باز ساخته شود. به‌دلیل نیاز به مهاجرت و احتمال ناچیز، به‌عنوان ریسک باقی‌مانده ثبت شد (رفتار همگرا: همیشه قدیمی‌ترین باز مبناست).
- **انطباق اسکیما ↔ کوئری ↔ منطق:** بررسی شد؛ `purchasePrice` در اجاره (غیرقابل‌فرض)، `isFurnished`, `unpaidSalary`, `dedupeKey` و… همگی منطبق‌اند.

---

## ۶) ماشین وضعیت (State Machine)

- `PlayerStateMachine`: شروع/پایان فعالیت‌ها (کار/تحصیل/استراحت) با `assertCanStartActivity` کنترل می‌شود؛ `validateSalaryAdjustment` سقف ۳ برابری دارد (کامنت قدیمی «۲۰۰٪» فقط یک خطای متنی است — اطلاعاتی).
- **اصلاح‌شده:** پاک‌شدن `stateData` با اجرای مجدد `/start` (باگ ۱۱).
- جریان ثبت‌نام: `AWAITING_GENDER → AWAITING_BIOGRAPHY → COMPLETED` — گام‌ها از `stateData` استخراج می‌شوند و با اصلاح ۱۱، تکرار `/start` دیگر گام را عقب نمی‌برد.
- `resetState` ادمین وضعیت فعالیت را بازنشانی می‌کند اما نوبت کاری فعال را نمی‌بندد؛ بازیکن همچنان با «پایان کار» می‌تواند آن را ببندد (قابل‌تحمل — اطلاعاتی).
- مرگ/پیری (`lifecycle.isDead`) هنوز به گیم‌پلی متصل نیست (اطلاعاتی).

---

## ۷) ممیزی API تلگرام (Telegram API)

- **پاسخ به کال‌بک‌ها:** تقریباً همهٔ مسیرها `ackCallback` دارند. دو مورد `ack` دوبله (`news:\d+`, `quest:main`) بی‌ضرر است. **مشکل واقعی:** ریت‌لیمیتر بدون پاسخ حذف می‌کرد (باگ ۱۳) → اصلاح شد.
- **ویرایش به‌جای پیام جدید:** `editPanel` در همهٔ پنل‌ها طبق قانون «یک پنل = یک پیام» رعایت شده است.
- **تعارض هندلر:** تنها تکرار واقعی `house:main` بود (باگ ۱۰) → رفع شد. بررسی سیستماتیک الگوها با `grep` هیچ تکرار دیگری نشان نداد.
- **کنترل سیلاب (flood):** ریت‌لیمیتر + تسویه‌های تنبل (به‌جای تایمر) + کش‌های یک‌دقیقه‌ای بافرها — معماری مطابق قواعد پروژه است.
- **دکمه بدون هندلر / هندلر بدون دکمه:** با اسکن متقابل کیبوردها و هندلرها (همان روش تست `civic-market-routing`) هیچ دکمهٔ مرده‌ای در مسیرهای فعال نماند. ۱۲ کیبورد/سرویس بدون اتصال (fam/pet/stk/auc/gym/loan/farm/craft/chal/pol/br/ad + auction) **قابلیت‌های منتشرنشده** هستند — طبق قانون پروژه (ممنوعیت قابلیت جدید بدون اجازه) دست نخوردند و فقط گزارش می‌شوند.

---

## ۸) ممیزی کارایی (Performance)

- **مسیر داغ پیام گروه:** `handleGroupContext` با کول‌داون ۶۰ ثانیه‌ای همگام‌سازی + `NewsService.claimForChat` با کول‌داون ۳۰ دقیقه‌ای و بررسی پیش از کوئری → مناسب.
- **بدون N+1:** منطقه، اعتبار، مأموریت و دستاوردها با `Promise.all` از `aggregate/count` استفاده می‌کنند. رتبه‌بندی با Snapshot ده‌دقیقه‌ای.
- **کش‌ها:** بافرهای منطقه (۱ دقیقه/۵۰۰ ورودی)، اسنپ‌شات رتبه، عکس پروفایل (نشتی‌اش رفع شد)، کول‌داون خبرها (هرز با پاکسازی).
- **تایمر گیم‌پلی:** ندارد (الگوی تنبل) — تنها تایمرِ نگهداری داده هر ۶ ساعت (`unref` شده) است. مطابق `AGENTS.md`.

---

## ۹) معماری (Architecture)

- لایه‌بندی روشن: `handlers → services → repositories → Prisma`؛ DI مرکزی در `container.ts`.
- «منبع حقیقت اشتغال» (`employment.ts`) واقعاً تنها نقطهٔ تعریف است — `grep` سراسری هیچ خوانش مستقیمی از `occupationId` به‌عنوان معیار اشتغال نشان نداد.
- سرویس‌های ایزولهٔ مالی با اینورینت‌های اقتصادی مستند (سینک‌ها، سقف‌ها، ضدفارم).
- بدون بازآرایی غیرضروری: همهٔ اصلاحات این ممیزی **کمینه و موضعی** بودند؛ هیچ ریفکتور ساختاری انجام نشد.
- **بدهی فنی ثبت‌شده:** ۱۲ ماژول منتشرنشدهٔ سیم‌نشده (مرده اما تمیز)، توابع کمکی اندک در `text.handler.ts` (۲۹۰۰+ خط — بزرگ ولی با تفکیک بخش‌ها قابل‌دفاع)، چند `catch(() => {})` که عمداً «هرگز مسیر اصلی را نشکن» طراحی شده‌اند.

---

## ۱۰) حالت‌های لبه (Edge Cases) — بررسی ذهنی

| سناریو | نتیجه |
|---|---|
| کلیک دوبله روی خرید/پرداخت/دریافت پاداش | همه با قفل شرطی یا یکتایی مهار شده‌اند (و در این ممیزی ۴ مورد جامانده اصلاح شد) |
| خرید همزمان یک کالا/ملک/آگهی توسط دو نفر | اصلاح شد (ادعای انحصاری) |
| کاربر بن‌شده/خارج‌شده از گروه | فیلترهای `status: ACTIVE` در عضویت‌ها؛ `linkPlayerToGroup` بی‌ضرر |
| ورودی خالی/خیلی بلند/یونیکد | محدودیت‌های طول + نرمال‌ساز فارسی + اعتبارسنجی عددی |
| کرش فرایند وسط تسویه | تسویه‌های پولی همگی تراکنشی شدند (سپرده/آگهی)؛ بقیه الگوی تنبل دارند و در بازدید بعدی تکرارپذیرِ امن‌اند |
| ادمین همزمان روی یک بازیکن | اکشن‌ها تراکنشی/ایدمپوتنت؛ `AdminLog` ثبت می‌شود |
| کال‌بک کهنه (پنل قدیمی) | سرویس‌ها وضعیت فعلی را چک می‌کنند (`status`, `claimedAt`, `isClosed`) و خطای «همین حالا…» می‌دهند |
| موجودی صفر/دقیقاً برابر مرز | کسر شرطی `gte` مرز را درست اداره می‌کند؛ شانسِ ضرر با موجودی ناکافی خنثی می‌شود |

---

## ۱۱) بررسی رگرسیون (Regression Check)

پس از هر دسته اصلاح، چرخهٔ کامل اجرا شد:

| مرحله | نتیجه |
|---|---|
| قبل از اصلاح | ۴۷۱/۴۷۸ قبول (۷ شکست در ۴ سوئیت) |
| بعد از اصلاحات | **۴۸۱/۴۸۱ قبول در ۵۰ سوئیت** ✅ |
| `tsc --noEmit` | پاک ✅ |
| `eslint src tests` | پاک ✅ |
| تست‌های رگرسیون افزوده | ۳ مورد: حفظ وضعیت ثبت‌نام (۲)، ثبت‌نام اتمیک دانشگاه (۱) |

تست‌های به‌روزشده **ضعیف نشدند**: انتظارهای وام به نرخ گرید A (۱۴٪) — که رفتار جاری عمدی است — تنظیم شد؛ فیکسچر اجاره `purchasePrice` گرفت (فیلد غیرقابل‌فرض اسکیما که سرویس عمداً از آن قیمت می‌سازد — ضد نردبان قیمتی); ماک `db.group` برای بافر سیاست شهردار اضافه شد؛ اسکنر `ui-consistency` از مثبت کاذب روی قالب‌های بک‌تیک اصلاح شد.

---

## ۱۲) مسائل باقی‌مانده (Remaining Issues)

| مورد | شدت | دلیل عدم اقدام |
|---|---|---|
| نبود ایندکس یکتای `(groupId, status=OPEN)` برای انتخابات | کم | نیازمند مهاجرت دیتابیس؛ احتمال وقوع ناچیز؛ رفتار فعلی همگرا |
| ۱۲ ماژول/کیبورد منتشرنشده (pet, stock, auction, gym, …) | اطلاعاتی | ✅ در دور دوم با اجازهٔ صریح کاربر به‌طور کامل سیم‌کشی شد |
| `furnishProperty`/`chargeOverdueMaintenance` بدون ورودی UI | اطلاعاتی | ✅ در دور دوم وصل شد (دکمهٔ مبله‌سازی + تسویهٔ تنبل شارژ) |
| `lifeCycle.isDead` متصل نیست | اطلاعاتی | قابلیت جدید |
| کول‌داون ۶۰ ثانیهٔ همگام‌سازی گروه در حالت ردشدن `groupRegistered:false` می‌دهد | اطلاعاتی | پیام کاربر متأثر نیست (بررسی شد) |
| ثبت کد رفرال از `stateData` اگر ثبت‌نام بیش از ۱۲ ساعت طول بکشد و رتنشن پاک کند | اطلاعاتی | سیاست نگهداری موجود؛ پنجرهٔ سخاوتمندانه |

---

## ۱۳) سلامت نهایی پروژه (Final Project Health)

**امتیاز: 🟢 پایدار و آمادهٔ عملیات**

- صحت مالی: پس از اصلاح ۷ باگ، **هیچ مسیر شناخته‌شده‌ای برای پول‌سازی، دوبارپرداخت یا سوختن پول** باقی نمانده است.
- پوشش تست: ۵۰ سوئیت/۴۸۱ تست سبز + ۳ تست رگرسیون جدید.
- انضباط کد: strict TS بدون خطا، ESLint پاک، الگوهای مالی یکدست.
- امنیت: مالکیت سمت سرور در همهٔ مسیرهای حساس؛ بدون تزریق؛ بدون نشت دادهٔ بین گروه‌ها.
- ریسک‌های باز: فقط موارد «اطلاعاتی/کم» جدول بالا.

**فهرست فایل‌های تغییریافته (۲۴ فایل):**
`src/`: handlers (text, expansion), keyboards/main.keyboard, help-content, middleware/rate-limit, profile-photo, repositories (housing, player), services (deposit, elections, lottery, education, marriage, trade, ad, leaderboard, registration), container · `tests/`: ۶ فایل

— پایان گزارش دور اول —

---
---

# گزارش دور دوم — ممیزی یکپارچهٔ کل سیستم و تکمیل ماژول‌های ناتمام

تاریخ: ۱۴۰۴-۰۶-۱۷ (2026-09-08) · همان شاخه · ادامهٔ ممیزی با مجوز صریح کاربر برای تکمیل همهٔ بخش‌های ناقص

## ۱) خلاصهٔ دور دوم

در این دور، کل پروژه به‌عنوان **یک سیستم یکپارچه** بازبینی شد: نه فایل‌به‌فایل، بلکه با تمرکز بر **اتصال بین سیستم‌ها** (سرویس → کیبورد → هندلر → متن/راهنما). نتیجه: **۱۳ سیستم کاملاً ساخته‌شده ولی قطع از رابط کاربری** شناسایی و به‌طور کامل سیم‌کشی شدند + ۳ شکاف بین‌سیستمی دیگر اصلاح شد.

**وضعیت نهایی دور دوم:**
- `tsc --noEmit` → پاک ✅
- `eslint src tests` → پاک ✅
- `jest` → **۵۰ سوئیت / ۵۱۹ تست، همه سبز** ✅ (۳۸ تست جدید)

## ۲) یافتهٔ اصلی: ۱۳ سیستم یتیم (ساخته‌شده، ولی بی‌سیم)

برای این سیستم‌ها لایهٔ سرویس، رندر پنل و کیبورد **کامل** وجود داشت اما هیچ هندلری ثبت نشده بود؛ یعنی بازیکن هرگز نمی‌توانست به آن‌ها برسد:

| سیستم | سرویس | پیشوند کال‌بک | وضعیت قبل | وضعیت بعد |
|---|---|---|---|---|
| 👪 خانواده/ازدواج | `marriageService` | `fam:*` | بدون هندلر | کامل |
| 🐾 حیوان خانگی | `petService` | `pet:*` | بدون هندلر | کامل |
| 📈 بورس | `stockService` | `stk:*` | بدون هندلر | کامل |
| 🔨 حراجی هفتگی | `auctionService` | `auc:*` | بدون هندلر | کامل |
| 💪 باشگاه | `gymService` | `gym:*` | بدون هندلر | کامل |
| 🤝 قرض بازیکنی | `playerLoanService` | `loan:*` | بدون هندلر | کامل |
| 🌱 مزرعه | `farmService` | `farm:*` | بدون هندلر | کامل |
| 🛠️ کارگاه | `craftService` | `craft:*` | بدون هندلر | کامل |
| 🎯 چالش منطقه | `challengeService` | `chal:*` | بدون هندلر | کامل |
| 📜 سیاست شهردار | `policyService` | `pol:*` | بدون هندلر | کامل |
| 🏢 شعبه‌ها | `branchService` | `br:*` | بدون هندلر | کامل |
| 📣 آگهی همگانی | `adService` | `ad:*` | بدون هندلر | کامل |
| 📊 گزارش هفتگی | `reportService` | `report:main` | بدون هندلر | کامل |

**نحوهٔ تکمیل (هم‌راستا با معماری موجود):**
- فایل جدید `src/bot/handlers/features.handler.ts` (~۱۲۰۰ خط): ثبت همهٔ کال‌بک‌ها با همان الگوی حاکم (`ackCallback` + `editPanel` + `handleCallbackError` فقط با `persianMessage`).
- **جریان‌های ورودی متنی** (که کیبوردها از قبل برایشان دکمهٔ انصراف داشتند ولی کدشان نبود): خواستگاری (ریپلای + مبلغ مهریه)، نام حیوان، درخواست قرض (ریپلای به وام‌دهنده + مبلغ)، پیشنهاد حراج، متن آگهی — همه با Context در `UserState` و انصراف امن.
- **نقاط ورود**: ۲۹ کلمهٔ فارسی جدید در روتر متنی + دکمه در منوهای موجود (شهر، بانک، خانه، درمانگاه، کار، آمار) بدون ساخت منوی موازی.
- **راهنما**: ۱۳ موضوع راهنمای تازه با اعداد واقعی (از روی ثابت‌های سرویس‌ها) + فصل جدید «بازار»؛ محدودیت‌های تست راهنما (≤۸ موضوع در فصل، ≤۱۶ خط پنل) حفظ شد.
- بخش‌های جدید به `VALID_ACTIVITY_SECTIONS` اضافه شدند تا اقامت/فعالیت مثل بقیهٔ بخش‌ها ثبت شود.

**اصلاح‌های طراحی حین سیم‌کشی (ریشه‌ای، نه ظاهری):**
- `br:open` قبلاً فقط «اولین کسب‌وکار» را برمی‌داشت؛ جریان انتخاب کسب‌وکار (`br:pick`) برای مالکان چند شرکت اضافه شد.
- دکمهٔ «درخواست قرض جدید» و دو کیبورد انصراف (نام حیوان، درخواست قرض) که کیبوردهای موجود فرضشان را داشتند ولی وجود نداشت، ساخته شد.
- پنل حیوان پس از غذا/هدیه با وضعیت دقیق تازه‌سازی می‌شود (نه حدس `canFeed`).

## ۳) شکاف‌های بین‌سیستمی کشف‌شده و اصلاح‌شده

1. **چالش منطقه هرگز جلو نمی‌رفت** — `challengeService.addProgress` هیچ فراخوانی نداشت؛ حالا هر فعالیت گروهی معتبر (همان مسیر مُهر سفر و کارت گروهی) یک واحد پیشرفت می‌دهد. بدون چالش، پاداش ۱۵۰ هزار تومانی‌اش عملاً حذف بود.
2. **گزارش هفتگی هرگز به اعلان‌ها نمی‌رفت** — `reportService.sendToNotifications` بی‌فراخوان بود؛ حالا بازکردن پنل گزارش، ارسال هفتگی را با کلید یکتا (هر هفته یک‌بار) انجام می‌دهد.
3. **ازدواج بدون خبررسانی** — پیشنهاد/پذیرش/رد/طلاق به طرف مقابل اطلاع نمی‌رسید و پیشنهاد در پنل گم می‌شد؛ `notificationService` (اختیاری، مثل `playerLoanService`) به `MarriageService` تزریق شد.
4. **مسکن: دو تابع کامل ولی قطع** — `furnishProperty` (مبله‌سازی ۵ میلیونی با اثر واقعی روی کیفیت استراحت) دکمه گرفت؛ `chargeOverdueMaintenance` (شارژ ۳۰ روزهٔ ۰٫۵٪) به‌صورت **تسویهٔ Lazy** هنگام باز شدن پنل خانه وصل شد — بدون تایمر جدید، طبق قرارداد معماری.

## ۴) تأیید سلامت مسیرهای بین‌سیستمی (بدون نیاز به اصلاح)

بازبینی شد و سالم بود: هر ۷ تریگر کوئست روزانه در نقاط درست فراخوانی می‌شوند · پاداش رفرال دوطرفه و اتمیک است · مُهر سفر در فعالیت گروهی ثبت می‌شود · موتور دستاوردها همهٔ کلیدها را می‌سنجد · انقضای اجاره در همهٔ ۸ نقطهٔ خواندن با فیلتر تاریخ به‌صورت تنبل مدیریت می‌شود · حراجی با Escrow + شرط مقدار فعلی (ضد رقابت) کار می‌کند · قرض بازیکنی نکول تنبل و اعتبار اجتماعی دارد.

## ۵) تصمیم‌های آگاهانه (انجام نشد — با دلیل)

| مورد | دلیل |
|---|---|
| فعال‌سازی `lifeCycle.isDead` (مرگ شخصیت) | مکانیک جدید گیم‌پلی بدون هیچ لایهٔ UI/بازگشتی؛ ابداع قابلیت یک‌طرفه ممنوع. تابع کتابخانه‌ای باقی می‌ماند و بقیهٔ سرویس (`getLifeStage`, `formatHumanHealth`) در شناسنامه فعال است. |
| مهاجرت ایندکس یکتای انتخابات | مانند دور اول: نیازمند مهاجرت دیتابیس؛ رفتار فعلی همگرا و کم‌ریسک. |

## ۶) آزمون‌های اضافه‌شده در دور دوم

- «هیچ دکمهٔ مرده‌ای در سیستم‌های تکمیل‌شده نیست»: ۳۷ کیبورد (از جمله همهٔ حالت‌های خانواده/حیوان/بورس/حراج/قرض/مزرعه/کارگاه/چالش/سیاست/شعبه/آگهی + منوهای تغییریافته شهر/بانک/خانه/کار/آمار) × همهٔ دکمه‌ها → هندلر زنده.
- «هر نژاد حیوان دکمهٔ سرپرستی دارد» و «هر سیاست شهردار قابل فعال‌سازی است».
- منبع اسکن هندلرهای تست‌های مسیریابی (callback/urban/daily-loop/ui-consistency) با `features.handler.ts` به‌روز شد.

## ۷) سلامت نهایی پس از دور دوم

**امتیاز: 🟢 کامل‌ترین وضعیت ممکن پروژه** — هیچ سیستم ساخته‌شده‌ای بی‌اتصال نمانده، هیچ دکمه‌ای مرده نیست، همهٔ جریان‌های مالی اتمیک‌اند و راهنما با رفتار واقعی هم‌خوان است.

**فایل‌های تغییریافتهٔ دور دوم:**
`src/bot/handlers/features.handler.ts` (جدید) · `features→bot.ts` · `text.handler.ts` · `keyboards/main.keyboard.ts` · `help-content.ts` · `modules/activity/activity.service.ts` · `modules/family/marriage.service.ts` · `services/container.ts` · `tests/`: callback-routing, civic-market-routing, daily-loop-routing, ui-consistency

— پایان گزارش دور دوم —

---

# 🟢 دور سوم — ممیزی عمیق نهایی و پایدارسازی (2026-09-08)

> بازخوانی کامل منبع از صفر با این پیش‌فرض که «هیچ تغییر قبلی لزوماً درست نیست»؛ هر مسیر از ورودی تا دیتابیس دوباره ردیابی شد. ممیزی پنج‌گذره: ایستا → منطقی → رفتاری → یکپارچگی/رگرسیون → کارایی.

## ۱) مشکل بحرانی کشف‌شده در دور سوم

**شناسهٔ گروه به‌جای شناسهٔ داخلی (خطر کرش P2003 در تولید):**
`openFeaturePanel` و ۶ کال‌بک گروهیِ `features.handler.ts` — یعنی `auc:main`، `auc:start_bid`، `auc:cancel_bid`، `chal:main`، `pol:main`، `pol:set` — مقدار `String(ctx.chat!.id)` (عدد عددی چت تلگرام) را به سرویس‌هایی می‌دادند که **شناسهٔ داخلی `Group.id` (uuid)** می‌خواهند:
- `RegionalChallenge.groupId` و `Auction.groupId` هر دو **FK به `Group.id`** هستند → ساخت حراجی/چالش با خطای خارجی‌کلید کرش می‌کرد؛
- `policy.setPolicy` با `db.group.update({where:{id}})` کار می‌کند → با شناسهٔ تلگرام هرگز اثر نمی‌کرد (شکست خاموش).

**اصلاح ریشه‌ای:** افزودن `resolveFeatureGroup(ctx, container)` (از مسیر `groupRepository.findByTelegramGroupId` → `group.id`) و `groupNotRegisteredAck`؛ هر ۸ نقطهٔ فراخوانی به الگوی جدید وصل شدند و برای گروه ثبت‌نشده، پنل/پیام دوستانهٔ «گروه ثبت‌نشده» نمایش داده می‌شود. باقی‌ماندهٔ `String(ctx.chat!.id)` در کل هندلرها: **صفر** (تأیید با grep).

## ۲) تزریق متن آزاد به پنل‌های مارک‌داون (رفع نقص اعتبارسنجی ورودی)

هیچ کمک‌تابعی برای پاک‌سازی مارک‌داون در پروژه وجود نداشت؛ متن آزاد کاربر (نام حیوان، متن آگهی، بیوگرافی و نام تلگرامی) بدون فرار در پنل‌های `parse_mode: Markdown` جا می‌گرفت و یک `*` یا `_` تنها، خطای «can't parse entities» و حذف خاموش پنل را باعث می‌شد.

**اصلاح در مرز ورودی (ریشه):**
- `plainInput()` در `src/utils/validation.ts` (حذف `* _ \` [ ]` + بریدن فاصله‌ها) — با بازنشر از `ui-kit.ts` برای لایهٔ ربات؛
- اعمال هنگام دریافت: نام حیوان (`handlePetNameText`) و متن آگهی (`handleAdText`)؛
- اعمال در مرز نوشتن ثبت‌نام: `firstName`/`lastName`/`biography` در `submitBiography` (با بررسی دوبارهٔ حداقل طول پس از پاک‌سازی و برچسب جایگزین «کاربر» برای نام‌های تمام‌نویسه‌ای) — محافظ همهٔ پنل‌های بالادست: شناسنامه، لیدربورد، حراجی، انتخابات، خانواده، خبر.

## ۳) بازتأیید مستقل (بدون تغییر — سالم)

- **خوانش کامل دوبارهٔ `features.handler.ts`** (هر ۱۳ سیستم: خانواده، حیوان، بورس، حراج، باشگاه، قرض، مزرعه، کارگاه، چالش، سیاست، شعبه، آگهی، گزارش) — پس از اصلاح بالا همهٔ کال‌بک‌ها درست.
- **`text.handler.ts`:** ترتیب دیسپچ (زمینه‌های ورودی قبل از ثبت‌نام و نقشهٔ کلمات)، هر ۵ جریان ورودی، `house:furnish` و `house:my_list`؛ پوشش کامل `EXACT_SECTIONS` — هیچ کلمه‌ای بی‌پاسخ نمی‌ماند (۱۳ بخش گیم‌پلی از پل `openFeaturePanel` و بقیه از `buildSectionReply`).
- **`start.handler.ts` / `group.handler.ts` / میدل‌ورهای خطا و نرخ:** ثبت‌نام آیدمپوتنت، تسویهٔ رفرال هرگز ثبت‌نام را نمی‌شکند؛ همگام‌سازی گروه با خنک‌کننده و هرس حافظه؛ پاسخ فارسی به خطاهای عملیاتی و اعلان به کلیک‌های ریت‌لیمیت‌شده.
- **پویش سراسری دکمهٔ مرده:** ۹۶ `callback_data` ایستا + همهٔ پیشوندهای پویا × همهٔ هندلرهای ثبت‌شده → **صفر دکمهٔ مرده**.
- **پویش یکپارچگی مالی:** هر ۳۲ `decrement` محافظ‌شده است (۲۶ برداشت کیف پول با شرط `gte:` + `updateMany.count===1`؛ کاهش موجودی/کالا با شرط `gt:0`؛ کاهش سلامت با `Math.min(player.health,…)`).
- **هیچ `delete()` دیتابیسی در پروژه نیست** → خطر آبشاریِ حذف بازیکن/گروه صفر؛ `resetState` ادمین فقط به‌روزرسانی ستون است.
- **پویش کوئری بی‌کران:** هیچ `findMany` بدون `take` روی جدول‌های رشدکننده؛ تنها تایمر پروژه، نگهداشت `app.ts` (زیرساختی).
- **اتمیک بودن حراجی:** بدهی شرطی → به‌روزرسانی مقید به `currentBid` فعلی (ضد رقابت) → بازپرداخت نفر قبلی → ثبت پیشنهاد؛ در رقابت، پول پیشنهاددهندهٔ جدید در همان تراکنش برمی‌گردد.
- **راهنما (`help-content.ts`):** اعداد همهٔ سیستم‌های جدید با ثابت‌های سرویس راستی‌آزمایی شد (مزرعه ۳۰۰/۷۰۰/۲۰۰۰ با برداشت ۳۷۰/۸۸۰/۲۶۰۰ · گام حراج ۵٪ · باشگاه ۸۰۰ هفته/سقف ۱۲۰ · قرض ۱۰٪/۸٪/۲٪/۷ روز · آگهی ۲ میلیون/۹۰ نویسه/روزی یک · شعبه ۱۵ میلیون/۴۰۰ هزار روزانه/حداکثر ۲ · چالش ۱۵۰ هزار).

## ۴) آزمون و اعتبارسنجی نهایی

- آزمون جدید: ۲ مورد برای `plainInput` در `tests/validation.test.ts`.
- **`tsc --noEmit`: پاک · `eslint`: پاک · `jest`: ۵۰ سوئیت / ۵۲۱ آزمون سبز.**

## ۵) باقی‌مانده‌های مستندشده (بدون رگرسیون، آگاهانه)

رقابت نظری دو درخواست‌بازِ همزمان در انتخابات و قرض بازیکنی · رقابت نظری خرید-آپسرت بورس · شاخهٔ مردهٔ `'home'` در `buildSectionReply` (غیرقابل‌رسید) · اقرار مضاعف خبر · پنجرهٔ ۱۲ ساعتهٔ نگهداشت · `lifeCycle.isDead` خاموش به تصمیم محصول · بازیکنان قدیمی نام ذخیره‌شدهٔ خود را حفظ می‌کنند (پاک‌سازی فقط در مرز نوشتن برای ثبت‌های جدید؛ مسئله صرفاً نمایشی).

**فایل‌های تغییریافتهٔ دور سوم:**
`src/bot/handlers/features.handler.ts` · `src/bot/handlers/text.handler.ts` · `src/bot/ui-kit.ts` · `src/utils/validation.ts` · `src/modules/registration/registration.service.ts` · `tests/validation.test.ts`

— پایان گزارش دور سوم —

---

# 🟢 دور چهارم — مهریه، ثبت گروه، پنل ادمین دیتابیسی و رفع انحراف اسکیما (2026-09-10)

## ۱) خلاصهٔ دور چهارم

سه نقص ریشه‌ای که در دورهای پیش دیده نشده بودند: (۱) قانون مهریه در چهار جا با هم
متضاد بود، (۲) ثبت گروه در اولین `/start` هرگز در دیتابیس ذخیره نمی‌شد و «سطح محیط:
نامشخص» از همین‌جا می‌آمد، (۳) پنل ادمین روی یک فهرست env سوار بود و دسترسی مدیریت
در `callback_data` نهفته بود. در این دور هر سه از ریشه اصلاح شدند و انحراف
اسکیما/مهاجرت نیز صفر شد.

## ۲) مهریه — تضاد چهار منبع برطرف شد

**ریشهٔ باگ:** `parseAmountInput` برای هر `value <= 0` مقدار `null` برمی‌گرداند، پس
ورودِ `۰` (که سه متنِ راهنما صریحاً برای «مهریه تعیین‌نشده» خواسته بودند) با پیام
«مبلغ نامعتبر» رد می‌شد. در همان حال متن راهنما کف ۵۰۰٬۰۰۰ تومان و «بدون سقف» را
اعلام می‌کرد، در حالی که کد سقف `1e12` را اعمال می‌کرد. یعنی ۵۰۰٬۰۰۰ پذیرفته می‌شد
و کمتر از آن به‌غلط رد — دقیقاً همان رفتار گزارش‌شده.

**اصلاح (منبع واحد حقیقت):** `src/modules/family/mahr.ts` تنها جایی است که قانون
مهریه تعریف می‌شود (`MAHR_RULES = { min: 1, max: 1e12, unset: 0 }`). اعتبارسنجی،
متن راهنما و متن اعتبارسنجی همه از همین ثابت می‌خوانند. طبق قانون محصول، زن مهریه
را تعیین می‌کند و بازی سقف مصنوعی نمی‌گذارد؛ مرز بالا فقط محافظ سرریز عددی است.

**سایر نقص‌های مسیر مهریه:**
- `propose()` الگوی count-then-create داشت (TOCTOU) → تراکنش `Serializable` + رسیدگی به `P2034`.
- `claimCoupleBonus` شرط `lastBonusDayIndex: { not: day }` روی مقدار NULL هرگز تطبیق نمی‌شد.
- `getView()` در هر باز شدن پنل یک `expireStale()` سراسری اجرا می‌کرد → حذف (تسویهٔ تنبل در نقطهٔ ورود).
- راهنمای گام ۱ «بازیکن آزاد را انتخاب کن» می‌گفت در حالی که جریان واقعی ریپلای‌به‌پیام است.

## ۳) ثبت گروه و سطح محیط

**ریشهٔ باگ:** `getChatAdministrators` نیاز به دسترسی ادمین ربات دارد و داخل
`Promise.all` استثنا می‌انداخت؛ در نتیجه `ensureGroupUpdated` پیش از نوشتن
`upsert` رها می‌شد و گروه هرگز ثبت نمی‌شد.

**اصلاح:** `src/bot/member-info.provider.ts` بازنویسی شد تا «تلگرام عدد را نداد» را
با `null` برگرداند (نه استثنا، و نه صفر). در `GroupService` عدد `null` هرگز مقدار
ذخیره‌شده را بازنویسی نمی‌کند. سطح محیط تنها از
`EnvironmentClassifier.classify(realMemberCount)` محاسبه می‌شود و
`realMemberCount = computeRealMemberCount(totalCount, botAdminCount)`.

**حذف تکرار قانون:** `RegisterGroupInput.memberCount` فیلدی مرده بود (هیچ‌جا خوانده
نمی‌شد) و `registerGroup()` هم در کد محصول صدا زده نمی‌شد، در حالی که محاسبهٔ سطح
محیط را دوباره پیاده می‌کرد. هر دو حذف شدند تا `ensureGroupUpdated` تنها مسیر و
تنها منبع محاسبه باشد.

## ۴) انحراف اسکیما/مهاجرت — صفر شد

`prisma migrate diff --from-url … --to-schema-datamodel …` پیش از این دور ۶۶ خط
انحراف می‌داد. سه شکاف واقعی بود:

| شکاف | اثر | اصلاح |
| :--- | :--- | :--- |
| نبود UNIQUE روی `referrals.referee_player_id` | ثبت رفرال تکراری برای یک نفر | مهاجرت `20260910000001_missing_unique_indexes` |
| نبود UNIQUE روی `region_projects(group_id, key)` | پروژهٔ منطقه‌ای تکراری | همان مهاجرت |
| ۹ رابطهٔ خارجی که در DB آبشاری بودند و در اسکیما RESTRICT | `prisma db push` می‌توانست آبشار را خاموش کند | `onDelete: Cascade` در اسکیما + بازسازی FK `pets` |

**خطر عملیاتی:** `package.json` روی `start` دستور `prisma db push --skip-generate`
داشت. چون برخی اشیاء (مثل ایندکس جزئی مهاجرت
`20260908000001_one_active_loan_per_player`) فقط در SQL خام وجود دارند، `db push`
آن‌ها را حذف می‌کرد. به `prisma migrate deploy` تغییر یافت.

## ۵) پنل ادمین — از فهرست env به دیتابیس

**چرا قبلی ناکافی بود:** دسترسی از `ADMIN_USER_IDS` خوانده می‌شد (تغییر آن نیاز به
دیپلوی داشت)، ادمین‌شدن کاربر جدید ممکن نبود، و دسترسی در خودِ `callback_data`
رمزگذاری شده بود.

**الگوی جدید:**
- جدول `bot_admins` (مهاجرت `20260910000000_bot_admins`) منبع حقیقت است؛
  `ADMIN_USER_IDS` از `env.ts` حذف شد.
- ادمین‌های اولیه `6910416744` (OWNER) و `8369939024` (ADMIN) با `bootstrap()`
  آیدمپوتنت ساخته می‌شوند و با `update: {}` هرگز ردیف موجود بازنویسی نمی‌شود
  (ادمین حذف‌شده خودکار برنمی‌گردد).
- عبارت‌ها: `پنل ادمین` · `ادمین ها` · `اضافه ادمین` · `حذف ادمین` — در چت خصوصی
  و هر گروهی که ربات عضو است. کاربر غیرادمین **هیچ** پاسخی نمی‌گیرد (عبارت مصرف
  می‌شود ولی بی‌صدا) تا وجود پنل فاش نشود.
- ناوبری با `editPanel` روی همان پیام؛ یک اقدام = یک پنل = یک پیام.
- دسترسی همیشه سمت سرور از دیتابیس سنجیده می‌شود؛ `callback_data` فقط «چه چیزی»
  را می‌گوید.
- مدیریت ادمین‌ها فقط OWNER؛ ادمین اصلی حذف‌شدنی نیست؛ `removeAdmin` فقط
  `isActive` را خاموش می‌کند و هیچ نوشتاری روی `players` انجام نمی‌دهد.
- هر جهش در `$transaction` با نوشتار شرطی `updateMany.count === 1` و یک ردیف
  `AdminLog` با مقدار قبل/بعد.
- مرزهای فیلدها در `ADMIN_FIELD_LIMITS` و متن راهنما از `fieldLimitText()` —
  پس پیام و اعتبارسنج هرگز از هم جدا نمی‌افتند.

**باگی که در همین دور پیدا و رفع شد:** `adjustBalance` با `assertPositiveAmount`
هر دلتای منفی را رد می‌کرد، پس دکمهٔ «کسر موجودی» همیشه شکست می‌خورد و شاخه‌های
`delta < 0` کد مرده بودند → `assertNonZeroAmount` با محافظ مرز.

## ۶) سایر اصلاح‌ها

- متن راهنمای عمومی هنوز `/admin` را تبلیغ می‌کرد (دستوری که حذف شده بود و وجود
  پنل را به همه لو می‌داد) → به `/start` کاهش یافت.
- `getDashboardMetrics` پیش‌تر ۱۰۰۰ ردیف تراکنش می‌خواند و در Node جمع می‌زد →
  `aggregate._sum` در دیتابیس.
- `maritalAdminLabels` دست‌ساز و نمایش جنسیت هاردکد شده در پنل ادمین → استفاده از
  `maritalStatusLabels` و `genderLabels` موجود پروژه.
- متن داشبورد در دو جا کپی شده بود → `renderAdminDashboardText` تنها منبع.

## ۷) آزمون و اعتبارسنجی نهایی

- آزمون‌های تازه: `tests/admin-panel.test.ts` (۱۵ مورد — عبارت‌ها، بی‌صدا بودن
  کاربر عادی، OWNER-only، تأیید پیش از حذف)، بازنویسی `tests/admin.service.test.ts`
  (۳۷ مورد روی دیتابیس ماک)، به‌روزرسانی `tests/marriage.test.ts` و
  `tests/group.service.test.ts`، افزودن `admin.handler.ts` به
  `HANDLER_SOURCES` در `tests/callback-routing.test.ts`.
- **راستی‌آزمایی روی PostgreSQL زنده:** هر ۱۵ مهاجرت از صفر اعمال شد و
  `migrate diff` خروجی «مهاجرت خالی» داد؛ سپس `AdminService` واقعی (نه ماک) روی همان
  دیتابیس اجرا شد: ۳۶ ادعا سبز — bootstrap آیدمپوتنت، OWNER/ADMIN، افزودن/حذف
  ادمین، افزایش **و کسر** موجودی، ردِ کسری که موجودی را منفی می‌کند، مرزهای
  `setField`، مسدودسازی با توقف فعالیت، و ثبت `AdminLog` با قبل/بعد.
- **`tsc --noEmit`: پاک · `eslint src tests`: پاک · `jest`: ۵۵ سوئیت / ۶۴۶ آزمون سبز · `dist` ساخته و بارگذاری شد.**

**فایل‌های تغییریافتهٔ دور چهارم:**
`prisma/schema.prisma` · `prisma/migrations/20260910000000_bot_admins/` ·
`prisma/migrations/20260910000001_missing_unique_indexes/` ·
`src/bot/handlers/admin.handler.ts` (تازه) · `src/modules/admin/admin.service.ts` ·
`src/modules/family/mahr.ts` (تازه) · `src/modules/family/marriage.service.ts` ·
`src/utils/format.ts` (تازه) · `src/bot/member-info.provider.ts` ·
`src/modules/groups/group.service.ts` · `src/bot/handlers/group.handler.ts` ·
`src/bot/handlers/text.handler.ts` · `src/bot/handlers/features.handler.ts` ·
`src/bot/handlers/start.handler.ts` · `src/bot/keyboards/main.keyboard.ts` ·
`src/bot/help-content.ts` · `src/bot/renders.ts` · `src/bot/ui-kit.ts` ·
`src/bot/bot.ts` · `src/app.ts` · `src/config/env.ts` · `src/services/container.ts` ·
`src/utils/commands.ts` · `src/database/repositories/{group,player}.repository.ts` ·
`package.json` · `README.md`

— پایان گزارش دور چهارم —

---

# 🟢 دور پنجم — دیباگ عمیقِ پس از پیاده‌سازی (2026-09-10)

این دور فقط «بازخوانی» نبود: کدِ تازه‌نوشته با سه روش مستقل اجرا شد —
(۱) آزمون یکپارچه روی Bot واقعی grammy با `bot.handleUpdate`، (۲) اجرای سرویس‌های
واقعی روی PostgreSQL زنده، (۳) پویش ایستایی دکمه‌های مرده. چهار باگ واقعی پیدا و
از ریشه رفع شد.

## ۱) `propose(mahr = 0)` استثنا می‌داد — ناسازگاری دامنهٔ ورودی/خروجی

**یافته (با اجرای واقعی روی دیتابیس):** `MAHR_RULES.unset` برابر `۰` است و خروجی
`propose` برای حالت «تعیین‌نشده» هم `۰` برمی‌گشت، ولی ورودیِ `۰` با
`ValidationError: Mahr out of range` رد می‌شد و فقط `null` کار می‌کرد. یعنی یک تابع
مقداری را که خودش تولید می‌کند نمی‌پذیرفت.

مسیر UI سالم بود (هندلر `unset` را به `null` نگاشت می‌کرد) — ولی این دقیقاً همان
دامِ «یک قاعده در دو جا» است که در دور چهارم قرار بود از بین برود، و برای هر
فراخوان بعدی تله بود. آزمون‌های واحد فقط `null` و `۱` را پوشش می‌دادند.

**ریشه:** `validateMahr` فقط `null` را حالت خاص می‌گرفت.
**اصلاح:** `normalizeMahrOffer()` در `mahr.ts` (منبع حقیقت) هر دو املا را به یک مقدار
نرمال می‌برد؛ `MarriageService` دیگر قاعدهٔ موازی ندارد.

## ۲) «کسر موجودی» صفر و پیام متناقض در پنل ادمین

**الف)** `allowZero` به *فیلد* گره خورده بود نه به *عملیات*؛ پس «تنظیم موجودی» روی
صفر (خالی‌کردن کیف پول) رد می‌شد، در حالی که «افزودن موجودی» با صفر هم باید رد شود.
**ب)** وقتی صفر رد می‌شد، پیام می‌گفت «باید بین ۰ تا ۱٬۰۰۰٬۰۰۰٬۰۰۰٬۰۰۰ باشد» — یعنی
بازه‌ای را اعلام می‌کرد که خودش نقضش کرده بود. این همان کلاسِ تضادِ متن/رفتار است
که در مهریه باگ ساخت.

**اصلاح:** `allowZero` از روی عملیات (دلتا در برابر مقدار مطلق) تعیین می‌شود و
`amountErrorText()` برای هر شش دلیلِ `AmountInvalidReason` پیام جداگانه دارد؛
`switch` بدون `default` است تا افزودن دلیل تازه خطای typecheck بدهد.

## ۳) `isIgnorableEditError` روی `description` تهی می‌ترکید

**یافته (با اجرای واقعی):** `toGrammyError` در grammy مقدار `description` را مستقیم
از بدنهٔ پاسخ تلگرام برمی‌دارد؛ در تایپ `string` است ولی در عمل می‌تواند `undefined`
شود. آن‌گاه `error.description.toLowerCase()` داخل بلوک `catch`ِ `editPanel`
`TypeError` می‌انداخت — یعنی خودِ مدیریت خطا می‌ترکید و به‌جای نادیده‌گرفتنِ بی‌خطرِ
«message is not modified» یک خطای عمومی به کاربر می‌رسید.

**اصلاح:** `(error.description ?? '')`.

## ۴) اعلام «گروه ثبت نشده» روی خطای موقت دیتابیس

`renderGroupInfoPanel` وقتی `group === null` بود می‌گفت «این گروه هنوز در دیتابیس ثبت
نشده است»؛ ولی `null` هم از «ردیفی نیست» می‌آید و هم از «نتوانستیم بخوانیم».
`syncFailed` موجود بود ولی استفاده نمی‌شد. حالا این دو حالت پیام جداگانه دارند.

## ۵) پویش ایستایی: ۳۹ دکمهٔ پنل ادمین

تست‌های موجود فقط `main.keyboard.ts` را پویش می‌کردند؛ دکمه‌های `admin.handler.ts`
بی‌پوشش بودند. پویش تازه اضافه شد: هر ۳۹ `callback_data` تولیدشده به یک هندلر زنده
می‌رسد، هر ۱۳ الگوی `^adm:` از حداقل یک دکمه قابل‌رسیدن است، و هر شش عملیات مخرب
از `adm:confirm` می‌گذرد.

> یک مورد که **باگ نبود** و صریحاً رد شد: `adm:degree:${level}` در نگاه اول مرده به
> نظر می‌رسید، ولی مقادیر `DegreeLevel` همه بزرگ‌اند و با `[A-Z]+` می‌خوانند. تستِ
> خامِ خودم جای‌گذاری درستی نداشت؛ حالا از خودِ enum مقدار می‌گیرد.

## ۶) راستی‌آزمایی روی PostgreSQL زنده

- **مهریه (۱۹ ادعا):** پارسر (۰/۱/۵۰۰ هزار/`1.5M`/سرریز) · `propose(0)` → تعیین‌نشده ·
  ردِ پذیرفتنِ پیشنهادِ تعیین‌نشده · `setMahr(1)` → `MAHR_SET` · `confirmProposal` →
  انتقال `۱` تومان از مرد به زن، هر دو `MARRIED`، یک ردیف دفتر مالی، ردیف `marriage`
  با مهریهٔ درست · ردِ پیشنهاد از شخص متأهل.
- **پنل ادمین (۳۶ ادعا، دور چهارم):** bootstrap آیدمپوتنت، OWNER/ADMIN، افزودن/حذف
  ادمین، افزایش **و کسر** موجودی، مرزهای `setField`، مسدودسازی با توقف فعالیت،
  `AdminLog` با قبل/بعد.

## ۷) آزمون یکپارچهٔ هندلرهای callback

`tests/admin-callbacks.test.ts` (۱۳ مورد) خودِ `registerAdminHandlers` را روی یک Bot
واقعی grammy اجرا می‌کند و Update ساختگی را از `bot.handleUpdate` می‌گذراند؛ پس
`guardAdmin`، `ackCallback`، رندر پنل و `editPanel` همه واقعاً اجرا می‌شوند.
پوشش می‌دهد: بی‌پاسخ/هشدار برای غیرادمین، OWNER-only بودن فهرست ادمین‌ها، ردِ
`adm:run:admin_remove` جعلی از یک ادمین عادی، ویرایش درجا (بدون `sendMessage` اضافه)،
و نبودِ `undefined|null|NaN|[object Object]` در متن پنل.

## ۸) وضعیت نهایی

**`tsc --noEmit`: پاک · `eslint src tests`: پاک · `jest`: ۵۶ سوئیت / ۶۷۶ آزمون سبز
(پایهٔ پروژه ۵۹۵ بود) · `dist` ساخته و بارگذاری شد.**

**فایل‌های تغییریافتهٔ دور پنجم:**
`src/modules/family/mahr.ts` · `src/modules/family/marriage.service.ts` ·
`src/bot/handlers/admin.handler.ts` · `src/bot/panel.ts` ·
`src/bot/handlers/group.handler.ts` · `tests/admin-callbacks.test.ts` (تازه) ·
`tests/admin-panel.test.ts` · `tests/marriage.test.ts` · `tests/callback-routing.test.ts`

— پایان گزارش دور پنجم —

---

# 🟢 دور ششم — دیباگ امنیت/مقیاس و راستی‌آزمایی استقرار (2026-09-10)

## ۱) تزریق مارک‌داون از راه نام تلگرام (نقص امنیتی/نمایشی)

**یافته:** `repliedTelegramUser` مقدار خام `first_name` تلگرام را می‌گرفت،
`addAdmin` آن را در `bot_admins.first_name` ذخیره می‌کرد و `renderAdminListPanel`
آن را در `*${name}*` رندر می‌کرد. نام تلگرام کاملاً در اختیار کاربر است؛ یک نام
مانند `Ali *[x](t.me/scam)* _bad_` هم پارس مارک‌داون تلگرام را می‌شکست (و کل پنل
ارسال نمی‌شد) و هم لینک دلخواه در پنل ادمین اصلی جا می‌داد.

این **دقیقاً همان کلاس نقصی** است که دور سوم برای ثبت‌نام با `plainInput()` بسته بود،
ولی در مسیر تازهٔ ادمین دوباره باز شده بود.

**اصلاح در مرز ورود:** `plainInput()` روی `first_name` و `username` پیش از ذخیره.
با تست بازتولید (`tests/admin-panel.test.ts`) که جفت‌بودن `*` و نبودِ `[` باز را
در متن رندرشده بررسی می‌کند.

## ۲) پنل فهرست ادمین‌ها از سقف تلگرام رد می‌شد

**یافته (با اندازه‌گیری واقعی):** `listAdmins` هیچ `take` نداشت و
`renderAdminListPanel` چهار خط برای هر ادمین می‌ساخت. با ۱۰۰ ادمین متن
**۹۳۳۰ نویسه** می‌شد؛ سقف تلگرام ۴۰۹۶ است. یعنی پیام رد می‌شد، هر دو fallback هم
رد می‌شدند و ادمین اصلی **هیچ چیز** نمی‌دید. تنها فهرستِ بی‌مرزِ پنل همین بود.

**اصلاح:** `listAdmins` هم‌شکل بقیهٔ فهرست‌ها صفحه‌بندی شد
(`{ total, page, pageSize, items }` با `DEFAULT_PAGE_SIZE`)، کیبورد دکمهٔ
قبلی/بعدی گرفت و `adm:admins` به الگوی `^adm:admins(?::\d+)?$` تبدیل شد تا هم
«ادمین‌ها» و هم دکمه‌های صفحه کار کنند.

## ۳) `حذف ادمین` برای ادمین‌های صفحهٔ دوم کار نمی‌کرد

**یافته:** جریان حذف با `listAdmins(...).find(...)` در فهرست می‌گشت. پس از
صفحه‌بندی (و حتی پیش از آن، با فهرست بزرگ) ادمینی که در صفحهٔ اول نبود پیدا
نمی‌شد و پیام نادرستِ «این کاربر در فهرست ادمین‌ها نیست» نمایش داده می‌شد.

**اصلاح:** خواندن مستقیم با `findAdmin(telegramUserId)` — جست‌وجو در صفحهٔ نمایش
جای پرس‌وجوی مستقیم بر پایهٔ کلید را نمی‌گیرد.

## ۴) راستی‌آزمایی مسیر استقرار

- `prisma migrate deploy` (دقیقاً دستور `npm start` تولید) روی دیتابیس **خالی**
  اجرا شد: هر ۱۵ مهاجرت با CLI خودِ Prisma اعمال شد — نه فقط با
  `offline-deps/scripts/migrate.js`.
- `migrate diff` روی همان دیتابیس: **«مهاجرت خالی»**.
- هر دو ادمین اولیه از خودِ SQL مهاجرت ساخته شدند (`6910416744` OWNER و
  `8369939024` ADMIN) و هر دو ایندکس UNIQUE تازه (`referrals_referee_player_id_key`
  و `region_projects_group_id_key_key`) در `pg_indexes` دیده شدند.
- **بوت واقعی `node dist/app.js`:** «Database connected» → «Starting bot with long
  polling…» → خاموشی تمیز با SIGTERM؛ بدون خطای bootstrap.
- `bot_admins` پس از بوت همچنان ۲ ردیف است → مهاجرت (`ON CONFLICT DO NOTHING`) و
  `bootstrap()` (با `update: {}`) با هم سازگارند و ردیف تکراری نمی‌سازند.
- ریسک بررسی‌شده و رد شده: `npm prune --omit=dev` در Docker ابزار Prisma را حذف
  نمی‌کند، چون `prisma@6.19.3` در `dependencies` است نه `devDependencies`.
- مسیر آفلاین (`install.sh`) از `migrate.js` و `node dist/app.js` استفاده می‌کند و
  هرگز `npm start` را صدا نمی‌زند؛ پس تغییر `start` به `migrate deploy` آن را
  تحت تأثیر قرار نمی‌دهد.

## ۵) بررسی‌هایی که سالم بودند (بدون تغییر)

- **برخورد کلیدواژه:** هیچ‌یک از ۴ عبارت ادمینی با ۱۰۶ کلید `EXACT_SECTIONS`
  نرمال‌شده برخورد ندارد.
- **طول پنل بازیکن:** با ۴۰ مهارت ۱۴۳۶ نویسه — زیر سقف.
- **مهریه:** هر دو مسیر نوشتن (`propose` و `setMahr`) از `validateMahrAmount`
  می‌گذرند؛ هیچ عدد مهریهٔ هاردکدشده‌ای بیرون از `mahr.ts` نمانده است.
- **`faDate(null)`** → `—` (پنل ادمین با `lastActivityAt: null` نمی‌شکند).

## ۶) وضعیت نهایی

**`tsc --noEmit`: پاک · `eslint src tests`: پاک · `jest`: ۵۶ سوئیت / ۶۸۲ آزمون سبز ·
`dist` ساخته شد · `prisma migrate deploy` روی دیتابیس خالی موفق · بوت برنامه بدون خطا.**

**فایل‌های تغییریافتهٔ دور ششم:**
`src/bot/handlers/admin.handler.ts` · `src/modules/admin/admin.service.ts` ·
`tests/admin-panel.test.ts` · `tests/admin.service.test.ts` · `tests/admin-callbacks.test.ts`

— پایان گزارش دور ششم —

---

# 🟢 دور هفتم — بازبینی نهایی عمیق: کاتالوگ مرکزی کلیدواژه‌ها، بازنویسی راهنما و صداقت پنل‌ها (2026-09-12)

## ۱) خلاصهٔ دور هفتم

هدف این دور «بازبینی نهایی» بود: میکروسکوپ روی همهٔ تغییرات اخیر، بازنویسی کامل
راهنما برای بازیکن تازه‌وارد، و یکسان‌سازی سیاست/لحن — **بدون هیچ قابلیت تازه**
(قانون طلایی پروژه). نتیجهٔ عددی:

| شاخص | پیش از این دور | اکنون |
| --- | --- | --- |
| شکل‌های پذیرفته‌شدهٔ کلیدواژه (`EXACT_SECTIONS`) | ۱۱۹ | **۱۵۶** |
| موضوع‌های راهنما (`HELP_TOPICS`) | ۴۹ | **۵۴** |
| فصل‌های راهنما (`HELP_CATEGORIES`) | ۷ | **۸** |
| آزمون‌ها | ۶۰ سوئیت / ۷۱۶ | **۶۱ سوئیت / ۷۲۰** |
| جدول‌های کلیدواژهٔ موازی در کد | ۲ (+ فهرست دستی راهنما) | **۱** |

## ۲) سه نسخهٔ متفاوت از یک جدول → یک منبع حقیقت: `src/bot/command-catalog.ts`

**ریشهٔ مشکل:** `EXACT_SECTIONS` داخل `text.handler.ts` بود، `chat-policy.ts`
جدول سیاست/عنوان/کلیدواژهٔ پیشنهادی خودش را داشت و راهنما فهرست دستورها را
**دستی** می‌نوشت. سه منبع مستقل یعنی هر تغییر باید سه‌جا تکرار می‌شد؛ در عمل هم
تکرار نشده بود (راهنما کلمه‌هایی را معرفی می‌کرد که وجود نداشتند و کلمه‌های
واقعی را جا می‌انداخت).

**رفع ریشه‌ای:** فایل تازهٔ `src/bot/command-catalog.ts` تنها منبع حقیقت شد:
`SECTION_CHAT_POLICY` / `SECTION_TITLES` / `SECTION_KEYWORDS` / `EXACT_SECTIONS`
به‌همراه `sectionPolicy`, `sectionTitle`, `sectionKeyword`, `isSectionAllowedHere`
و `catalogForPolicy(policy)` که برای هر بخش `{section, title, policy, keyword,
synonyms[]}` می‌دهد. `chat-policy.ts` فقط «اجرای» سیاست را بر عهده دارد و همان
جدول‌ها را برای سازگاری با importهای موجود بازصادرات می‌کند (بدون شکستن معماری).

**آزمون‌ها هم از پویش متن منبع نجات پیدا کردند:** چهار سوئیت
(`brand-navigation`, `chat-policy`, `daily-loop-routing`, `status-panel-routing`)
پیش‌تر جدول کلیدواژه‌ها را با regex از *متن سورس* بیرون می‌کشیدند — یعنی اگر
جدول جابه‌جا می‌شد آزمون بی‌صدا سبز می‌ماند. حالا همان جدول واقعی import می‌شود.

**دستاورد پایداری:** تست تازهٔ «راهنما هر کلیدواژهٔ بازیکن را مستند می‌کند» روی
خروجی `catalogForPolicy` اجرا می‌شود؛ پس راهنما دیگر **نمی‌تواند** دستوری را
معرفی کند که در بازی وجود ندارد یا کلیدواژه‌ای را جا بیندازد.

## ۳) شناسهٔ داخلی بخش به بازیکن نشت می‌کرد

`sectionKeyword(section)` برای بخش ناشناخته خودِ شناسهٔ داخلی را برمی‌گرداند و در
جملهٔ «در گروه منطقه‌ات کلمهٔ «auction» را بفرست» ظاهر می‌شد — نقض صریح قاعدهٔ
«هیچ اصطلاح توسعه‌دهنده‌ای به بازیکن نرسد». رفع: تابع حالا `null` برمی‌گرداند و
`keywordHint()` در `chat-policy.ts` در نبود کلیدواژه جملهٔ عمومی انسانی می‌سازد
(«از منوی اصلی وارد این بخش شو»). `groupOnlyAlert` هم همان رفتار را دارد.

## ۴) بازنویسی راهنما (`src/bot/help-content.ts`)

- **فهرست دستورها تولید می‌شود، نه نوشته:** `commandLine()` از کاتالوگ خط
  `آیکون *عنوان* — «کلمهٔ اصلی» یا «مترادف» …` می‌سازد. `visibleWords()` با
  `collapseWord()` شکل‌هایی را که پس از حذف نیم‌فاصله یکسان‌اند (مثل «مهارت ها» و
  «مهارتها») ادغام می‌کند و حداکثر سه کلمه نشان می‌دهد؛ `QUIET_WORD_FORMS`
  شکل‌های زائد (مثل «شناسنامه‌ی‌من») را پنهان می‌کند. فهرست‌ها هرگز از جدول
  دستورها جدا نمی‌افتند.
- **دو فهرست جدا بر پایهٔ سیاست محیط:** `commands` (همهٔ بخش‌های چت خصوصی و
  مشترک) و `commands_group` (فقط بخش‌های گروه‌محور) — بازیکن تازه‌وارد دقیقاً
  می‌فهمد کدام کلمه در کدام چت کار می‌کند.
- **۵ موضوع تازه:** `where` (تقسیم گروه/خصوصی و چرا)، `commands_group`,
  `panels` (ناوبری، صفحه‌بندی، عدد فرستادن، «پاسخ» دادن به پیام، انقضای ۱۵
  دقیقه‌ای حالت ورودی)، `errors` (۱۳ پیام خطای واقعیِ خودِ بازی + راه‌حل هرکدام)،
  `rules` (سقف‌های واقعی: ۱ حیوان خانگی، ۱ وام فعال، ۱ قرارداد اجاره، سپرده از
  ۵۰۰٬۰۰۰ تا ۵۰٬۰۰۰٬۰۰۰، قرعه ۲۰۰٬۰۰۰ در هفته، طلاق ۵٬۰۰۰٬۰۰۰، …).
- **فصل تازهٔ `guide`** (where, commands, commands_group, panels, errors, rules) و
  متن ورودی راهنما با «هشت فصل».
- **عدد واقعی به‌جای تقریب:** موضوع `group` از `environmentThresholdsText()` ساخته
  می‌شود (روستا ≤۱۴، شهر ۱۵–۳۰، استان ۳۱–۵۰، کشور ≥۵۱ + مالیات محلی ۲٪) و عنوان
  محلی هر سطح را هم توضیح می‌دهد.
- **تفکیک برچسب‌های تکراری:** `leaderboard` → «🏆 لیدربورد» و `rank` →
  «🥇 رتبه و جدول‌ها» (پیش‌تر هر دو یک برچسب داشتند و در یک پنل دو دکمهٔ هم‌نام
  می‌ساختند).
- **راستی‌آزمایی اندازه:** طولانی‌ترین متن راهنما ۱۷۷۰ نویسه (سقف تلگرام ۴۰۹۶)،
  هر پنل فصل ≤۱۵ سطر، و صفر واژهٔ ممنوعه (`undefined`, `Error`, `callback_data`,
  `PrismaClient`, …).

## ۵) صداقت پنل خانواده: «پاداش آماده است» ولی دکمه خطا می‌داد

**ریشه:** `MarriageService.getView` برای `bonusReady` فقط «امروز گرفته نشده» را
می‌سنجید، درحالی‌که `claimCoupleBonus` فعالیت ۴۸ ساعتهٔ همسر را هم شرط می‌کند.
نتیجه: پنل وعدهٔ پاداش می‌داد، دکمه ساخته می‌شد و کلیک با خطای «همسرت فعال نبوده»
رد می‌شد — تجربهٔ «ربات دروغ می‌گوید».

**رفع:** `getView` حالا هر دو شرط را می‌سنجد و `bonusWaitingForSpouse` را
برمی‌گرداند؛ `renderFamilyPanel` در این حالت دلیل و عدد واقعی
(`MARRIAGE_INFO.spouseActiveHours`) را می‌گوید و `buildFamilyKeyboard` فقط وقتی
دکمهٔ پاداش می‌سازد که واقعاً قابل دریافت باشد.

## ۶) عنوان گروه بدون پاک‌سازی در مرز ورود نوشته می‌شد

عنوان گروه کاملاً در اختیار کاربر است و در پنل شناسنامه، وضعیت منطقه، خبر،
رتبه‌بندی مناطق، مهاجرت و پنل ادمین با `parse_mode` مارک‌داون رندر می‌شود. عنوانی
مثل `*[x](t.me/…)*` هم کل پنل را ارسال‌نشدنی می‌کرد و هم لینک دلخواه در پیام‌های
بازی جا می‌گذاشت. رفع: `sanitizeGroupTitle()` با همان `plainInput()` مسیر ثبت‌نام،
و استفاده از عنوان پاک‌شده **هم در مقایسهٔ «تغییر کرده؟» هم در نوشتن** — وگرنه هر
همگام‌سازی یک نوشتار بیهوده و یک «تغییر کرد» دروغین می‌ساخت.

## ۷) تنظیم موجودی توسط ادمین با نوشتار شرطی (الگوی مالی پروژه)

`AdminService.setField` برای `balance` از `player.update` کورکورانه استفاده می‌کرد:
اگر بین خواندن و نوشتن، موجودی عوض شده بود (پاداش کار، معامله یا ادمین دیگر)
تغییر همزمان **بی‌صدا پاک** می‌شد. رفع: نوشتار شرطی
`updateMany({ where: { id, balance } })` + بررسی `count === 1` + `ConflictError`
داخل همان تراکنش — همان الگویی که بقیهٔ جابه‌جایی‌های پول رعایت می‌کنند.

## ۸) پیام یکدست «این بخش در گروه انجام می‌شود»

مزایده، چالش و قوانین منطقه در `features.handler.ts` هرکدام پنل دست‌ساز و متفاوتی
برای چت خصوصی داشتند (لحن و کلمهٔ پیشنهادی ناهمگون با بقیهٔ بازی). همه از
`groupOnlyNotice(section)` مرکزی خوانده می‌شوند و سوئیت تازهٔ
`tests/group-only-parity.test.ts` هم یکدستی متن/کلمهٔ پیشنهادی برای **همهٔ** بخش‌های
`GROUP_ONLY` را قفل می‌کند و هم نبودِ مسیر دست‌ساز را (تطبیق اسکلت متن + شمارش
فراخوانی).

## ۹) «زندگی من» از پاراگراف خام به پنل واقعی

پیش‌تر این بخش یک پاراگراف متنی بود که بازیکن را به «شغل‌ها» و «آگهی‌ها»
ارجاع می‌داد — کلمه‌هایی که در جدول کلیدواژه‌ها وجود ندارند و هیچ‌جا را باز
نمی‌کردند. حالا `panel()` با ردیف‌های کار (از `resolveJob`، هرگز `occupationId`)،
تحصیلات، سلامت، خستگی، تجربه، تأهل، محل زندگی و سن + بخش زندگی‌نامه، و پانویسی که
فقط کلمه‌های **واقعاً** روت‌شدنی را پیشنهاد می‌دهد («کار»، «بانک»، «خانه»،
«محل زندگی»، «خانواده»، «آمار»).

## ۱۰) اصلاح‌های کوچک اما ریشه‌ای

- `rewards.service.ts`: کامنت فرمول قدیمی (`۵۰٬۰۰۰ × کمینهٔ(زنجیره، ۷)`) با فرمول
  واقعی جایگزین شد: `۵۰٬۰۰۰ + ۲۵٬۰۰۰ × min(streak, 7)` → روز اول ۷۵٬۰۰۰ و سقف
  ۲۲۵٬۰۰۰. (کامنت اشتباه، منبع بعدیِ عدد اشتباه در راهنماست.)
- `utils/classes/texts.ts`: پانویس منوی اصلی ادعا می‌کرد «همین کلمات در گروه هم کار
  می‌کنند»؛ حالا تفکیک واقعی را می‌گوید (کار/خانه/فروشگاه/منطقه فقط در گروه،
  دعوت دوستان فقط در چت خصوصی).
- `text.handler.ts` مسیر «پاسخ به پیام» برای شناسنامه: اگر خواندن پروفایل ناموفق
  باشد بازیکن بی‌پاسخ نمی‌ماند و پیام انسانی می‌گیرد.
- **ادعاهای راهنما راستی‌آزمایی شدند** (نه فرض): «امار» و «آمار» → `stats`،
  «نشان» و «نشان‌ها» → `achievements`، «کارت» = «کارت روزانه» → `quests`،
  نیم‌فاصله/فاصله بی‌فرق، «حراجی» و «مزایده» → `auction`.

## ۱۱) باقی‌ماندهٔ آگاهانه (بدون رگرسیون)

- `claimCoupleBonus` هنوز رخداد `MARRIAGE_REGISTERED` ثبت می‌کند؛ نوع دقیق‌تر
  نیازمند افزودن مقدار به enum و **مهاجرت تازه** است و چون «قابلیت/تغییر اسکیما
  بدون تأیید کاربر» ممنوع است، دست‌نخورده ماند (رفتار مالی و idempotency درست است).
- پوشش کلیدواژه‌ها عمداً به شکل‌های طبیعی رایج محدود ماند؛ افزودن هر کلمهٔ تازه
  باید با تست برخورد (`EXACT_SECTIONS` یکتاست) انجام شود، نه با حدس.

## ۱۲) آزمون و اعتبارسنجی نهایی

- `npx tsc --noEmit` → **پاک**
- `npx eslint .` → **پاک** (بدون هیچ warning)
- `npx jest` → **۶۱ سوئیت / ۷۲۰ آزمون سبز** (+۴ آزمون تازه در این دور)
- `npm run build` (`prisma generate && tsc`) → **موفق**؛ `prisma/seed.ts` هم به
  `dist/seed.js` کامپایل و `offline-deps/dist.tar.gz` بازسازی شد (۴۱۰ ورودی،
  شامل `dist/app.js` و `dist/seed.js` در ریشهٔ tar همان‌طور که `install.sh` انتظار
  دارد). مسیر آفلاین دست‌نخورده باقی ماند.

**فایل‌های تغییریافتهٔ دور هفتم:**
`src/bot/command-catalog.ts` (تازه) · `src/bot/chat-policy.ts` ·
`src/bot/help-content.ts` · `src/bot/renders.ts` ·
`src/bot/handlers/text.handler.ts` · `src/bot/handlers/features.handler.ts` ·
`src/modules/family/marriage.service.ts` · `src/modules/groups/group.service.ts` ·
`src/modules/admin/admin.service.ts` · `src/modules/rewards/rewards.service.ts` ·
`src/utils/classes/texts.ts` · `tests/command-catalog.test.ts` (تازه) ·
`tests/group-only-parity.test.ts` (تازه) · `tests/brand-navigation.test.ts` ·
`tests/chat-policy.test.ts` · `tests/daily-loop-routing.test.ts` ·
`tests/status-panel-routing.test.ts` · `offline-deps/dist.tar.gz`

— پایان گزارش دور هفتم —

---

# 🟢 دور هشتم — ممیزی نهایی یکپارچگی: مدیریت، اخطار/مسدودسازی، حذف حساب و بستهٔ آفلاین (2026-09-13)

منبع حقیقت این دور: خودِ مخزن، اسکیما/مهاجرت‌ها، آزمون‌ها و یک PostgreSQL زنده (نه گزارش‌های قبلی).
هر ادعای دورهای پیش با کد بازسنجی شد؛ آنچه درست بود دست نخورد.

## ۱) خلاصهٔ کامل ممیزی (Full Audit Summary)

مسیر کامل هر قابلیت ردیابی شد: کلیدواژه → هندلر → گارد دسترسی → ماشین وضعیت → سرویس → ریپازیتوری → دیتابیس → رندر → پاسخ تلگرام.
دامنهٔ بررسی: `src/bot` (هندلرها، کیبوردها، `command-catalog.ts`، `chat-policy.ts`، `help-content.ts`، `renders.ts`، `ui-kit.ts`، `panel.ts`، میان‌افزارها)، `src/modules` (family، admin، groups، economy، occupation، city، market، pets، rewards، lifecycle)، `src/services` و ریپازیتوری‌ها، `prisma/schema.prisma` و هر ۲۲ مهاجرت، همهٔ آزمون‌ها، و `offline-deps`.

یافتهٔ اصلی این دور: **زیرسیستم «مدیریت روی بازیکن» فقط پوسته داشت.** هیچ مدل اخطاری در اسکیما نبود، هیچ متدی برای اخطار/لغو/پیام/حذف حساب در `AdminService` وجود نداشت و هیچ مسیر callbackی برای آن‌ها ثبت نشده بود (با `git show` روی کامیت پایه راستی‌آزمایی شد). یعنی خواسته‌های «اخطار + مسدودسازی خودکار»، «پیام به بازیکن» و «حذف کامل کاربر و داده‌های وابسته» در عمل وجود نداشتند.

دو باگ واقعیِ یکپارچگی هم در سیستم‌های موجود پیدا و رفع شد (رقابتِ لغوِ پیشنهاد ازدواج، و قفل‌شدن cooldown پس از شکستِ همگام‌سازی گروه). بقیهٔ هسته‌های زندگی/اقتصاد/مهریه که در دورهای قبل اصلاح شده بودند، بازسنجی و **بدون تغییر** تأیید شدند.

## ۲) باگ‌های پیداشده (Bugs Found)

| # | باگ | شدت | وضعیت |
| :- | :-- | :-- | :- |
| ۱ | `withdrawProposal` با نوشتارِ بی‌قید، پاسخِ پذیرفته‌شده را بازنویسی می‌کرد → ازدواجِ ثبت‌شده با پیشنهادِ «لغوشده» (وضعیت ناممکن) | بالا | رفع شد |
| ۲ | شکستِ همگام‌سازی/ثبت گروه همان cooldown شصت‌ثانیه‌ایِ موفقیت را می‌گرفت → راهنمای «یک بار دیگر /start بفرست» عملاً کار نمی‌کرد | متوسط | رفع شد |
| ۳ | نبودِ کاملِ سیستم اخطار (مدل، سرویس، مسیر، پنل) → «۳ اخطار = مسدودسازی خودکار» هیچ پشتوانه‌ای نداشت | بحرانی | پیاده‌سازی شد |
| ۴ | نبودِ «پیام مدیریت به بازیکن» (نه در سرویس، نه در پنل) | بحرانی | پیاده‌سازی شد |
| ۵ | نبودِ «حذف حساب و داده‌های وابسته»؛ در حالی که ۳۲ کلید خارجیِ RESTRICT به `players` یعنی حذفِ ساده با خطای P2003 می‌شکند یا دادهٔ یتیم می‌سازد | بحرانی | پیاده‌سازی شد |
| ۶ | `callback_data` دکمهٔ «لغو اخطار» در شکلِ بلند ۷۴ بایت می‌شد (سقف تلگرام ۶۴) → دکمه هرگز ارسال نمی‌شد | بالا | رفع شد (`adm:wrev`) |
| ۷ | تجزیهٔ اشتباهِ بخش‌های همان callback (اندیسِ اشتباه) → `BigInt(uuid)` با SyntaxError می‌ترکید | بالا | رفع شد؛ با آزمون یکپارچه پوشش گرفت |
| ۸ | `prisma-engines/schema-engine.gz` در git بیتِ اجرا نداشت (100644) و Prisma آن را spawn می‌کند → `packaging.sh` روی clone تازه با EACCES می‌شکست | بالا | رفع شد (100755 + کپی اجرایی در packaging) |
| ۹ | `install.sh` هرگز `prisma generate` نمی‌زند، پس با تغییر اسکیما بدون بازسازیِ `node_modules-linux.tar.gz`، سرورِ آفلاین با «مدل ناشناخته» بالا می‌آمد | بالا | بسته بازسازی + مستند + راستی‌آزمایی شد |
| ۱۰ | فایل تکراری و ارجاع‌نشدهٔ `/schema-engine.gz` (۱۹٫۹ مگابایت) در ریشهٔ مخزن | پایین | حذف شد |
| ۱۱ | نوشتارِ مردهٔ `marriage.updateMany({isActive:false})` پیش از حذفِ قطعیِ همان ردیف | پایین | با تسویهٔ واقعیِ همسر جایگزین شد |
| ۱۲ | موجودی‌های تولیدشدهٔ `docs` (مسیرها/متن‌ها) و شمارهای گزارش UX کهنه بودند | پایین | بازتولید و به‌روز شد |
| ۱۳ | دلیلِ اخطار هرگز به بازیکن نمی‌رسید: اعلان فقط موقع مسدودسازی ساخته می‌شد، در حالی که متنِ پنل به ادمین می‌گفت «همان دلیل به بازیکن نشان داده می‌شود» | بالا | رفع شد (اعلان برای هر اخطار) |
| ۱۴ | بازیکنِ مسدود حتی نمی‌توانست دلیلِ مسدودسازی را بخواند: `BAN_EXEMPT_SECTIONS` فقط `help` و `group_info` بود و بخش «اعلان» بسته می‌ماند | بالا | رفع شد (دسترسی خواندنی، فقط در چت خصوصی) |
| ۱۵ | پنل moderation برای حسابِ مسدود هم پانویسِ «با ۳ اخطار فعال مسدود می‌شود» را نشان می‌داد (گمراه‌کننده) + دو خط خالیِ اضافه پس از فهرست اخطارها | پایین | رفع شد |

## ۳) ریشهٔ علل (Root Cause)

- **نوشتارِ بی‌قید به‌جای نوشتارِ شرطی:** قاعدهٔ طلایی مخزن (`updateMany` با شرطِ وضعیت داخل `$transaction`) در `withdrawProposal` رعایت نشده بود؛ خواندن و نوشتن، دو لحظهٔ جدا بودند.
- **کشِ cooldown فقط برای مسیر موفق طراحی شده بود:** در `catch` هیچ بازنشانی/تنزیلی وجود نداشت، پس شکست، گران‌ترین جریمه را داشت.
- **پوسته پیش از مغز:** پنل و برچسب‌های «اخطار/حذف» در UI و متن‌ها وجود داشت ولی لایهٔ داده و سرویس هرگز ساخته نشده بود؛ هیچ آزمونِ یکپارچه‌ای هم نبود که «دکمه → سرویس» را واقعاً اجرا کند (آزمون‌های موجود ایستا/منبع‌محور بودند).
- **محدودیت‌های پروتکل تلگرام در تست پوشش نداشت:** تستِ سقف ۶۴ بایت فقط کیبوردهای `main.keyboard.ts` و نمونه‌های کوتاه را می‌دید، نه کیبوردهای داخلِ هندلر ادمین با uuid واقعی.
- **متنِ پنل جلوتر از رفتارِ واقعی نوشته شده بود:** جملهٔ «دلیل به بازیکن نشان داده می‌شود» هیچ مسیر داده‌ای پشتش نبود؛ از طرفی دسترسیِ خواندنیِ بازیکنِ مسدود به اعلان‌های خودش هم در سیاستِ بخش‌ها دیده نشده بود.
- **معماری آفلاین دو نکتهٔ پنهان داشت:** git بیتِ اجرا را روی برخی checkoutها نگه نمی‌دارد، و کلاینتِ تولیدشدهٔ Prisma عملاً یک «مصنوعِ ساخت» است که داخل بستهٔ `node_modules` منتشر می‌شود.

## ۴) اصلاحات (Fixes)

- `src/modules/family/marriage.service.ts`: لغوِ پیشنهاد با `updateMany` شرطی (`id + proposerId + status + createdAt`) و بررسی `count !== 1` → `ConflictError('Already answered')`.
- `src/bot/handlers/group.handler.ts`: ثابتِ `GROUP_SYNC_FAILURE_RETRY_MS = 10s`؛ در `catch`، cooldown به `now - (60s - 10s)` تنزل می‌یابد + `pruneSyncCache()`؛ یعنی تلاشِ دوباره ممکن است ولی توفانِ درخواست نه.
- `src/modules/admin/admin.service.ts` (+۸۶۸ خط): `getModerationView`، `issueWarning`، `revokeWarning`، `sendPlayerMessage`، `getDeletionPreview`، `deletePlayer` به‌همراه `buildDeletionPreview`، `closeSharedStates`، `erasePlayerData`، `assertNotBotAdmin`، `lockPlayerRow` و `PLAYER_DATA_PLAN`؛ اعتبارسنج‌های مشترک `validateWarningReason`/`validateAdminMessage`/`textLimitText` و ثابت‌های `WARNING_BAN_THRESHOLD`/`WARNING_REASON_LIMITS`/`ADMIN_MESSAGE_LIMITS`.
- `src/bot/handlers/admin.handler.ts` (+۵۰۴ خط): زیرپنلِ سلسله‌مراتبی moderation، چهار مسیر callback تازه، `stagePlayerDeletion`، شاخه‌های `apply` برای warning/warning_revoke/message/player_delete، جریان‌های ورودیِ دلیلِ اخطار و متنِ پیام، `postChangeKeyboard`، برچسب اقدام‌ها و جزئیاتِ خوانای گزارش مدیریت.
- `prisma/schema.prisma` + مهاجرت `20260913030000_player_warnings`.
- `offline-deps/packaging.sh` (کپی اجراییِ موتور)، بیتِ اجرای `prisma-engines/schema-engine.gz`، حذف `/schema-engine.gz`، بازسازی دو بستهٔ آفلاین، و `scripts/offline-bundle-verify.cjs`.
- `src/modules/admin/admin.service.ts`: هر اخطار (نه فقط اخطارِ منجر به بن) یک اعلانِ `WARNING` برای خودِ بازیکن می‌سازد با دلیل و شمارِ اخطارهای باقی‌مانده (`dedupeKey = warning:<id>`)؛ برای حسابِ ازقبل مسدود، متنِ صادقانهٔ «حساب مسدود است؛ این اخطار هم ثبت شد» جای شمارِ گمراه‌کننده را می‌گیرد.
- `src/bot/chat-policy.ts`: مجموعهٔ تازهٔ `PRIVATE_READ_ONLY_SECTIONS = {'notifications'}` — بازیکنِ مسدود یا پایان‌یافته می‌تواند اعلان‌های خودش را **فقط در چت خصوصی** بخواند (این بخش هیچ نوشتاری ندارد)؛ در گروه بسته می‌ماند تا دلیلِ اخطارِ یک بازیکن عمومی نشود.
- `src/bot/handlers/admin.handler.ts`: پانویسِ پنل moderation برای حسابِ مسدود عوض شد، فاصلهٔ اضافیِ فهرست اخطارها حذف شد و نتیجهٔ ثبتِ اخطار صریح می‌گوید دلیل به بازیکن نشان داده شد.
- `scripts/admin-moderation-smoke.cjs` (راستی‌آزمایی روی PostgreSQL زنده) و به‌روزرسانی `offline-deps/README.md` (چه بسته‌ای را کِی بازسازی کنیم + چگونه راستی‌آزمایی کنیم).

## ۵) ازدواج و مهریه (Marriage / Mahr Changes)

- **قانون مهریه تغییر نکرد** (نیاز هم نداشت): عروس مهریه را تعیین می‌کند، `MAHR_RULES = { min: 1, max: 1_000_000_000_000, unset: 0 }`؛ سقفِ ۵۰۰٬۰۰۰ یا هر سقفِ مصنوعیِ دیگری وجود ندارد و تنها کران، کرانِ فنیِ سرریز متناسب با ستون `DECIMAL(65,30)` است. داماد باید صریحاً مهریه را بپذیرد تا عقد بسته شود. یکسانیِ قانون در UI → اعتبارسنجی ورودی → وضعیت → سرویس → دیتابیس → callback → تأیید بازسنجی شد و سالم بود.
- تنها تغییر: رفعِ رقابتِ لغو (بند ۲/۱) + دو آزمون (به‌روزرسانیِ آزمونِ لغو به نوشتارِ شرطی، و آزمونِ «لغو در رقابت با پذیرش می‌بازد و نمی‌تواند پاسخ را بازنویسی کند»).
- انقضای ۷۲ ساعتهٔ پیشنهاد، تأییدِ دومرحله‌ایِ عقد، مشروط بودنِ پرداخت مهریه به موجودی، طلاق با قفلِ شرطی و پاداشِ زوج — همه بازسنجی و بدون تغییر تأیید شدند. ثبتِ دوبارهٔ ازدواج یا پرداختِ دوبارهٔ پاداش ممکن نیست (نوشتارهای شرطی + `dedupeKey`).

## ۶) ثبت گروه و سطح محیط (Group Registration Changes)

- ثبتِ تکراری با `/start`های بعدی رخ نمی‌دهد (بازسنجی شد: مسیر `ensureGroupUpdated` و کشِ همگام‌سازی)؛ شروعِ دوباره اطلاعاتِ گروهِ جاری را نشان می‌دهد.
- تغییر این دور فقط **رفتار پس از شکست** است: تنزل cooldown به ۱۰ ثانیه تا «دوباره /start بفرست» واقعاً ثبت را بیازماید.
- «سطح محیط» از دادهٔ واقعیِ اعضا محاسبه/خوانده می‌شود (`member-info.provider` + `group.service`); هیچ مقدارِ جعلی یا «نامشخص»ِ بی‌دلیل در UI باقی نمانده است (بازسنجی شد؛ تغییر لازم نبود).

## ۷) پنل ادمین (Admin Panel Changes)

- ناوبری سلسله‌مراتبی حفظ و تکمیل شد: پنل → بازیکنان/گروه‌ها/گزارش‌ها → جزئیات بازیکن → (اقتصاد · زندگی · وضعیت و محدودیت‌ها · **اخطار و پیام** · مهارت‌ها · اقدام‌ها).
- زیرپنل moderation: نمای اخطارها با دلیل، ادمینِ ثبت‌کننده و تاریخ؛ «فاصله تا مسدودسازی» از آستانهٔ واقعی؛ برای حسابِ مسدود، پیامِ صریحِ «مسدود است» به‌جای شمارشِ گمراه‌کننده.
- هر نوشتار، پیش‌نمایش + تأییدِ یک‌بارمصرف (توکن ۵ دقیقه‌ای، CAS، محدود به همان ادمین و همان چت) دارد؛ پنل با Edit روی همان پیام جلو می‌رود و چت را پر نمی‌کند.
- «پیام به بازیکن»: متن در اعلان‌های بازی ثبت می‌شود و پیامِ خصوصی هم تلاش می‌شود؛ اگر نرسد، پنل **دقیقاً** می‌گوید نرسیده (نه موفقِ دروغین).
- «حذف حساب»: پیش‌نمایشِ واقعی شامل مانع‌ها، فهرستِ پاک‌شدنی‌ها با شمار، اثرهای تسویه و مبلغِ خارج‌شده از گردش؛ اگر مانعی باشد، حذف انجام نمی‌شود و دلیلِ عملیاتی‌اش گفته می‌شود.
- ادمین اصلی `6910416744` (OWNER) مدیریتِ ادمین‌ها را هم دارد؛ ادمین دوم `8369939024` همهٔ عملیات عادی را دارد ولی افزودن/حذف ادمین نه — در بک‌اند (`assertOwner` + `ownerOnly` در `authorizedTransaction`) و نه فقط در UI. حذفِ ادمین فقط دسترسی را برمی‌دارد.
- هدفِ هر اقدامِ مدیریتی نمی‌تواند یک ادمینِ فعالِ ربات باشد (جلوگیری از وضعیتِ متناقضِ «بازیکنِ مسدود/حذف‌شده با پنل مدیریت»).

## ۸) اصلاحات امنیتی (Security Fixes)

- **مسدودسازی اتمیک:** ردیفِ بازیکن با `SELECT … FOR UPDATE` قفل می‌شود، سپس بن با نوشتارِ شرطی (`status != BANNED`)؛ بازندهٔ رقابت نه بنِ دوباره می‌زند، نه اعلان/گزارش تکراری. آزمونِ همزمانی و smoke روی دیتابیس زنده این را می‌سنجند.
- **ضدِ IDOR:** لغوِ اخطار با `{ id, playerId, isActive }` مقید به همان بازیکن است؛ شناسهٔ اخطارِ بازیکن دیگر هرگز روی پنلِ این بازیکن اثر نمی‌کند. دسترسیِ ادمین همیشه از دیتابیس خوانده می‌شود، نه از payload.
- **غیرادمین:** عبارت ورودیِ «پنل ادمین» برای غیرادمین هیچ پاسخی تولید نمی‌کند؛ callbackهای ادمین هم برای غیرادمین بی‌صدا/با هشدارِ کوتاه رد می‌شوند و هرگز محتوا یا سرویس را لمس نمی‌کنند (با آزمون پوشش داده شد).
- **ضدِ replay:** توکنِ تأیید یک‌بارمصرف و کلیدشده به ادمین+چت است؛ انقضا/لغو/مصرف‌شده با پیامِ روشن رد می‌شود و هیچ سرویسی صدا زده نمی‌شود.
- **ورودیِ میان‌دستی:** ورودیِ متنیِ مربوط به چتِ دیگر پذیرفته نمی‌شود؛ اگر دسترسی ادمین در میانهٔ جریان گرفته شود، state پاک می‌شود و ربات بی‌صدا می‌ایستد.
- **ورودی عددی/متنی:** دلیلِ اخطار و پیام، پیش از رسیدن به دیتابیس با همان تابعی پاک‌سازی و محدود می‌شوند که سرویس موقع نوشتن صدا می‌زند (یک قانون، دو لایه)؛ متنِ آزاد هرگز مارک‌داون پنل را نمی‌شکند.
- **حذف حساب بدون آبشارِ کور:** داده‌ای که حقِ دیگران است (کارمندِ فعال/حقوقِ معوق، وام یا سپردهٔ فعال، قرضِ باز، اجارهٔ فعال، برندهٔ موقتِ حراجیِ باز، نامزدیِ انتخاباتِ باز) مانع حذف است؛ دفتر کل و تاریخچه با `ON DELETE SET NULL` نگه داشته می‌شوند.

## ۹) تجربهٔ کاربری تلگرام (Telegram UX Changes)

- همهٔ متن‌های تازه فارسیِ طبیعی و کوتاه‌اند؛ بدون اصطلاحِ توسعه‌دهنده، بدون نامِ مدل/سرویس/جدول، بدون stack trace. خطاها با `feature/action` در logger ثبت می‌شوند و کاربر پیامِ کوتاهِ عملیاتی می‌گیرد.
- ایموجی و قالب‌بندی محدود و هم‌سبکِ بقیهٔ پنل‌ها (`panel`/`fa`/`money` از `ui-kit`).
- پیام‌های صادقانه: «مسدود است» به‌جای «۰ اخطار تا مسدودسازی»، «پیام خصوصی نرسید» به‌جای سکوت، «لغو اخطار مسدودسازی را برنمی‌گرداند» صریح در همان پیش‌نمایش.
- سمتِ بازیکن هم کامل شد: هر اخطار با دلیل و شمارِ اخطارهای باقی‌مانده در بخش «اعلان» می‌نشیند و بازیکنِ مسدود می‌تواند در چت خصوصی بخواندش؛ پس جملهٔ «حسابت مسدود است» دیگر بدون دلیل نیست.
- پرداختِ متنِ پنل moderation: پانویسِ «با ۳ اخطار فعال مسدود می‌شود» برای حسابِ مسدود حذف شد (جایش توضیحِ ماندگاریِ اخطارها آمد) و فاصلهٔ اضافیِ فهرست اخطارها از بین رفت.
- پنل همیشه روی همان پیام Edit می‌شود؛ پس از اقدام، کیبوردِ درست برگردانده می‌شود (حذف/اقدام مدیریتی → خانهٔ پنل؛ اخطار/لغو/پیام → همان پنل moderation با نمای تازه).

## ۱۰) کلیدواژه‌ها و callbackها (Keyword / Callback Changes)

- **هیچ کلیدواژهٔ بازیکنی اضافه، حذف یا تغییر نام داده نشد** (`docs/keyword-guide.json` بدون تغییر: ۱۵۶ شکل واقعی). ورودیِ مدیریت همان عبارتِ دقیقِ «پنل ادمین» در گروه و خصوصی است.
- چهار callback تازهٔ ادمین: `adm:mod:<id>`، `adm:warn:<id>`، `adm:msg:<id>`، `adm:wrev:<id>:<uuid>` + استفاده از `adm:confirm:player_delete:<id>` و `adm:apply:<token>` موجود.
- `docs/telegram-routes.json` بازتولید شد: ۲۰۲ → **۲۰۶** مسیر ثبت‌شده (دقیقاً چهار مسیر تازه).
- تست‌های موجود (`callback-routing.test.ts`) تأیید می‌کنند هر دکمهٔ تولیدشدهٔ ادمین به یک هندلرِ زنده می‌رسد و هیچ `^adm:` هندلری بدون دکمه نمانده؛ تستِ تازهٔ این دور سقف ۶۴ بایت را با **uuid واقعی** می‌سنجد.

## ۱۱) دیتابیس و مهاجرت (Database / Migration Changes)

- مدل تازهٔ `PlayerWarning` → جدول `player_warnings` با دو ایندکس (`player_id,is_active` و `issued_by`) و کلید خارجی `ON DELETE CASCADE`؛ مهاجرت `prisma/migrations/20260913030000_player_warnings/migration.sql`.
- اعمال روی PostgreSQL زنده با `offline-deps/scripts/migrate.js` → **۲۲ مهاجرت** موفق؛ `prisma migrate diff --from-url … --to-schema-datamodel …` → «empty migration» = **صفر انحراف**.
- موجودیِ قطعیِ کلیدهای خارجی به `players` از `pg_constraint` روی دیتابیس زنده: **۴۴ کلید خارجی** = ۸ `CASCADE` + ۴ `SET NULL` + ۳۲ `RESTRICT`؛ به‌علاوهٔ `user_states` که با شناسهٔ تلگرام کلید می‌خورد و کلید خارجی ندارد. در اسکیما **۴۶ فیلدِ رابطه‌ای با `Player`** هست (۴۴ کلید خارجی + `Group.residents` و `Group.visitors` که سمتِ دیگرِ `players.home_group_id` / `players.current_region_id` هستند) و همهٔ ۴۶ مورد در `PLAYER_DATA_PLAN` قاعدهٔ صریح دارند (`self/delete/block/settle/cascade/set_null/by_telegram_id`).
- ترتیبِ پاک‌کردن در `erasePlayerData` بر اساس همان کلیدهای خارجی است (زنجیرهٔ کسب‌وکار → زنجیرهٔ ملک → مالی شخصی → بازار/شهر → دارایی و پیشرفت → زندگی و روابط → نوبت کاری) و با پولی که از گردش خارج می‌شود یک سندِ `WITHDRAWAL` ثبت می‌شود.

## ۱۲) آزمون‌های اضافه/به‌روزشده (Tests Added / Updated)

- **`tests/admin-moderation.test.ts` (تازه، ۳۳ آزمون):** پوششِ ایستای اسکیما↔`PLAYER_DATA_PLAN` (هیچ رابطهٔ بی‌قاعده و هیچ قاعدهٔ کهنه)، مرزهای متن، آستانهٔ اخطار، بنِ اتمیک و آزادسازی ظرفیت شغل، بازندهٔ رقابتِ همزمان، قفلِ `FOR UPDATE`، لغو (بدون unban خاموش)، پیام (ثبت ماندگار)، نمای moderation، مانع‌ها/پیش‌نمایش/ترتیبِ پاک‌کردن/سندِ تسویه، محافظِ ادمینِ هدف، و سقف ۶۴ بایت با uuid واقعی.
- **`tests/admin-callbacks.test.ts` (+۲۱ آزمون → ۴۳):** اجرای واقعیِ `registerAdminHandlers` روی Bot گرمی برای پنل moderation، لغو با uuid واقعی، پیش‌نمایش حذف و حالتِ مانع‌دار، یک‌بارمصرف بودن تأییدها، گزارشِ صادقانهٔ نرسیدنِ پیام خصوصی، سکوت/ردِ غیرادمین، و جریان‌های ورودیِ «دلیل اخطار» و «متن پیام» (معتبر/نامعتبر/انصراف/چتِ دیگر/ادمینِ عزل‌شده).
- **`tests/chat-policy.test.ts` (+۱):** بازیکنِ مسدود/پایان‌یافته بخش «اعلان» را در چت خصوصی باز می‌کند، در گروه نه، و بقیهٔ بخش‌ها برایش بسته می‌ماند.
- **`tests/admin-moderation.test.ts` (+۲):** رسیدنِ دلیلِ اخطار به اعلانِ خودِ بازیکن (با `dedupeKey` و شمارِ باقی‌مانده)، و متنِ صادقانه برای اخطارِ بازیکنِ ازقبل مسدود؛ به‌علاوهٔ «فقط یک اعلان» در مسیر بن و در مسیر بازندهٔ رقابت.
- **`tests/admin-callbacks.test.ts` (+۱):** پانویسِ درست برای حسابِ مسدود و شمارشِ واقعی تا مسدودسازی برای حسابِ فعال.
- **`tests/marriage.test.ts` (+۱ و به‌روزرسانی ۱ → ۳۲):** نوشتارِ شرطیِ لغو و آزمونِ رقابتِ «لغو در برابر پذیرش».
- اسکریپت‌های راستی‌آزمایی روی دیتابیس زنده: `scripts/admin-moderation-smoke.cjs` (تازه) و `scripts/offline-bundle-verify.cjs` (تازه)؛ `scripts/admin-ux-smoke.cjs` بدون تغییر بازاجرا شد.

## ۱۳) نتیجهٔ آزمون (Test Result)

- پایهٔ شروعِ این دور: **۶۶ سوئیت / ۹۰۷ آزمون** (با worktree روی کامیت پایه اندازه‌گیری شد).
- پایان: **۶۷ سوئیت / ۹۶۶ آزمون — همه سبز** (+۵۹ آزمون، +۱ سوئیت).
- راستی‌آزمایی روی PostgreSQL زنده (پورت 5433، دیتابیسِ `_ux_test`، هر بار از صفر migrate شده): هر سه اسکریپت `PASS` — آستانه و بنِ اتمیک، رقابتِ همزمان، لغو، محافظِ ادمین، پیامِ ماندگار، مانع‌های حذف، **یکپارچگی ارجاعی روی هر ۴۴ کلید خارجی (صفر ردیفِ یتیم)**، تسویهٔ همسر، ماندگاری دفتر کل و سرگذشت.

## ۱۴) نتیجهٔ ESLint (ESLint Result)

`npx eslint src tests scripts` → **صفر خطا، صفر هشدار** (شامل دو اسکریپت تازهٔ `.cjs`).

## ۱۵) نتیجهٔ Build (Build Result)

- `tsc --noEmit` → پاک.
- `tsc` → `dist/` (۴٫۰ مگابایت) + کامپایل جداگانهٔ `prisma/seed.ts` در `dist/` (مطابق `packaging.sh`).
- `prisma generate` به‌صورت آفلاین با موتورِ داخل مخزن (`PRISMA_SCHEMA_ENGINE_BINARY`) → موفق؛ کلاینتِ تولیدشده شامل `PlayerWarning`.

## ۱۶) مصنوعات آفلاین (Offline Artifacts)

- `offline-deps/dist.tar.gz` بازسازی شد: ۶۰۱٬۵۳۶ → **۶۲۴٬۰۳۰ بایت**.
- `offline-deps/node_modules-linux.tar.gz` بازسازی شد: ۱۸٬۸۳۳٬۷۵۰ → **۱۸٬۸۵۲٬۰۵۵ بایت**. دلیلِ بازسازی: وابستگی‌ها عوض نشده‌اند (فهرستِ بسته‌ها با diff دقیقاً یکسان است)، ولی `install.sh` هرگز `prisma generate` نمی‌زند و کلاینتِ Prisma داخل همین بسته منتشر می‌شود؛ بدون بازسازی، سرور آفلاین مدل `PlayerWarning` را نمی‌شناخت. کلاینتِ داخل بسته با کلاینتِ تولیدشدهٔ تازه **بایت‌به‌بایت یکسان** است (sha256 مقایسه شد).
- `postgres-linux-x64/arm64.tar.gz` و `node-v22.22.3-linux-x64.tar.xz` بدون تغییر (وابستگی عوض نشد).
- `offline-deps/packaging.sh`: موتورِ اسکیما به مسیر موقت کپی و اجرایی می‌شود؛ بیتِ اجرای `prisma-engines/schema-engine.gz` در git اصلاح شد؛ فایل تکراری `/schema-engine.gz` (۱۹٬۹۳۰٬۸۷۲ بایت) حذف شد.
- `scripts/offline-bundle-verify.cjs`: خودِ بستهٔ منتشرشده را در پوشهٔ موقت باز می‌کند و با کلاینت و `dist`ِ همان بسته، جریان واقعی اخطار → بن → پیش‌نمایش → حذف را روی PostgreSQL زنده اجرا می‌کند (صفر موتور بومی، سقف ۶۴ بایت، صفر یتیم، ماندگاری سندِ تسویه). نتیجه: `PASS`.
- `offline-deps/README.md`: جدولِ «چه بسته‌ای را کِی بازسازی کنیم» + دستورهای راستی‌آزمایی.
- `docs/telegram-routes.json` و `docs/ui-text-inventory.jsonl` بازتولید شدند (۲۰۶ مسیر / ۵۱۷۳ متن) و شمارهای `docs/UX-UI-REPORT.fa.md` به‌روز شد.

## ۱۷) هش کامیت (Commit Hash)

`fedf27a` (کد + تست + بسته‌ها) و `dbc6aa6` (گزارش دور هشتم) و یک کامیتِ پیگیری برای رفعِ بند ۲/۱۳–۱۵ — همه روی شاخهٔ `arena/01a099ed-virtuallife` (والد: `9359198`).

## ۱۸) نتیجهٔ Push (Push Result)

`git push origin arena/01a099ed-virtuallife` → موفق (`* [new branch]`). طبق خواسته: **بدون PR و بدون merge**.

## ۱۹) مسائل باقی‌مانده (Remaining Issues)

- حذفِ حساب، ردیفِ `marriages` را هم پاک می‌کند (کلید خارجیِ لازم و RESTRICT)؛ همسر «جداشده» + اعلان + ردیفِ سرگذشت می‌گیرد، ولی رکوردِ مشترکِ ازدواج باقی نمی‌ماند. برای نگهداریِ آن به مدلِ soft-delete/آرشیو نیاز است (تغییرِ معماری، بیرون از این دور).
- حذفِ مشارکت‌کنندهٔ چالش منطقه‌ای، امتیازش را از پیشرفتِ منطقه کم می‌کند (در پیش‌نمایش صریح گفته می‌شود؛ بازتخصیص امتیاز انجام نشد).
- «لغو اخطار» عمداً مسدودسازی را برنمی‌گرداند؛ بازگرداندن دسترسی یک اقدامِ جدا و صریح است.
- آستانهٔ اخطار (۳) و مرزهای متن، ثابتِ زمانِ کامپایل‌اند نه تنظیماتِ زمانِ اجرا؛ پنلِ «تنظیمات» همین را صادقانه می‌گوید.
- صفِ سراسریِ اخطارها (نمای همهٔ اخطارهای فعال بدون انتخابِ بازیکن) ساخته نشد؛ مسیر فعلی از صفحهٔ بازیکن است.
- در فهرستِ اخطارها و گزارش مدیریت، ادمینِ اقدام‌کننده با شناسهٔ عددیِ تلگرام نشان داده می‌شود (نه نام)؛ این دقیقاً همان سبکِ موجودِ `گزارش مدیریت` است و عمداً تغییر نکرد.
- اعلانِ اخطار فقط در بازی ثبت می‌شود و پیامِ خصوصیِ خودکار نمی‌فرستد؛ ارسالِ خصوصیِ مستقیم همان اقدامِ جداگانهٔ «پیام به بازیکن» است.
- اسکریپت‌های smoke به PostgreSQL زنده نیاز دارند و عمداً بخشی از jest نیستند (CI بدون دیتابیس).
- `.npmrc` مخزن به `registry.npmmirror.com` اشاره می‌کند که از این محیط مرده است؛ `packaging.sh` صریحاً `--registry=https://registry.npmjs.org` را پاس می‌دهد (فایلِ کامیت‌شدهٔ `.npmrc` دست نخورد).

## ۲۰) موارد بیرون از دامنه (Out-of-Scope Items)

- بازطراحیِ هستهٔ زندگی/اقتصاد/مهریه (در دورهای قبل اصلاح شده و سالم تأیید شد).
- افزودنِ قابلیتِ تازهٔ گیم‌پلی یا کلیدواژهٔ بازیکن؛ تغییرِ نامِ کلیدواژه‌های موجود.
- تایمر/کرون/صفِ پس‌زمینه (معماری مخزن بر مبنای بررسیِ انقضا هنگام استفاده است).
- تغییرِ معماریِ حذف به soft-delete، چندزبانگی، یا بازنویسیِ پنل‌ها.
- ساختِ PR یا merge (طبق دستور).

— پایان گزارش دور هشتم —

---

# 🟢 دور نهم — راستی‌آزمایی زندهٔ شش سناریوی بازی و یکتایی حساب بانکی (2026-09-13)

هدفِ این دور، «بازی‌کردنِ واقعی» با سیستم بود: هر شش سناریوی ممیزی روی
PostgreSQL زنده و از مسیرِ کاملِ سرویس‌ها (container واقعی، نه mock) اجرا شد تا
آنچه فقط در سطح کد خوانده شده بود، با رفتارِ واقعیِ دیتابیس سنجیده شود.
خروجی، یک باگِ واقعیِ اقتصادی بود (حساب بانکی تکراری) که ریشه‌ای اصلاح شد.

## ۱) خلاصهٔ کامل ممیزی (Full Audit Summary)

دو اسکریپتِ راستی‌آزمایی زنده اضافه شد و همراه سه اسکریپتِ قبلی، هر شش سناریو
پوشش داده شد:

| سناریو | اسکریپت | نتیجه |
|---|---|---|
| شناسنامه / تحصیل / مهارت / شغل / درآمد | `scripts/gameplay-live-smoke.cjs` | ✅ ثبت‌نام دانشگاه، شهریه، فارغ‌التحصیلی، مدرک روی شناسنامه، مهارتِ رشته، اثرِ مدرک بر درآمد |
| شغل / خستگی / سلامت / بهره‌وری | `scripts/gameplay-live-smoke.cjs` | ✅ شیفت، پرداختِ خالص پس از مالیات، خستگی/سلامتِ واقعی، بهره‌وریِ بدنی در دستمزد، توقف خودکار در خستگی بحرانی |
| درآمد / بانک / خرج / کسب‌وکار | `scripts/gameplay-live-smoke.cjs` | ✅ حساب یکتا، واریز/برداشت بدون خلق پول، تأسیس و سرمایه، آگهی/استخدام، تسویهٔ حقوق، برداشت سود |
| خواستگاری / مهریه / پذیرش | `scripts/marriage-live-smoke.cjs` | ✅ قانون مهریه روی `numeric(65,30)`، پذیرش صریح، ثبت یک‌باره، طلاقِ تک‌تسویه |
| شروع گروه / ثبت / سطح محیط | `scripts/gameplay-live-smoke.cjs` | ✅ ثبت یک‌باره، سطح محیط از اعضای واقعی، عددِ نامشخص هرگز بازنویسی نمی‌کند |
| پنل ادمین / جست‌وجو / مشاهده / ویرایش / اخطار | `scripts/admin-ux-smoke.cjs` + `scripts/admin-moderation-smoke.cjs` | ✅ مجوزها، اخطار/مسدودسازی اتمیک، پیام و حذف حساب، سلامت ارجاع‌ها |

## ۲) باگ‌های پیداشده (Bugs Found)

1. **حساب بانکی تکراری (واقعی و اقتصادی).** یکتاییِ حساب فقط در کد بود
   («اول بخوان، اگر نبود بساز»). دو درخواست همزمان در اولین استفاده — کلیک
   دوبله روی «حساب بانکی» — دو حساب می‌ساختند. چون همهٔ خوانده‌ها
   `findFirst` بدون ترتیب بودند، واریز/برداشت/سود می‌توانستند روی حساب‌های
   متفاوت بنشینند: پس‌انداز بازیکن بی‌صدا دو تکه می‌شد و عددِ روی پنل لزوماً
   پولِ واقعی او نبود. در `bank_accounts` فقط ایندکسِ ساده بود، نه یکتایی.
2. **لاگِ گمراه‌کننده.** همان نقضِ یکتایی که کد *عمداً* اداره می‌کند (کلید
   `dedupeKey` اعلان/رخداد، حساب بانکی، وام فعال، ثبت گروه) با سطح `error`
   لاگ می‌شد؛ در محیطِ عملیاتی هم Prisma خطاها را خام به stdout می‌داد.
   نتیجه: هشدارِ بی‌مورد و پنهان‌شدنِ خطاهای واقعی.
3. **سکوتِ UI در برداشت سودِ محدودشده.** برداشتِ بیشتر از موجودیِ خزانه رد
   نمی‌شود بلکه به موجودیِ واقعی محدود می‌شود (رفتارِ درست و امن)، ولی پنل
   فقط عددِ کوچک‌تر را نشان می‌داد و بازیکن دلیلش را نمی‌دید.

## ۳) ریشهٔ علل (Root Cause)

- «یک حساب برای هر بازیکن» یک *قصدِ طراحی* بود که هیچ‌جا در دیتابیس ثبت نشده
  بود؛ رابطهٔ `Player.bankAccounts` هم به‌شکلِ آرایه تعریف شده بود، یعنی اسکیما
  عملاً چند حساب را مجاز می‌دانست. هر قاعده‌ای که فقط در کد باشد، با اولین
  اجرای همزمان می‌شکند.
- لاگرِ Prisma بین «رقابتِ مهارشده» و «خطای واقعی» تفاوتی قائل نبود.
- پنلِ برداشت سود، تفاوت «مبلغِ درخواستی» و «مبلغِ برداشت‌شده» را گزارش نمی‌کرد.

## ۴) اصلاحات (Fixes)

- `prisma/schema.prisma`: `bank_accounts.player_id` یکتا شد؛ ایندکسِ ساده حذف
  شد؛ رابطهٔ سمت بازیکن از آرایه به `bankAccount BankAccount?` تبدیل شد
  (یک‌به‌یکِ واقعی). هیچ `include`/`select`ی در کد از نامِ قبلی استفاده نمی‌کرد.
- مهاجرتِ `20260913050000_unique_bank_account_per_player`: اول رکوردهای
  تکراریِ موجود در قدیمی‌ترین حسابِ هر بازیکن جمع می‌شوند (جمعِ موجودی، بدون
  خلق یا حذف پول)، بعد ایندکسِ یکتا ساخته می‌شود. هیچ جدولی به `bank_accounts`
  کلید خارجی ندارد (وام‌ها به بازیکن وصل‌اند)، پس حذفِ تکراری‌ها دادهٔ وابسته
  را بی‌صاحب نمی‌کند.
- `src/database/repositories/banking.repository.ts`: `createAccount` در صورت
  `P2002` همان حسابِ ساخته‌شده را برمی‌گرداند (بازندهٔ رقابت حساب می‌گیرد، نه
  خطا)؛ همهٔ خوانده‌های حساب به `findUnique` روی کلید یکتا رفتند.
- `src/database/prisma-log.ts` (جدید) + `src/database/client.ts`: نقضِ یکتایی
  `warn` است و بقیهٔ خطاها `error`؛ رویدادهای خطای Prisma در همهٔ محیط‌ها از
  لاگرِ ساختاریافتهٔ برنامه رد می‌شوند.
- `src/bot/handlers/text.handler.ts`: پنلِ برداشت سود وقتی مبلغ محدود شده باشد
  صریح می‌گوید «خزانه کمتر از مبلغ درخواستی بود؛ همان … برداشت شد.»

## ۵) ازدواج و مهریه (Marriage / Mahr Changes)

تغییرِ کد لازم نشد؛ رفتارِ واقعی روی دیتابیس زنده اثبات شد:

- ستون `mahr` واقعاً `numeric(65,30)` است و مبلغِ سقف (`۱٬۰۰۰٬۰۰۰٬۰۰۰٬۰۰۰`)
  بدون هیچ گرد‌شدنی دقیقاً ذخیره شد؛ `۵۰۰٬۰۰۱` هم پذیرفته شد، یعنی **هیچ سقف
  ساختگیِ ۵۰۰٬۰۰۰‌تومانی وجود ندارد** — تنها مرز، محافظِ سرریزِ عدد است.
- یک قانون، دو ورودی: مهریهٔ زن و پیشنهادِ مرد هر دو از `validateMahrAmount`
  می‌گذرند و مقادیرِ نامعتبر (`منفی`، `۱٫۵`، `بیش از سقف`) در هر دو رد می‌شوند.
  تنها تفاوتِ مستند، `۰` در پیشنهادِ مرد است که یعنی «تعیین‌نشده».
- **پذیرشِ صریحِ مرد الزامی است**: زنِ تعیین‌کنندهٔ مهریه نمی‌تواند خودش عقد را
  نهایی کند (`Cannot accept`)؛ پس از `confirmProposal` مرد، ازدواج ثبت می‌شود.
- پول دقیقاً جابه‌جا شد: `−مهریه` از مرد، `+مهریه` به زن، یک ردیفِ دفتر کل،
  یک ردیفِ `marriages`، دو رخداد و دو اعلان. جمعِ موجودی‌ها پیش و پس از عقد
  برابر ماند (پول از هیچ ساخته نشد).
- تکرارِ callback پس از عقد چیزی را عوض نکرد؛ دو پذیرشِ همزمان فقط یک ازدواج
  ساخت؛ «پس‌گرفتنِ پیشنهاد» در برابرِ «پذیرش» یا می‌بازد یا می‌برد، هرگز هر دو
  (موجودی دست‌نخورده ماند).
- موجودیِ ناکافی مرد، کل تراکنش را برگرداند: پیشنهاد در همان `MAHR_SET` ماند
  (نیمه‌پذیرفته نشد) و پولی جابه‌جا نشد.
- پاداشِ روزانهٔ زوج زیر ۱۰ درخواست همزمان دقیقاً یک‌بار پرداخت شد و ادعای
  دوباره در همان روز رد شد؛ طلاق یک‌بار تسویه شد و هزینهٔ طلاق از گردشِ پول
  بیرون رفت (تامینِ پولِ منفی نشد).

## ۶) ثبت گروه و سطح محیط (Group Registration Changes)

تغییرِ کد لازم نشد؛ ریشهٔ «سطح محیط: نامشخص» روی دیتابیس زنده بسته تأیید شد:

- اولین فراخوانی گروه را ساخت (`created=true`) با ۲۰ عضوِ واقعی → `CITY`.
- `/start` دوم گروه را **دوباره ثبت نکرد** (`created=false`) و وقتی تلگرام عددی
  نداد (`null`)، عددِ ذخیره‌شده و سطح محیط دست‌نخورده ماند
  (`memberCountSynced=false`)؛ یعنی هیچ مقدارِ جعلی یا «نامشخص» نوشته نمی‌شود.
- رشدِ واقعیِ جمعیت (۶۰ عضو) سطح را به `COUNTRY` برد (`environmentChanged=true`).
- سه ثبتِ همزمانِ گروهِ تازه → دقیقاً یک ردیف.
- عضویتِ بازیکن دو بار صدا زده شد → یک ردیفِ عضویت.
- متنِ آستانه‌های راهنما (`environmentThresholdsText`) و طبقه‌بندِ واقعی هر دو از
  `REGION_CONFIG` می‌آیند: مرزهای `city` روی هر دو یکسان جواب داد و جمعیتِ منفی
  رد شد.

## ۷) پنل ادمین (Admin Panel Changes)

تغییری لازم نشد؛ دو اسکریپتِ ادمین دوباره اجرا و سبز شدند (مجوزهای اصلی/ثانوی،
اخطار و مسدودسازیِ اتمیک زیر رقابت، لغوِ اخطار، پیام به بازیکن، حذفِ کامل حساب
با سلامت ارجاع‌ها و تسویهٔ همسر).

## ۸) اصلاحات امنیتی (Security Fixes)

- **Double-spend در بانک**: حذفِ امکانِ دو حساب برای یک بازیکن در سطح دیتابیس
  (مهارِ رقابتِ اولین استفاده).
- **برداشتِ همزمانِ سود**: دو برداشتِ همزمان از یک خزانه، خزانه را دقیقاً یک‌بار
  خالی کرد؛ هرگز منفی نشد و کیف پول فقط به‌اندازهٔ خالصِ واقعی رشد کرد.
- **تأسیسِ همزمانِ کسب‌وکار**: دو درخواستِ موازی با سرمایهٔ دقیقاً یک هزینهٔ
  تأسیس → فقط یکی برنده شد و یک کسب‌وکار ساخته شد.
- **دسترسیِ بیگانه**: کارمند نمی‌تواند حقوقِ کسب‌وکارِ دیگری را تسویه کند و
  بازیکنِ غریب نمی‌تواند از خزانهٔ دیگری برداشت کند.
- **تسویهٔ دوبارهٔ حقوق**: تسویهٔ فوریِ دوم هیچ‌کس را پرداخت نکرد؛ حتی عقب
  بردنِ ساعتِ کسب‌وکار، کارمندی را که `paidUntilAt`‌اش «اکنون» است دوباره
  پرداخت نمی‌کند (لنگرِ هر کارمند جداست). دو تسویهٔ همزمان روی یک بازهٔ
  واقعاً پرداخت‌نشده → دقیقاً یکی پرداخت کرد.
- **پولِ پیش از استخدام**: بازهٔ قبل از لحظهٔ استخدام پرداخت نمی‌شود و ردیفِ
  حقوقی هم برایش ساخته نمی‌شود.

## ۹) تجربهٔ کاربری تلگرام (Telegram UX Changes)

- پنلِ برداشت سود، محدودشدنِ مبلغ به موجودیِ واقعیِ خزانه را صریح می‌گوید.
- لاگِ عملیاتی صادق‌تر شد: رقابتِ مهارشده `warn` است و خطای واقعی `error`
  (پیامِ فنی به بازیکن نمی‌رسد؛ فقط در لاگ می‌ماند).
- موجودیِ ایستای متن‌ها به‌روز شد: **۵۱۷۴** رشته/قالب فارسی، ۲۰۶ مسیر، ۱۵۶
  کلیدواژه (بدون کلیدواژه یا callback تازه).

## ۱۰) کلیدواژه‌ها و callbackها (Keyword / Callback Changes)

هیچ کلیدواژه یا callbackی اضافه، حذف یا تغییر نام داده نشد؛
`docs/telegram-routes.json` و `docs/keyword-guide.json` بازتولید و بدون تغییر
ماندند (۲۰۶ / ۱۵۶).

## ۱۱) دیتابیس و مهاجرت (Database / Migration Changes)

- مهاجرتِ تازه: `20260913050000_unique_bank_account_per_player`
  (جمع‌کردنِ تکراری‌ها + `DROP INDEX` ساده + `CREATE UNIQUE INDEX IF NOT EXISTS`).
- شمارِ مهاجرت‌های اعمال‌شده: **۲۳**، بدون drift —
  `prisma migrate diff --from-url <db> --to-schema-datamodel prisma/schema.prisma`
  خروجیِ «empty migration» داد.
- مسیرِ ترمیمِ داده جداگانه روی PostgreSQL زنده آزموده شد: سه حساب تکراری →
  یک حسابِ بازمانده (قدیمی‌ترین) با موجودیِ کاملِ ۴۰۰، یکتایی برقرار، و اجرای
  دوبارهٔ مهاجرت بی‌اثر.
- هیچ مدلِ تازه‌ای اضافه نشد؛ فقط یک محدودیتِ یکتایی و شکلِ رابطهٔ آن.

## ۱۲) آزمون‌های اضافه/به‌روزشده (Tests Added / Updated)

- `tests/banking.test.ts` (+۴): بازندهٔ رقابت همان حسابِ برنده را می‌گیرد؛
  نقضِ یکتاییِ بی‌پشتوانه بلعیده نمی‌شود؛ هر خطای دیگری عیناً بالا می‌رود؛
  خواندنِ حساب از کلید یکتا (پاسخِ پایدار). mock قدیمیِ `findFirst` به
  `findUnique` به‌روز شد.
- `tests/database-logging.test.ts` (سوئیتِ جدید، ۳ آزمون): نقضِ یکتایی → `warn`؛
  خطای کلید خارجی/تایم‌اوت/رکوردِ مفقود → `error`؛ پیامِ لاگ بدون اصطلاحِ گیج‌کننده.

## ۱۳) نتیجهٔ آزمون (Test Result)

`npx jest` → **۶۸ سوئیت / ۹۷۳ آزمون، همه سبز** (پایهٔ دورِ قبل ۶۷/۹۶۶ بود:
+۱ سوئیت و +۷ آزمون).

## ۱۴) نتیجهٔ ESLint (ESLint Result)

`npx eslint src tests` → پاک، بدون هیچ خطا یا هشدار.

## ۱۵) نتیجهٔ Build (Build Result)

`tsc` پاک؛ `prisma generate` با موتورِ آفلاینِ داخل مخزن
(`prisma-engines/` + `PRISMA_SCHEMA_ENGINE_BINARY`) و بدون هیچ دانلود؛
`offline-deps/packaging.sh` کامل اجرا شد (نصبِ تازه، generate، tsc، کامپایلِ
seed، پیرایش به وابستگی‌های تولیدی، بررسیِ نبودِ موتورِ native، بسته‌بندی).

## ۱۶) مصنوعات آفلاین (Offline Artifacts)

- `offline-deps/dist.tar.gz`: **۶۲۶٬۰۸۴ بایت** (بازسازی شد).
- `offline-deps/node_modules-linux.tar.gz`: **۱۸٬۸۴۹٬۳۱۵ بایت** — این بار
  *لازم* بود بازسازی شود، چون تغییرِ اسکیما کلاینتِ تولیدشده را عوض می‌کند و
  کلاینتِ تولیدشده داخلِ همین بسته حمل می‌شود (`install.sh` دستورِ
  `prisma generate` ندارد).
- `scripts/offline-bundle-verify.cjs` → **PASS**: بستهٔ حمل‌شده در مسیرِ موقت
  باز شد و جریانِ واقعیِ اخطار/مسدودسازی/حذف حساب را روی PostgreSQL زنده اجرا کرد.
- مهاجرتِ تازه با همان `offline-deps/scripts/migrate.js` (بدون CLI) اعمال می‌شود
  و `install.sh` پس از آن seed را اجرا می‌کند.

## ۱۷) هش کامیت (Commit Hash)

`d1c491e` (کد + تست + مهاجرت + اسکریپت‌های راستی‌آزمایی + بسته‌ها) و کامیتِ
مستندات که همین گزارشِ دور نهم را دارد — هر دو روی شاخهٔ
`arena/01a099ed-virtuallife` (والد: `9359198`). کامیت‌های پیشینِ این ممیزی:
`fedf27a`، `dbc6aa6`، `d68bee8`.

## ۱۸) نتیجهٔ Push (Push Result)

`git push origin arena/01a099ed-virtuallife` → موفق. طبق خواسته: **بدون PR و
بدون merge**.

## ۱۹) مسائل باقی‌مانده (Remaining Issues)

- طبقه‌بندیِ سطحِ لاگ بر اساسِ *متنِ* پیامِ Prisma است، چون رویدادِ `error` در
  این نسخه کدِ خطا را به‌شکلِ ساختاریافته نمی‌دهد؛ الگوها عمداً تنگ گرفته
  شدند (فقط نقضِ یکتایی) و پیامِ کامل همیشه لاگ می‌شود، پس چیزی پنهان نمی‌شود.
- اسکریپت‌های smoke به PostgreSQL زنده نیاز دارند و عمداً بخشی از `jest`
  نیستند (CI بدون دیتابیس)؛ اجرایشان پیش از هر استقرار توصیه می‌شود.
- فهرستِ مهارت‌ها از seed می‌آید؛ در دیتابیسِ بدون seed، `listCatalog()` خالی
  است (رفتارِ درست، چون نصبِ واقعی seed را اجرا می‌کند). اسکریپتِ gameplay
  مهارت‌های موردنیازش را خودش می‌سازد تا مستقل بماند.
- برداشتِ سود به موجودیِ خزانه محدود می‌شود (نه ردِ درخواست)؛ این تصمیمِ
  طراحیِ موجود است و اکنون صادقانه گزارش می‌شود.
- بدهی‌های فنیِ ثبت‌شدهٔ دورهای قبل (ماژول‌های منتشرنشده، `text.handler.ts`
  بزرگ، `catch`های عمدیِ «مسیر اصلی نشکند») بدون تغییر باقی ماندند.

## ۲۰) موارد بیرون از دامنه (Out-of-Scope Items)

- بازطراحیِ اقتصاد/هستهٔ زندگی/مهریه (در این دور هم سالم تأیید شدند).
- افزودنِ قابلیتِ تازهٔ گیم‌پلی، کلیدواژهٔ تازه یا تغییرِ نامِ کلیدواژه‌ها.
- تایمر/کرون/صفِ پس‌زمینه؛ soft-delete برای ازدواج؛ چندزبانگی.
- ساختِ PR یا merge (طبق دستور).

— پایان گزارش دور نهم —

---

# 🟢 دور دهم — ممیزیِ رقابتِ اقتصادی: «هرچه پرداخت شد باید تحویل شود» (2026-09-13)

درسِ دور نهم این بود که باگِ حسابِ بانکیِ تکراری با *خواندنِ کد* پیدا نشد؛ با
اجرای همزمان روی دیتابیسِ زنده پیدا شد. پس این دور همان روش را به همهٔ
مسیرهای پول تعمیم داد: یک مهارِ رقابت (concurrency harness) که هر ادعا را چند
بارِ موازی صدا می‌زند و دو اصل را مطالبه می‌کند — «هر هفته‌ای که پولش گرفته شد
باید تحویل شود» و «هرچه از صندوقی بیرون رفت باید به جیبِ کسی برسد».
نتیجه: **پنج مسیرِ پول واقعاً خراب بودند** و اصلاح شدند.

## ۱) خلاصهٔ کامل ممیزی (Full Audit Summary)

- اسکریپتِ تازهٔ `scripts/economy-race-smoke.cjs` پنج مسیرِ پولی را با ۳ تا ۵
  درخواستِ موازی می‌آزماید: عضویتِ باشگاه، تسویهٔ سپردهٔ سررسیدشده، پاداشِ
  روزانهٔ حضور، برداشتِ محصول، پاداشِ اقامتِ معرفی.
- افزون بر اجرای زنده، یک اسکنِ ساختاری روی همهٔ `src/modules` و
  `src/database/repositories` انجام شد تا هر متدی که *پیش از* تراکنشِ پرداخت،
  نوشتاری روی دیتابیس دارد پیدا شود (الگوی «ادعا بیرون از تراکنش» و الگوی
  «اول پول را ببر، بعد قفل بزن»).
- نمونهٔ مقایسه‌ایِ سالم هم در همین مخزن بود: `lottery.settleLastWeek` و
  `playerLoan.repay` و `bankingRepository.repayLoan` دقیقاً همان کاری را
  می‌کنند که باید (قفل اول، و شکست = پرتاب برای بازگشتِ تراکنش). اصلاح‌ها
  همان الگو را به پنج مسیرِ باقی‌مانده برد، بدون هیچ بازطراحی.

## ۲) باگ‌های پیداشده (Bugs Found)

1. **باشگاه: سه هفته پول، یک هفته عضویت.** تمدید از عضویتی حساب می‌شد که
   *پیش از* تراکنش خوانده شده بود. سه خریدِ همزمان، هر سه از یک مبدأ حساب
   کردند؛ هر سه حقِ اشتراک کسر شد و سه ردیف عضویت با **یک** تاریخ انقضا نوشته
   شد. روی دیتابیس زنده اندازه‌گیری شد: `paid for 3 week(s) but the membership
   only covers 7.00 day(s)`.
2. **سپرده: سوختنِ نقدینگیِ بانک.** صندوق بانک *پیش از* قفلِ شرطی کسر می‌شد و
   بازندهٔ رقابت به‌جای پرتاب کردن، `return false` می‌کرد — یعنی تراکنش
   commit می‌شد و کسرِ صندوق می‌ماند. روی دیتابیس زنده با سه تسویهٔ موازی:
   صندوق **۱۵٬۲۴۶٬۵۷۶** از دست داد، بازیکن **۵٬۰۸۲٬۱۹۲** گرفت؛ یعنی
   **۱۰٬۱۶۴٬۳۸۴ پول نابود شد**. هر دوبار کلیک روی پنل سپرده همین را تکرار
   می‌کرد و بانک را به‌تدریج بی‌نقدینگی می‌برد (سپرده‌های همه «معوق» می‌شد).
   دو نکته دربارهٔ شدتِ اثر: `settleMatured` در `getBoard` صدا زده می‌شود،
   یعنی **هر بار باز کردنِ پنل سپرده** آن را اجرا می‌کند (نه فقط یک دستورِ
   خاص)، و این باگ *متفاوت* از آن چیزی است که در دور اولِ همین جدول در همان
   متد اصلاح شد (دور اول «بستنِ سپرده بیرون از تراکنشِ پرداخت» را بست؛ این دور
   «کسرِ صندوق پیش از قفل» و «بازندهٔ رقابت که به‌جای پرتاب، `return` می‌کرد»
   را).
3. **صندوقِ هفتهٔ بی‌نقص: ادعا بیرون از تراکنشِ پرداخت.** ردیفِ
   `weekly_chests` پیش از پرداخت نوشته می‌شد؛ اگر پرداخت می‌مرد، صندوق برای
   همیشه «گرفته‌شده» می‌ماند و پول هرگز نمی‌رسید.
4. **نشان‌ها (achievements): همان الگو.** ردیفِ نشان بیرون از تراکنشِ پرداخت
   نوشته می‌شد و علاوه بر آن، *هر* خطایی (نه فقط نقضِ یکتایی) بی‌صدا بلعیده
   می‌شد؛ یعنی هم پاداش می‌سوخت و هم هیچ ردی در لاگ نمی‌ماند.
5. **پاداشِ اقامتِ معرفی: همان الگو.** `bonusPaidAt` پیش از تراکنشِ پرداخت
   نوشته می‌شد، در حالی که مرحلهٔ اولِ معرفی در **همان فایل** الگوی درست را
   داشت (ادعا داخل تراکنش) — ناسازگاریِ دو مرحلهٔ یک سیستم.
6. **پیامِ گمراه‌کننده به بازیکن.** در صندوقِ هفته، هر خطای دیتابیس (مثلاً قطع
   اتصال) به بازیکن «صندوق این هفته را گرفته‌ای» می‌گفت.

## ۳) ریشهٔ علل (Root Cause)

سه الگوی تکراری، هیچ‌کدام با بازبینیِ تک‌فایلی دیدنی نبودند:

- «مقدارِ وابسته به وضعیت را *پیش از* تراکنش حساب کن» → در باشگاه، چون کسرِ
  شرطیِ موجودی همان قفلِ ردیفِ بازیکن است، درخواستِ دوم منتظر می‌ماند؛ ولی
  مقدارِ تمدید از خواندهٔ کهنهٔ پیش از تراکنش می‌آمد.
- «اول پول را ببر، بعد قفل بزن، و اگر قفل نگرفت `return` کن» → `return` در
  callback تراکنش یعنی **commit**؛ تنها `throw` تراکنش را برمی‌گرداند.
- «ادعا را بیرون از تراکنشِ پرداخت بنویس» → ادعا و پرداخت دو نقطهٔ شکستِ جدا
  می‌شوند و شکستِ دومی، اولی را برای همیشه می‌سوزاند.

## ۴) اصلاحات (Fixes)

- `src/modules/gym/gym.service.ts`: خواندنِ عضویتِ فعال و محاسبهٔ انقضا به
  *داخل* تراکنش و **پس از** کسرِ شرطی منتقل شد؛ نتیجهٔ تراکنش
  (`expiresAt`, `extended`) به بیرون برمی‌گردد تا کلیدِ dedupe و پاسخِ پنل هم
  از همان مقدارِ واقعی بیایند.
- `src/modules/banking/deposit.service.ts`: وقتی قفلِ بستنِ سپرده صفر ردیف
  می‌گیرد، تراکنش با یک نشانهٔ داخلی (`SettledElsewhere`) **پرتاب** می‌کند تا
  کسرِ صندوق برگردد؛ بیرون از تراکنش این نشانه به «کاری نکن» ترجمه می‌شود و
  *برخلافِ* «صندوق نقدینگی نداشت» شمارندهٔ تسویهٔ معوق را بالا نمی‌برد (پس
  پیامِ پنل دروغ نمی‌گوید).
- `src/modules/quests/daily-quest.service.ts`: ساختِ ردیفِ صندوق داخلِ همان
  تراکنشِ پرداخت؛ فقط `P2002` به «صندوق این هفته را گرفته‌ای» ترجمه می‌شود و
  هر خطای دیگری عیناً بالا می‌رود.
- `src/modules/achievements/achievement.service.ts`: ادعا و پرداخت در یک
  تراکنش؛ `P2002` یعنی «پیش‌تر پرداخت شده» و هر خطای دیگر با `logger.warn`
  ثبت می‌شود و نشان در بازدیدِ بعدیِ تخته‌نشان **دوباره** پرداخت می‌شود
  (تخته هم به‌خاطرِ یک نشان بسته نمی‌شود).
- `src/modules/rewards/rewards.service.ts`: پاداشِ اقامت دقیقاً هم‌شکلِ مرحلهٔ
  اولِ معرفی شد — قفلِ شرطی داخل تراکنش، و در صورتِ نگرفتنِ قفل هیچ پولی
  جابه‌جا نمی‌شود.

## ۵) ازدواج و مهریه (Marriage / Mahr Changes)

بدون تغییرِ کد. `scripts/marriage-live-smoke.cjs` پس از این دور دوباره اجرا و
سبز شد (قانونِ مهریه روی `numeric(65,30)`، پذیرشِ صریح، جابه‌جاییِ دقیقِ پول،
بی‌اثریِ تکرار و رقابت، بازگشتِ تراکنش در موجودیِ ناکافی، پاداشِ روزانهٔ
یک‌بار، طلاقِ تک‌تسویه).

## ۶) ثبت گروه و سطح محیط (Group Registration Changes)

بدون تغییرِ کد. `scripts/gameplay-live-smoke.cjs` دوباره سبز شد: ثبتِ یک‌باره،
بی‌اثریِ عددِ نامشخص (`null`) روی جمعیت و سطحِ محیطِ ذخیره‌شده، ارتقای سطح با
رشدِ واقعی، ثبتِ همزمان → یک ردیف، عضویتِ بدونِ ردیفِ تکراری، و هم‌خوانیِ
متنِ آستانه‌ها با طبقه‌بند.

## ۷) پنل ادمین (Admin Panel Changes)

بدون تغییر. هر دو اسکریپتِ ادمین (`admin-ux-smoke` و `admin-moderation-smoke`)
دوباره سبز شدند.

## ۸) اصلاحات امنیتی (Security Fixes)

- **Double-charge**: عضویتِ باشگاه دیگر نمی‌تواند دو بار پول بگیرد و یک بار
  تحویل دهد؛ هفته‌های پرداخت‌شده همیشه روی هم انباشته می‌شوند (live: سه خریدِ
  پشت‌سرهم → ۷، ۱۴ و ۲۱ روز).
- **نشتیِ نقدینگیِ بانک**: بسته شد؛ حالا «هرچه صندوق از دست داد = آنچه بازیکن
  گرفت» (live: پس از اصلاح `pool lost 5,082,192 / player received 5,082,192`).
- **سوختنِ پاداش**: سه مسیرِ پاداش (صندوقِ هفته، نشان، پاداشِ اقامت) دیگر
  نمی‌توانند ادعا را بدون پرداخت ثبت کنند؛ شکست = بازگشتِ کامل و تلاشِ دوباره
  در بازدیدِ بعدی.
- **رقابتِ پاداش‌ها**: پاداشِ روزانهٔ حضور، برداشتِ محصول و پاداشِ اقامتِ
  معرفی زیر ۳ تا ۵ درخواستِ موازی دقیقاً یک‌بار پرداخت می‌شوند (یک ردیفِ
  دفتر کل، یک جابه‌جاییِ موجودی).
- **صداقتِ پیام**: خطای واقعیِ دیتابیس دیگر پشتِ «قبلاً گرفته‌ای» پنهان
  نمی‌شود؛ فقط نقضِ یکتایی (`P2002`) آن پیام را می‌گیرد.

## ۹) تجربهٔ کاربری تلگرام (Telegram UX Changes)

- هیچ متنِ تازهٔ بازیکن اضافه نشد؛ موجودیِ ایستای متن‌ها **۵۱۷۴** رشته ماند
  (فقط شمارهٔ خط‌ها جابه‌جا شد و فهرست بازتولید شد).
- تجربهٔ واقعی بهتر شد: بازیکنی که دو بار پشتِ هم «باشگاه» را می‌زند حالا دو
  هفته عضویت می‌گیرد (نه یک هفته با دو بار پرداخت)، و پاداشی که به خطایِ
  لحظه‌ای دیتابیس می‌خورد در بازدیدِ بعدی می‌رسد به‌جای آنکه برای همیشه گم شود.
- پیامِ پنلِ سپرده دربارهٔ «تسویهٔ معوق» فقط وقتی نشان داده می‌شود که واقعاً
  صندوق نقدینگی نداشته باشد.

## ۱۰) کلیدواژه‌ها و callbackها (Keyword / Callback Changes)

بدون تغییر: ۲۰۶ مسیر و ۱۵۶ کلیدواژه (بازتولید و مقایسه شد).

## ۱۱) دیتابیس و مهاجرت (Database / Migration Changes)

**هیچ تغییرِ اسکیما یا مهاجرتی لازم نشد** — هر پنج باگ در لایهٔ سرویس بودند،
نه در مدلِ داده. شمارِ مهاجرت‌ها همان **۲۳** است و drift صفر باقی ماند.
(اسکنِ ساختاری هم تأیید کرد دو نوشتارِ باقی‌ماندهٔ «پیش از تراکنش» —
علامت‌گذاریِ هفتهٔ بدون بلیت در قرعه‌کشی و علامت‌گذاریِ روزِ بدونِ هدیه در
حیوان خانگی — پولی نیستند و قفلِ شرطیِ خودشان را دارند، پس بی‌خطرند.)

## ۱۲) آزمون‌های اضافه/به‌روزشده (Tests Added / Updated)

- `tests/banking-health-rental-passport.test.ts` (+۶): انباشتِ سه هفتهٔ پیاپی؛
  تمدید از برندهٔ رقابتِ همزمان (۱۴ روز، نه ۷ روز)؛ بدون موجودی هیچ نوشتاری
  انجام نمی‌شود؛ به ازای هر هفته دقیقاً یک حقِ اشتراک در دفتر کل؛ بازگشتِ کسرِ
  صندوق وقتی تسویه را درخواستِ دیگری برده است؛ و تفکیکِ «رقابت را باخت» از
  «صندوق خالی بود».
- `tests/daily-loop.test.ts` (+۳): ردیفِ صندوقِ هفته را همان تراکنشی می‌نویسد
  که پولش را می‌دهد؛ ادعا و پرداختِ نشان در یک تراکنش؛ و اگر پرداخت بمیرد نشان
  «گرفته‌شده» نمی‌ماند تا در بازدیدِ بعدی دوباره پرداخت شود.
- `tests/referral.test.ts` (۳ آزمون در جا به‌روز شد، شمارِ آزمون‌ها عوض نشد):
  قفلِ پاداشِ اقامت حالا داخلِ تراکنش سنجیده می‌شود؛ رقابتِ بازنده هیچ پول و
  هیچ ردیفِ دفترِ کلی نمی‌گذارد.
- یک آزمونِ قدیمیِ صندوقِ هفته که نقضِ یکتایی را با `new Error('unique
  violation')` جعل می‌کرد، به `Prisma.PrismaClientKnownRequestError` با کدِ
  واقعیِ `P2002` تغییر کرد — چون سرویس (درست) فقط همان را ترجمه می‌کند.
- ابزارِ مشترکِ تست: `makeTransactionScopedDb` — دیتابی که در آن نوشتنِ «ادعا»
  بیرون از تراکنشِ پرداخت با پرتابِ صریح لو می‌رود؛ همان اشتباهِ ساختاری را در
  آینده هم می‌گیرد.

## ۱۳) نتیجهٔ آزمون (Test Result)

`npx jest` → **۶۸ سوئیت / ۹۸۲ آزمون، همه سبز** (پایهٔ دورِ نهم ۶۸ سوئیت /
۹۷۳ آزمون بود؛ +۹ آزمون = ۶ در `banking-health-rental-passport` و ۳ در
`daily-loop`).

## ۱۴) نتیجهٔ ESLint (ESLint Result)

`npx eslint src tests` → پاک، بدون خطا و هشدار.

## ۱۵) نتیجهٔ Build (Build Result)

`tsc` پاک (شاملِ کامپایلِ `prisma/seed.ts` برای بستهٔ آفلاین)؛ هیچ تولیدِ
کلاینتِ تازه‌ای لازم نشد چون اسکیما دست نخورد.

## ۱۶) مصنوعات آفلاین (Offline Artifacts)

- `offline-deps/dist.tar.gz`: **۶۲۷٬۷۸۱ بایت** (بازسازی شد).
- `offline-deps/node_modules-linux.tar.gz`: **۱۸٬۸۴۹٬۳۱۵ بایت — بدون تغییر**،
  چون نه اسکیما عوض شد و نه وابستگی‌ها (طبق قاعدهٔ خودِ
  `offline-deps/README.md`).
- `scripts/offline-bundle-verify.cjs` → **PASS** روی PostgreSQL زنده.
- هر پنج اسکریپتِ راستی‌آزمایی زنده پس از بازسازیِ بسته دوباره اجرا و سبز
  شدند: `economy-race-smoke`, `marriage-live-smoke`, `gameplay-live-smoke`,
  `admin-moderation-smoke`, `admin-ux-smoke`.

## ۱۷) هش کامیت (Commit Hash)

`0750330` (کد + تست + اسکریپتِ رقابت + بستهٔ `dist`) و کامیتِ مستندات که همین
گزارشِ دور دهم را دارد — روی شاخهٔ `arena/01a099ed-virtuallife`.
کامیت‌های پیشینِ این ممیزی: `fedf27a`, `dbc6aa6`, `d68bee8`, `d1c491e`,
`ab1b08d`, `955a53e`.

## ۱۸) نتیجهٔ Push (Push Result)

`git push origin arena/01a099ed-virtuallife` → موفق. **بدون PR و بدون merge.**

## ۱۹) مسائل باقی‌مانده (Remaining Issues)

- تشخیصِ `P2002` در نُه جا از هشت فایل با الگوی تکراریِ
  `error instanceof Prisma.PrismaClientKnownRequestError && error.code === 'P2002'`
  نوشته شده است؛ یک helper مشترک پاک‌سازیِ خوشایندی است ولی در این دور برای
  بزرگ‌نشدنِ diff انجام نشد (رفتار همه‌جا یکسان است).
- مهارِ رقابتِ `economy-race-smoke` مسیرهای پولیِ پُرریسک را پوشش می‌دهد، نه
  همهٔ ماژول‌ها (بورس، حراجی، اجاره، وام بازیکنی و … الگوی درستِ «قفل اول،
  شکست = پرتاب» را در بازبینیِ کد داشتند و سالم دیده شدند).
- اسکریپت‌های smoke به PostgreSQL زنده نیاز دارند و عمداً بخشی از `jest`
  نیستند؛ پیش از هر استقرار توصیه می‌شوند.
- «سوختنِ پاداش» در سه مسیر اصلاح شد، ولی اگر در گذشتهٔ دیتابیسِ واقعی چنین
  رخ داده باشد (نشانِ ثبت‌شده بدون ردیفِ پرداخت)، بازیابیِ تاریخیِ آن یک
  اقدامِ جداست و در این دور انجام نشد.

## ۲۰) موارد بیرون از دامنه (Out-of-Scope Items)

- بازطراحیِ اقتصاد/هستهٔ زندگی/مهریه/پنل‌ها (الگوهای سالمِ موجود حفظ شدند).
- افزودنِ قابلیت، کلیدواژه یا callback تازه؛ تغییرِ اسکیما یا مهاجرت.
- تایمر/کرون/صفِ پس‌زمینه؛ بازیابیِ دادهٔ تاریخیِ پاداش‌های سوخته.
- ساختِ PR یا merge (طبق دستور).

— پایان گزارش دور دهم —

---

# 🟢 دور یازدهم — ادامهٔ ممیزیِ پول از آخرین وضعیت: سودِ بانکی، شانسِ روزانه، و خبری که از تراکنشِ برگشتی باز می‌ماند (2026-09-13)

دور دهم با پنج مسیرِ پولیِ اصلاح‌شده و یک مهارِ رقابت بسته شد. این دور دقیقاً از
همان‌جا ادامه یافت: همان دو پرسش («هرچه از صندوق رفت به جیب رسید؟» و «اگر پرداخت
بمیرد، ادعا می‌سوزد؟») با دو اسکنِ ساختاریِ تازه و دو مهارِ زنده به باقیِ
مسیرهای پول برده شد. نتیجه: **دو باگِ پولیِ تازه (F و G) با شاهدِ زنده**، و یک
ناسازگاریِ ساختاری در چهار مسیر (H) که همان قاعدهٔ دور دهم را نقض می‌کرد.

## ۱) خلاصهٔ کامل ممیزی (Full Audit Summary)

- دو اسکنِ ساختاری روی همهٔ `src/`:
  1. **کاهشِ صندوق/خزانه پیش از قفلِ شرطی** — هر چهار نقطهٔ `pool.debit` و
     `pool.debitUpTo` دستی خوانده شدند: `deposit.breakEarly` سالم (بازنده
     `ConflictError` پرتاب می‌کند)، `loan.take` سالم، `deposit.settleMatured`
     اصلاحِ دور دهم، و `banking.repository.settleInterest` **خراب** (باگ F).
  2. **بازندهٔ رقابت که به‌جای پرتاب `return` می‌کند** — ۷ نقطه پیدا شد و همه
     دستی خوانده شدند: `player.repository` (ثبت‌نامِ تحصیل)، `auction.closeDue`،
     `elections.settleExpiredElections`، `trade.sweepExpired`،
     `fortune.applyMoney` و دو متدِ معرفی — در شش مورد، نوشتارِ پیش از `return`
     یک `updateMany` شرطیِ **صفر-ردیفی** بود (یعنی هیچ چیز نوشته نشده) پس
     بی‌خطرند؛ تنها `fortune` واقعاً مشکل داشت (باگ G).
     *نکتهٔ صادقانه دربارهٔ خودِ اسکنر:* این اسکنر فقط `return`های خالی/`false`
     را می‌دید و `return { ... }` را نه — یعنی باگ F را اسکنرِ «کاهشِ صندوق»
     پیدا کرد، نه این یکی. هر دو اسکن با خواندنِ دستیِ همهٔ نقاطِ علامت‌خورده
     تکمیل شدند.
  3. **نوشتارِ غیرمالی داخلِ تراکنشِ مالی** — چهار نقطه: `recordPlayerEvent`
     داخلِ تراکنش در `rewards.claimDailyStreak`، `daily-quest.claimCard`،
     `overtime.startOvertime` و `trade.buyListing` (باگ H).
- دو مهارِ زنده: `scripts/economy-race-smoke.cjs` از ۵ به **۷ بخش** گسترش یافت
  (سودِ حسابِ بانکی و شانسِ روزانه اضافه شدند) و اسکریپتِ تازهٔ
  `scripts/reward-burn-smoke.cjs` با **تزریقِ خطا در سطح دیتابیس** (یک `CHECK`
  که هر ردیفِ دفتر کل را رد می‌کند) می‌پرسد: اگر پرداخت وسط کار بمیرد، ادعا
  ثبت می‌ماند یا نه؟

## ۲) باگ‌های پیداشده (Bugs Found)

1. **F — سوختنِ نقدینگیِ بانک در تسویهٔ سودِ حساب.** در
   `banking.repository.settleInterest` صندوق بانک با `debitUpTo` **پیش از** قفلِ
   شرطی (`lastInterestAt`) کسر می‌شد و بازندهٔ رقابت به‌جای پرتاب،
   `return { interestAccrued: 0 }` می‌کرد — یعنی تراکنش commit می‌شد و کسرِ
   صندوق می‌ماند. روی دیتابیس زنده با سه کلیکِ همزمان روی «دریافت سود»:
   صندوق **۱۲۳٬۳۰۰** از دست داد و حسابِ بازیکن **۴۱٬۱۰۰** گرفت، یعنی
   **۸۲٬۲۰۰ پول نابود شد** (دقیقاً دو برابرِ سودِ پرداختی = دو بازندهٔ رقابت).
   ضمناً شمارندهٔ `totalDepositInterest` در ترازنامهٔ صندوق هم به ازای پولی که
   هرگز پرداخت نشده بود باد می‌کرد. مسیر کاملاً در دسترسِ بازیکن است: دکمهٔ
   «دریافت سود» در پنل بانک.
2. **G — سوختنِ شانسِ روزانه.** در `fortune.draw` ردیفِ `dailyFortune` (ادعا)
   **بیرون از** تراکنشِ پرداخت نوشته می‌شد و پرداخت بعداً در تراکنشی جدا انجام
   می‌گرفت. اگر پرداخت می‌مرد، ادعا می‌ماند و چون کلیدِ یکتا `(بازیکن، روز)`
   است، کشیدنِ دوباره همان روز ممکن نبود. با تزریقِ خطا روی دیتابیس زنده
   اندازه‌گیری شد: `claim rows left by the failure: 1`، `wallet delta: 0`، و
   تلاشِ دوباره روی دیتابیسِ سالم → `ConflictError: Already drawn` با
   `wallet delta: 0` ⇒ **شانسِ آن روز برای همیشه سوخت**.
3. **G۲ — پیامِ غلط به بازیکن.** همان‌جا هر خطایی (`catch {}`) به «شانس
   امروزت را کشیده‌ای» ترجمه می‌شد؛ یعنی قطعِ لحظه‌ایِ دیتابیس هم به بازیکن
   می‌گفت «قبلاً کشیده‌ای» — نقضِ صریحِ قاعدهٔ دور دهم (فقط `P2002`).
4. **G۳ — دادهٔ ذخیره‌شدهٔ ناراست.** توانِ پرداختِ «ضرر» با یک `count`ِ جدا
   *پیش از* ادعا سنجیده می‌شد؛ اگر موجودی بین آن دو عوض می‌شد، کسرِ شرطیِ داخل
   تراکنش بی‌اثر می‌ماند ولی ردیفِ ذخیره‌شده می‌گفت «ضررِ X تومان» — و پنلِ
   «شانس» همان را به بازیکن نشان می‌داد در حالی که هیچ پولی کسر نشده بود.
5. **H — خبرِ بازمانده از تراکنشِ برگشتی.** در چهار مسیرِ مالی
   (`claimDailyStreak`, `claimCard`, `startOvertime`, `buyListing`) رخدادِ خوراک
   *داخلِ* تراکنش ثبت می‌شد، ولی `EventService` کلاینتِ ریشه را دارد نه `tx` را:
   پس آن نوشتار بخشی از تراکنش نبود. دو اثر: (الف) اگر تراکنش برمی‌گشت، ردیفِ
   خبری باز می‌ماند و درآمد/فروشی را روایت می‌کرد که هرگز رخ نداده؛ (ب) وسطِ
   یک تراکنشِ باز، کانکسیونِ دومی از استخر اشغال می‌شد. این دقیقاً همان چیزی
   است که دور دهم برای نشان‌ها قاعده گذاشت («ثبتِ رخداد پس از تراکنش») و این
   چهار نقطه از آن قاعده جا مانده بودند.

## ۳) ریشهٔ علل (Root Cause)

- همان سه الگوی دور دهم، این بار در دو فایلِ دیگر: «اول پولِ صندوق را ببر، بعد
  قفل بزن» و «بازندهٔ رقابت `return` می‌کند نه `throw`» (F)؛ و «ادعا بیرون از
  تراکنشِ پرداخت» (G).
- یک الگوی تازه: «نوشتارِ best-effortِ غیرمالی داخلِ تراکنشِ مالی» (H) — چون
  `EventService` عمداً هیچ‌وقت نباید مسیرِ بازی را بشکند، نوشتارش از تراکنش
  بیرون است؛ پس جایش هم باید بیرون از تراکنش باشد.
- ریشهٔ مشترکِ G۲ و G۳: ترجمهٔ «هر خطا» به یک پیامِ ثابت، و سنجیدنِ شرطِ پول
  در زمانی جدا از زمانِ اعمالِ آن.

## ۴) اصلاحات (Fixes)

- `src/database/repositories/banking.repository.ts`: نشانهٔ داخلیِ
  `InterestSettledElsewhere` (هم‌شکلِ `SettledElsewhere` در سپرده)؛ بازندهٔ رقابت
  حالا **پرتاب** می‌کند تا کسرِ صندوق و باد شدنِ شمارنده برگردد، و بیرون از
  تراکنش به `{ interestAccrued: 0 }` ترجمه می‌شود — پس بازیکن همان پیامِ
  «سود جدیدی نداری» را می‌بیند و هیچ خطایی به او نشت نمی‌کند. `newBalance` هم
  که به پنل برمی‌گردد حالا **تازه** خوانده می‌شود (پیش‌تر عددِ کهنهٔ پیش از
  تراکنش برمی‌گشت).
- `src/modules/rewards/fortune.service.ts`: کلِ `draw` در **یک** تراکنش؛ ادعا
  *آخرین* نوشتار است، پس `P2002` با پرتاب کل تراکنش را برمی‌گرداند (کشیدنِ
  تکراری حتی یک ریال جابه‌جا نمی‌کند) و شکستِ پرداخت هیچ ادعایی به جا
  نمی‌گذارد. تصمیمِ «ضرر» از خودِ کسرِ شرطی گرفته می‌شود (نه از یک `count`ِ
  قبلی)، پس آنچه ذخیره و نمایش داده می‌شود همان است که واقعاً رخ داده. فقط
  `P2002` پیامِ «قبلاً کشیده‌ای» می‌گیرد. متدِ خصوصیِ `applyMoney` که دیگر
  لازم نبود حذف شد (کدِ مرده).
- `src/modules/rewards/rewards.service.ts`, `src/modules/quests/daily-quest.service.ts`,
  `src/modules/occupation/overtime.service.ts`, `src/modules/market/trade.service.ts`:
  ثبتِ رخدادِ خوراک به **پس از** تراکنشِ پرداخت منتقل شد. در اضافه‌کاری،
  تراکنش حالا `{ playerId, result }` برمی‌گرداند تا مسیرِ «قفل را دیگری برد»
  (`skipped`) هیچ رخدادی ثبت نکند؛ در معامله، شناسهٔ داخلیِ فروشنده از تراکنش
  بیرون می‌آید و از پاسخِ خرید **حذف** می‌شود (تست دارد که به خریدار
  برنمی‌گردد).

## ۵) ازدواج و مهریه (Marriage / Mahr Changes)

بدون تغییرِ کد؛ `scripts/marriage-live-smoke.cjs` پس از این دور دوباره سبز شد.

## ۶) ثبت گروه و سطح محیط (Group Registration Changes)

بدون تغییرِ کد؛ `scripts/gameplay-live-smoke.cjs` دوباره سبز شد.

## ۷) پنل ادمین (Admin Panel Changes)

بدون تغییر. دو مسیرِ تغییرِ موجودیِ ادمین (`adjustBalance` و `setPlayerField`)
بازبینی شدند: هر دو نوشتارِ مقایسه‌ای (`where: { balance: <مقدارِ خوانده‌شده> }`)
با ردیفِ دفتر کل و گزارشِ ادمین در یک تراکنش دارند و در رقابت `ConflictError`
می‌دهند — سالم بودند و دست نخوردند. هر دو smoke ادمین سبز.

## ۸) اصلاحات امنیتی (Security Fixes)

- **نشتیِ نقدینگیِ صندوق بانک** در پرکاربردترین مسیرِ سود بسته شد: هر کلیکِ
  «دریافت سود» می‌توانست بخشی از صندوق را نابود کند؛ حالا
  «صندوق از دست داد = حساب گرفت» (live: پیش از اصلاح ۱۲۳٬۳۰۰ در برابر
  ۴۱٬۱۰۰؛ پس از اصلاح برابر).
- **سوختنِ پاداش** در شانسِ روزانه بسته شد: شکستِ پرداخت دیگر ادعا به جا
  نمی‌گذارد و تلاشِ دوباره دقیقاً یک‌بار می‌پردازد (live: پیش از اصلاح BURNED،
  پس از اصلاح RECOVERED با ۸۰٬۰۰۰ و یک ردیفِ دفتر کل).
- **دوبار پرداخت در رقابت**: ادعایِ شانسِ روزانه آخرین نوشتارِ تراکنش است، پس
  دو کلیکِ همزمان یکی را `ConflictError` می‌دهد و پولِ کلیکِ بازنده برمی‌گردد
  (در مهارِ رقابت: ۳ کشیدنِ موازی → یک برنده، دو `ConflictError`، موجودی دقیقاً
  به اندازهٔ ردیفِ ذخیره‌شده).
- **خوراکِ صادقانه**: هیچ ردیفِ خبری نمی‌تواند از تراکنشی که برگشته باز بماند،
  و «اضافه‌کاریِ رد شده» دیگر خبری ثبت نمی‌کند.
- **صداقتِ پیام‌ها**: فقط `P2002` به بازیکن «قبلاً گرفته‌ای/کشیده‌ای» می‌گوید.

## ۹) تجربهٔ کاربری تلگرام (Telegram UX Changes)

- هیچ متنِ تازهٔ بازیکن اضافه نشد و هیچ متنی عوض نشد: موجودیِ ایستای متن‌ها
  **۵۱۷۴** رشته ماند (فقط شمارهٔ خط‌ها جابه‌جا شد و فهرست بازتولید شد).
- پیامِ «سود جدیدی نداری» وقتی نشان داده می‌شود که واقعاً سودی نباشد؛ پیش از
  این، بازندهٔ رقابت هم همان پیام را می‌گرفت در حالی که یک بار سودِ کامل
  پرداخت شده بود و دو بار پولِ صندوق سوخته بود.
- عددِ موجودیِ حساب که پس از تسویهٔ سود برمی‌گشت، کهنه بود (خواندهٔ پیش از
  تراکنش)؛ حالا تازه خوانده می‌شود.
- پنلِ «شانس» دیگر نمی‌تواند ضرری را روایت کند که کسر نشده است.

## ۱۰) کلیدواژه‌ها و callbackها (Keyword / Callback Changes)

بدون تغییر: ۲۰۶ مسیر و ۱۵۶ کلیدواژه (بازتولید و مقایسه شد).

## ۱۱) دیتابیس و مهاجرت (Database / Migration Changes)

**هیچ تغییرِ اسکیما یا مهاجرتی لازم نشد**؛ شمارِ مهاجرت‌ها همان **۲۳** و drift
صفر است. هر دو باگ در لایهٔ سرویس/ریپازیتوری بودند.
(اسکریپتِ تازهٔ `reward-burn-smoke.cjs` با `ALTER TABLE ... ADD CONSTRAINT ...
NOT VALID` خطا را تزریق می‌کند؛ این فقط روی دیتابیسِ دورریختنیِ `_ux_test`
اجرا می‌شود، مهاجرت نیست و در پایان همان اجرا پاک می‌شود.)

## ۱۲) آزمون‌های اضافه/به‌روزشده (Tests Added / Updated)

- `tests/daily-loop.test.ts` (+۳، و ۲ آزمونِ شانس به‌روز):
  - تازه: «کشیدنِ روزانه را همان تراکنشی ادعا می‌کند که پولش را می‌دهد»؛
    «خطای واقعیِ دیتابیس به‌جای «قبلاً کشیده‌ای» بالا می‌رود»؛ «رخدادِ خوراکِ
    کارتِ روزانه پس از commitِ پرداخت ثبت می‌شود».
  - به‌روز: کشیدنِ تکراری حالا با `Prisma.PrismaClientKnownRequestError` و کدِ
    واقعیِ `P2002` سنجیده می‌شود (پیش‌تر `new Error('unique violation')` بود که
    دیگر ترجمه نمی‌شود)؛ و «ضررِ بی‌اثر وقتی موجودی کافی نیست» به‌جای
    `player.count` از خودِ گاردِ `balance >= amount` می‌آید و بررسی می‌کند که
    ردیفِ ذخیره‌شده هم `neutral/0` باشد.
- `tests/expansion.test.ts` (+۴): ترتیبِ «اول پرداخت، بعد خبر» برای استریک،
  اضافه‌کاری و معاملهٔ بازار؛ «اضافه‌کاریِ ردشده هیچ خبری ثبت نمی‌کند»؛ و
  بررسی اینکه شناسهٔ داخلیِ فروشنده در پاسخِ خرید به خریدار برنمی‌گردد.
- ابزار: هر چهار تستِ ترتیب، `$transaction` را طوری می‌پوشانند که لحظهٔ
  «commit» را ثبت کند و ترتیبِ واقعیِ فراخوانی‌ها را بسنجد (نه فقط حضورشان را).

## ۱۳) نتیجهٔ آزمون (Test Result)

`npx jest` → **۶۸ سوئیت / ۹۸۹ آزمون، همه سبز** (پایهٔ دور دهم ۶۸/۹۸۲ بود؛
+۷ آزمون).

## ۱۴) نتیجهٔ ESLint (ESLint Result)

`npx eslint src tests` → پاک، بدون خطا و هشدار.

## ۱۵) نتیجهٔ Build (Build Result)

`tsc` پاک (شاملِ کامپایلِ `prisma/seed.ts` برای بستهٔ آفلاین).

## ۱۶) مصنوعات آفلاین (Offline Artifacts)

- `offline-deps/dist.tar.gz`: **۶۲۹٬۸۲۹ بایت** (بازسازی شد و بازبینی شد که
  اصلاحات داخلش هستند).
- `offline-deps/node_modules-linux.tar.gz`: **۱۸٬۸۴۹٬۳۱۵ بایت — بدون تغییر**
  (نه اسکیما عوض شد، نه وابستگی‌ها).
- `offline-deps/README.md`: فهرستِ اسکریپت‌های راستی‌آزمایی کامل شد — دو مهارِ
  پولی (`economy-race-smoke` از دور دهم که جا مانده بود، و `reward-burn-smoke`
  از این دور) با توضیحِ اینکه هرکدام چه پرسشی را جواب می‌دهند.
- راستی‌آزمایی زنده پس از بازسازیِ بسته، هر هفت مورد سبز:
  `economy-race-smoke` (۷ بخش)، `reward-burn-smoke` (۳ بخش)،
  `marriage-live-smoke`, `gameplay-live-smoke`, `admin-moderation-smoke`,
  `admin-ux-smoke`, `offline-bundle-verify`.

### شاهدِ زندهٔ این دور (پیش از اصلاح → پس از اصلاح)

| باگ | اندازه‌گیری پیش از اصلاح | اندازه‌گیری پس از اصلاح |
|---|---|---|
| F — سه کلیکِ همزمان «دریافت سود» | صندوق ۱۲۳٬۳۰۰ از دست داد / حساب ۴۱٬۱۰۰ گرفت ⇒ ۸۲٬۲۰۰ نابود | صندوق = حساب؛ یک ردیفِ دفتر کل؛ ساعتِ سود یک‌بار ۵ روز جلو؛ شمارندهٔ صندوق = سودِ پرداختی |
| G — مرگِ پرداخت وسطِ کشیدنِ شانس | `claim rows: 1`، کیف پول ۰، تلاشِ دوباره → `Already drawn` ⇒ BURNED | `claim rows: 0`، کیف پول ۰، تلاشِ دوباره → ۸۰٬۰۰۰ با یک ردیفِ دفتر کل ⇒ RECOVERED |
| C/G — مرگِ پرداخت وسطِ صندوقِ هفته | `reward-burn-smoke`: «the failed attempt left no claim behind» با `actual: 1, expected: 0` شکست | PASS؛ ادعا نمی‌ماند، پول نمی‌رود، تلاشِ دوباره یک‌بار می‌پردازد و کشیدنِ سوم `ConflictError` می‌گیرد |

## ۱۷) هش کامیت (Commit Hash)

`04e0d89` — کد، تست‌ها، دو مهارِ زنده، بستهٔ `dist`، `AGENTS.md` و
`offline-deps/README.md`؛ و کامیتِ مستنداتی که همین گزارشِ دور یازدهم را دارد.
هر دو روی شاخهٔ `arena/01a099ed-virtuallife`.
کامیت‌های پیشینِ این ممیزی: `fedf27a`, `dbc6aa6`, `d68bee8`, `d1c491e`,
`ab1b08d`, `955a53e`, `0750330`, `be487a8`, `97b88e2`.

## ۱۸) نتیجهٔ Push (Push Result)

`git push origin arena/01a099ed-virtuallife` → موفق. **بدون PR و بدون merge.**

## ۱۹) مسائل باقی‌مانده (Remaining Issues)

- اسکنرِ «بازندهٔ رقابت با `return`» فقط `return`های خالی/`false` را می‌بیند؛
  `return { ... }` را نه (باگ F را اسکنِ دیگری پیدا کرد). اگر اسکنر به ابزاری
  دائمی تبدیل شود باید این حالت هم پوشش داده شود.
- چهار مسیرِ `EventService` اصلاح شد؛ الگوی «نوشتارِ best-effort داخلِ تراکنش»
  می‌تواند در آینده دوباره ظاهر شود — قاعده‌اش به `AGENTS.md` اضافه شد ولی
  تستِ سراسریِ ایستا (lint rule اختصاصی) نوشته نشد.
- تشخیصِ `P2002` همچنان در نُه جا از هشت فایل با الگوی تکراری نوشته شده است
  (helper مشترک = پاک‌سازیِ آینده).
- مهارهای زنده به PostgreSQL واقعی نیاز دارند و عمداً بخشی از `jest` نیستند؛
  `reward-burn-smoke` افزون بر آن حقِ `ALTER TABLE` می‌خواهد (فقط روی
  `_ux_test`).
- بازیابیِ تاریخیِ ضررهای گذشته (سودِ سوختهٔ صندوق، شانس‌های سوخته) یک اقدامِ
  جداست و انجام نشد؛ این دور فقط جلوی تکرار را گرفت.

## ۲۰) موارد بیرون از دامنه (Out-of-Scope Items)

- بازطراحیِ اقتصاد/بانک/مهریه/پنل‌ها؛ افزودنِ قابلیت، کلیدواژه یا callback تازه.
- تغییرِ اسکیما یا مهاجرت؛ تایمر/کرون/صفِ پس‌زمینه.
- بازیابیِ دادهٔ تاریخیِ پول‌های سوخته.
- ساختِ PR یا merge (طبق دستور).

— پایان گزارش دور یازدهم —

---

# 🟢 دور دوازدهم — هم‌خوانیِ اقتصادی: دفتر کل باید بتواند بگوید پول از کجا به کجا رفت (2026-09-13)

دور یازدهم با دو باگِ پولیِ اثبات‌شده و دو مهارِ زنده بسته شد. این دور از همان‌جا
ادامه یافت، ولی با پرسشِ سطحِ بالاتر: نه «این دکمه دوبار پرداخت می‌کند؟» بلکه
«پس از یک روزِ کاملِ بازی، آیا کلِ اقتصاد هنوز جمع است؟». ابزارِ پاسخ داخلِ خودِ
مخزن بود و هیچ‌کس صدایش نمی‌کرد: `src/modules/economy/money-supply.ts`.

## ۱) خلاصهٔ کامل ممیزی (Full Audit Summary)

- `grep` روی همهٔ `src/` نشان داد ماژولِ ممیزیِ پول (`moneySupply`,
  `publicSectorFlow`, `auditBankPool`, `auditLedger`, `reconcilePlayer`) در
  **هیچ مسیرِ اجراییِ محصول** استفاده نمی‌شود و در `tests/economy-audit.test.ts`
  فقط با دادهٔ دست‌ساز آزموده شده. تنها مصرف‌کنندهٔ واقعی‌اش یک ابزارِ
  همراهِ بستهٔ آفلاین است: `offline-deps/scripts/economy-audit.js` (فقط‌خواندنی،
  با کد خروج ۱ برای پایش). یعنی همان ابزاری که برای «چند تومان در بازی هست و
  آیا با دفتر کل می‌خواند؟» نوشته شده بود، هرگز با دادهٔ واقعیِ یک نشستِ بازی
  روبرو نشده بود — و همان‌طور که در باگ K دیده شد، وقتی هم روبرو شد اشتباه
  گزارش می‌داد.
- پس این دور آن را به دادهٔ واقعی برد: یک مهارِ زندهٔ تازه
  (`scripts/economy-invariants-smoke.cjs`) که یک روزِ کاملِ بازی را **فقط از
  مسیرِ سرویس‌های محصول** اجرا می‌کند و بعد با همان توابع ممیزی می‌سنجد.
  نتیجه: چهار نقص (یکی در دفتر کلِ محصول، سه تا در خودِ ماژولِ ممیزی) که بدون
  دادهٔ واقعی دیده نمی‌شدند.
- افزون بر آن، دادهٔ زندهٔ به‌جامانده از شش مهارِ دیگر با یک اسکریپتِ بازرسی
  وارسی شد: شکلِ طرف‌های هر ردیفِ دفتر کل به تفکیکِ نوع، منفی‌بودنِ هر صندوق،
  هم‌خوانیِ وضعیت و زمان‌نگار (سپرده/وام/آگهی/ساعتِ سود)، و رابطهٔ بستهٔ
  ترازنامهٔ بانک. ترازنامهٔ بانک روی دادهٔ زنده **دقیقاً** بسته بود
  (`expected == actual == 994,835,608` پس از شش مهارِ پیاپی).

## ۲) باگ‌های پیداشده (Bugs Found)

1. **I — یک نوعِ دفتر کل برای دو جهتِ متفاوتِ پول.** هم «افتتاح سپردهٔ مدت‌دار»
   (پول از بخش خصوصی بیرون می‌رود و در ترازنامهٔ بانک می‌نشیند؛ منبعِ وام‌ها و
   سودها) و هم «واریز از کیف پول به حسابِ بانکیِ خودِ بازیکن» (پول داخلِ بخش
   خصوصی جابه‌جا می‌شود) با `BANK_DEPOSIT` ثبت می‌شدند. هیچ ممیزی‌ای نمی‌تواند
   این دو را از هم جدا کند: یکی انقباضِ پول است و دیگری هیچ. اثرِ عملی‌اش همین
   جا ظاهر شد — ماژولِ ممیزی مجبور بود حدس بزند و غلط حدس می‌زد.
2. **I۲ — آشتیِ کیف‌پولی، اختلافِ خیالی می‌ساخت.** `reconcilePlayer` موجودیِ
   *کیف پول* را با دفتر کلی می‌سنجید که پولِ *حساب بانکی* را هم شامل می‌شود
   (مثلاً سودِ سپرده به حساب می‌رود نه به کیف). نتیجه: هر بازیکنی که یک بار از
   بانک استفاده کرده باشد «اختلافِ دفتر کل» می‌خورد — یعنی ابزارِ تشخیصِ «پاداش
   بدون ثبت» برای همهٔ بانک‌دارها آلارمِ کاذب می‌داد.
3. **I۳ — ترازِ بخشِ عمومی وارونه شمرده می‌شد.** `publicSectorFlow` واریزِ
   بازیکن به حسابِ خودش را «درآمدِ عمومی» و برداشت از حسابش را «هزینهٔ عمومی»
   می‌شمرد؛ یعنی هرچه بازیکنان بیشتر پولشان را به بانک جابه‌جا می‌کردند، گزارشِ
   «انقباض/تورم» بزرگ‌تر می‌شد، بدونِ آنکه یک ریال بین بخش‌ها رفته باشد.
4. **J — سپردهٔ مدت‌دار دوبار شمرده می‌شد.** `moneySupply.total` جمعِ «ادعای
   بازیکنان» و «صندوق‌های عمومی» است و اصلِ سپردهٔ مدت‌دار در **هر دو** هست
   (چون افتتاحِ سپرده به صندوق بانک واریز می‌کند). پس عددِ «چقدر پول در بازی
   وجود دارد» به ازای هر سپردهٔ فعال، یک‌بار اضافی شمرده می‌شد.
5. **K — ممیزِ آفلاین برای هر بازیکنِ بانک‌دار آلارمِ کاذب می‌داد.**
   `offline-deps/scripts/economy-audit.js` (ابزارِ فقط‌خواندنیِ همراهِ بستهٔ
   آفلاین، با کد خروج ۱ برای Cron/پایش) سه نقص داشت که همه از همان ریشهٔ
   I۲/J می‌آیند: آشتی را با `reconcilePlayer` یعنی فقط *کیف پول* می‌سنجید،
   ستونِ `type` را از دفتر کل نمی‌خواند (پس حتی با تابعِ درست هم جابه‌جاییِ
   داخلی قابلِ شناخت نبود)، و در پاسخ به «چند تومان پول در بازی است» همان
   `total`ی را چاپ می‌کرد که سپردهٔ مدت‌دار را دوبار می‌شمرد. روی دادهٔ زندهٔ
   یک نشستِ سالم، این ابزار یک بازیکن را با **اختلافِ خیالیِ ۵٬۰۰۰٬۰۰۰** ریالی
   (دقیقاً مبلغِ واریزِ بانکی‌اش) خطا می‌داد و کد خروج ۱ برمی‌گرداند — یعنی
   پایشِ استقرار بی‌دلیل قرمز می‌شد.
6. **مشاهده (باگ نیست، ثبت شد):** دو ردیفِ دفتر کلِ «بی‌طرف» در دادهٔ زنده پیدا
   شد: یکی تسویهٔ نهاییِ پیش از حذفِ حساب توسط ادمین (`WITHDRAWAL`) که چون ردیفِ
   بازیکن پاک می‌شود، عمداً بی‌طرف است، و دیگری یک سندِ ساختگیِ خودِ اسکریپتِ
   آزمونِ ادمین («سند آزمایش»). هر دو در گزارشِ ممیزی به‌عنوانِ استثنا مستند
   شدند، نه به‌عنوانِ نقص.

## ۳) ریشهٔ علل (Root Cause)

- نبودِ یک «منبعِ حقیقت» برای اینکه هر ردیفِ دفتر کل پول را بین کدام دو بخش
  جابه‌جا کرده. قاعدهٔ خودِ ماژول درست بود («جهت از طرف‌ها فهمیده می‌شود نه از
  نوع») ولی یک *نوع* برای دو جریانِ متفاوتِ بخش‌ها استفاده شده بود، پس طرف‌ها
  دیگر کافی نبودند.
- آزموده‌نشدنِ ابزار با دادهٔ واقعی: هر سه نقصِ ماژول (I۲، I۳، J) با فیکسچرِ
  کوچکِ تک‌ردیفی دیده نمی‌شوند؛ فقط وقتی دیده می‌شوند که یک بازیکنِ واقعی هم
  حساب بانکی داشته باشد، هم سپرده، هم سود.
- صورت‌حسابِ کیف پول جهتِ ردیف را از طرفش می‌فهمد
  (`destinationPlayerId === playerId ? 'in' : 'out'`)؛ پس ثبتِ دوطرفه برای
  «واریز به حسابِ خودم» ممکن نبود (واریز را «درآمد» نشان می‌داد). یعنی راه‌حلِ
  «ردیف را دوطرفه کن» از قبل بسته بود و تنها راه، تفکیکِ نوع بود.

## ۴) اصلاحات (Fixes)

- `src/modules/banking/deposit.service.ts`: افتتاحِ سپردهٔ مدت‌دار حالا
  `TransactionType.DEPOSIT` می‌نویسد (مقداری که در enum وجود داشت و در هیچ جای
  `src/` استفاده نمی‌شد) با کامنتی که مرزِ دو نوع را توضیح می‌دهد.
- `src/modules/finance/ledger.service.ts`: برچسبِ `DEPOSIT` از «واریز»ِ مبهم به
  **«سپردهٔ مدت‌دار»** تغییر کرد — همان واژه‌ای که راهنمای بات از قبل برای این
  قابلیت به کار می‌برد؛ `BANK_DEPOSIT` همان «واریز بانکی» ماند.
- `src/modules/economy/money-supply.ts`:
  - `INTERNAL_PRIVATE_TYPES` و `isInternalPrivateMove()` — تشخیصِ جابه‌جاییِ
    داخلیِ بخش خصوصی، فقط وقتی یک طرفِ بازیکن هم داشته باشد (ردیفِ بی‌طرف همان
    ناهنجاریِ همیشگی می‌ماند)؛
  - `reconcilePrivateSector()` و `PrivateHoldings` — آشتیِ «کیف پول + حساب‌های
    بانکی» با دفتر کلِ بدونِ ردیف‌های داخلی؛
  - `auditLedger()` حالا `bankBalances` اختیاری می‌گیرد و با آشتیِ بخشِ خصوصی
    کار می‌کند (بدونِ آن، دقیقاً رفتارِ قبلی)؛
  - `publicSectorFlow()` ردیف‌های داخلی را نمی‌شمارد؛
  - `moneyInExistence()` — هم‌پوشانیِ سپردهٔ مدت‌دار با ترازنامهٔ بانک را یک‌بار
    می‌شمارد تا پرسشِ «چقدر پول وجود دارد» درست جواب داده شود؛
  - مستنداتِ `lockedDeposits` و مرزِ `DEPOSIT`/`BANK_DEPOSIT` صریح نوشته شد.
- `offline-deps/scripts/economy-audit.js`: آشتی با `reconcilePrivateSector` و
  موجودیِ حسابِ بانکیِ هر بازیکن؛ ستونِ `type` به پرسشِ دفتر کل اضافه شد؛
  «پولِ موجود» با `moneyInExistence` چاپ می‌شود (و `total` با برچسبِ «جمع ادعا و
  صندوق»); به‌علاوه یک خودآزمایی که اگر ردیف‌های دفتر کل بدونِ `type` خوانده
  شوند، به‌جای گزارشِ اختلافِ خیالی با کد خروج ۱ و پیامِ صریح متوقف می‌شود.
  هر دو جهت روی دادهٔ زنده آزموده شد: روی نشستِ سالم «هر ۳ بازیکن با دفتر کل
  می‌خواند» و کد خروج ۰؛ روی دیتابیسی که موجودی‌ها با دست کاشته شده بودند
  (نشستِ مهارِ رقابت) دقیقاً همان سه بازیکنِ دست‌کاشته با مبلغِ درست گزارش
  شدند و کد خروج ۱.
- `scripts/economy-invariants-smoke.cjs` (تازه): ممیزیِ زندهٔ هم‌خوانیِ اقتصاد.
- `scripts/economy-race-smoke.cjs` و `scripts/reward-burn-smoke.cjs`: صندوق بانک
  حالا از مسیرِ واقعیِ `pool.creditDeposit` نقد می‌شود نه با نوشتنِ مستقیمِ
  `balance`؛ وگرنه رابطهٔ بستهٔ ترازنامه روی همان دیتابیسِ آزمایشی نقض می‌شد و
  ممیزِ تازه نمی‌توانست سخت‌گیر باشد.

### مهارِ تازه چه چیزی را مطالبه می‌کند

یک روزِ کامل فقط از مسیرِ سرویس‌ها: بودجه از `admin.adjustBalance` (تا خودش ردیف
دفتر کل بگذارد)، افتتاح حساب و واریز بانکی، پنج روز سودِ حساب (با عقب‌بردنِ
ساعتِ سود)، عضویتِ باشگاه، پاداشِ حضور، شانسِ روزانه، خرید از فروشگاه، ثبت و
خریدِ آگهی بین دو بازیکن، و افتتاح و سررسیدِ سپردهٔ مدت‌دار. سپس:

1. ترازنامهٔ بانک بسته باشد (`auditBankPool.ok` با `BANK_POOL_SEED`)؛
2. داراییِ خصوصیِ هر بازیکن (کیف + حساب بانکی) **تا ریال** با خالصِ دفتر کلِ او
   برابر باشد؛
3. شمارندهٔ `totalDeposited` صندوق دقیقاً به اندازهٔ ردیف‌های `DEPOSIT`ِ جلسه
   بالا رفته باشد، و «هرچه بانک پرداخت کرد» (`totalDisbursed` +
   `totalDepositInterest`) دقیقاً برابرِ ردیف‌های `DEPOSIT_PAYOUT` و
   `DEPOSIT_INTEREST` و `LOAN_DISBURSEMENT` باشد؛
4. هیچ ردیفِ بی‌طرفی در جلسه نباشد؛
5. **هیچ صندوقی در کل دیتابیس منفی نباشد** (کیف بازیکن، حساب بانکی، ترازنامهٔ
   بانک و هر شش شمارنده‌اش، خزانهٔ کسب‌وکار، ماندهٔ وام، حقوقِ معوق، انبارها،
   پروژه‌های منطقه، اصلِ سپرده، صندوق‌های هفتگی، و مبلغِ هر ردیف دفتر کل)؛
6. وضعیت‌ها با زمان‌نگارهایشان نخوانند (سپردهٔ تسویه‌شده `payout` و `settledAt`
   دارد، وامِ تسویه‌شده ماندهٔ صفر، آگهیِ فروخته‌شده `soldAt`، ساعتِ سود در
   آینده نباشد، ازدواجِ ثبت‌شده مهریهٔ بزرگ‌تر از صفر)؛
7. آنچه بخش خصوصی در دست دارد دقیقاً همان باشد که از بخش عمومی بیرون آمده
   (`heldBySession == spending − income`) — یعنی پولی از هیچ پیدا نشده و به هیچ
   گم نشده.

## ۵) ازدواج و مهریه (Marriage / Mahr Changes)

بدون تغییرِ کد؛ `scripts/marriage-live-smoke.cjs` دوباره سبز شد و ممیزِ تازه هم
«مهریهٔ بزرگ‌تر از صفر برای هر ازدواجِ ثبت‌شده» را روی کل دیتابیس مطالبه می‌کند.

## ۶) ثبت گروه و سطح محیط (Group Registration Changes)

بدون تغییرِ کد؛ `scripts/gameplay-live-smoke.cjs` دوباره سبز شد.

## ۷) پنل ادمین (Admin Panel Changes)

بدون تغییرِ رفتار. تنها تماسِ این دور با ادمین، استفاده از مسیرِ واقعیِ
`admin.adjustBalance` برای بودجه‌دادن به بازیکنانِ آزمون است (به‌جای نوشتنِ
مستقیمِ `balance`) تا هر ریالِ موجودی یک ردیفِ دفتر کل پشتش باشد. هر دو smoke
ادمین سبز ماندند.

## ۸) اصلاحات امنیتی (Security Fixes)

این دور مستقیماً یک رخنهٔ دسترسی نبست؛ آنچه بست **از بین رفتنِ راهِ پنهان‌شدنِ
پول** بود:

- هیچ جریانِ مالی نمی‌تواند پشتِ یک نوعِ مبهم پنهان شود (سپردهٔ مدت‌دار دیگر مثلِ
  جابه‌جاییِ داخلی به نظر نمی‌رسد).
- ممیزیِ «آیا پولی از هیچ ساخته شد؟» حالا روی دادهٔ واقعی قابلِ اجراست و در
  مهارِ زنده به‌صورتِ خودکار مطالبه می‌شود (بند ۲ و ۷ بالا).
- منفی‌شدنِ هر صندوقی در کل دیتابیس — از جمله شش شمارندهٔ ترازنامهٔ بانک —
  یک ناهنجاریِ سخت است و در هر اجرا بررسی می‌شود.

## ۹) تجربهٔ کاربری تلگرام (Telegram UX Changes)

- یک برچسبِ دفتر مالی دقیق‌تر شد: ردیفِ افتتاحِ سپردهٔ مدت‌دار به‌جای «واریز»
  حالا «سپردهٔ مدت‌دار» می‌گوید (همان واژهٔ راهنما). ردیفِ واریز به حساب بانکی
  همان «واریز بانکی» است.
- شمارِ کلِ متن‌های بازیکن **۵۱۷۴** ماند (یک رشته جایگزین شد، نه اضافه)؛
  ۲۰۶ مسیر و ۱۵۶ کلیدواژه بدون تغییر؛ فهرست بازتولید شد.

## ۱۰) کلیدواژه‌ها و callbackها (Keyword / Callback Changes)

بدون تغییر.

## ۱۱) دیتابیس و مهاجرت (Database / Migration Changes)

**هیچ تغییرِ اسکیما و هیچ مهاجرتی لازم نشد** — کلیدِ اصلاح این بود که مقدارِ
`DEPOSIT` از قبل در `enum TransactionType` وجود داشت و در `src/` استفاده
نمی‌شد؛ پس فقط معنا و مصرف عوض شد. شمارِ مهاجرت‌ها همان **۲۳** و drift صفر است.
ردیف‌های تاریخیِ سپردهٔ مدت‌دار با نوعِ قدیمی می‌مانند و عمداً backfill نشد
(نگاه کنید به «باقی‌مانده»).

## ۱۲) آزمون‌های اضافه/به‌روزشده (Tests Added / Updated)

`tests/economy-audit.test.ts` (+۶، از ۱۵ به ۲۱):

- واریز/برداشتِ بانکیِ خودِ بازیکن جریانِ عمومی نیست (درآمد، هزینه و شمارِ
  mint/burn دست‌نخورده می‌مانند)؛
- جابه‌جاییِ داخلی فقط وقتی شناخته می‌شود که یک طرفِ بازیکن داشته باشد (ردیفِ
  بی‌طرف هنوز ناهنجاری است)؛
- بازیکنِ بانک‌دار با «داراییِ خصوصی» تا ریال آشتی می‌شود؛
- همان داده با آشتیِ کیف‌پولیِ قدیمی اختلافِ خیالی می‌دهد (مستندسازیِ دلیلِ
  تغییر، با حسابِ دقیقِ `ledgerNet = 205,000` و `difference = −5,000`)؛
- `auditLedger` وقتی `bankBalances` داده شود اختلافِ کاذب نمی‌دهد و وقتی نشود
  همان رفتارِ قبلی را دارد؛
- سپردهٔ مدت‌دار یک‌بار شمرده می‌شود (`total` در برابر `moneyInExistence`، و
  حالتِ صندوقِ نازک‌تر از سپرده‌ها).

## ۱۳) نتیجهٔ آزمون (Test Result)

`npx jest` → **۶۸ سوئیت / ۹۹۵ آزمون، همه سبز** (پایهٔ دور یازدهم ۶۸/۹۸۹ بود؛
+۶ آزمون).

## ۱۴) نتیجهٔ ESLint (ESLint Result)

`npx eslint src tests` → پاک.

## ۱۵) نتیجهٔ Build (Build Result)

`tsc` پاک (شاملِ کامپایلِ `prisma/seed.ts` برای بستهٔ آفلاین).

## ۱۶) مصنوعات آفلاین (Offline Artifacts)

- `offline-deps/dist.tar.gz`: **۶۳۱٬۷۴۴ بایت** (بازسازی شد).
- `offline-deps/node_modules-linux.tar.gz`: **۱۸٬۸۴۹٬۳۱۵ بایت — بدون تغییر**
  (نه اسکیما عوض شد، نه وابستگی‌ها؛ چون `DEPOSIT` از قبل در enum بود).
- `offline-deps/README.md`: مهارِ تازه به فهرستِ راستی‌آزمایی اضافه شد و توضیح
  داده شد که هر سه مهارِ پولی چه پرسشی را جواب می‌دهند؛ ممیزِ آفلاینِ
  `economy-audit.js` هم که تاکنون در هیچ فهرستی نبود، با کارکرد و کد خروجش
  مستند شد.
- راستی‌آزمایی زنده پس از بازسازی، هر هشت مورد سبز:
  `economy-invariants-smoke`, `economy-race-smoke`, `reward-burn-smoke`,
  `marriage-live-smoke`, `gameplay-live-smoke`, `admin-moderation-smoke`,
  `admin-ux-smoke`, `offline-bundle-verify`.

### خروجیِ واقعیِ ممیزِ هم‌خوانی (یکی از اجراها)

```
session ledger: 13 rows — mint 7 / burn 4, public net -58047946
money supply: wallets 53037671, bank accounts 5010275, locked deposits 0,
              region funds 0, bank pool 499982054
claims plus vaults 558030000; money actually in existence 558030000
daily fortune rolled "loss" for 45000; account interest paid 10275
```

`public net` منفی است یعنی بخش عمومی دقیقاً ۵۸٬۰۴۷٬۹۴۶ تومان به بخش خصوصی
داده، و جمعِ «کیف پول‌ها + حساب‌های بانکی» همان عدد است — تا ریال.

## ۱۷) هش کامیت (Commit Hash)

`c77b0b3` (کد، تست‌ها، مهارِ تازه، بستهٔ `dist`، `AGENTS.md` و
`offline-deps/README.md`) و کامیتِ مستنداتی که همین گزارشِ دور دوازدهم را دارد —
روی شاخهٔ `arena/01a099ed-virtuallife`.

## ۱۸) نتیجهٔ Push (Push Result)

`git push origin arena/01a099ed-virtuallife` → موفق. **بدون PR و بدون merge.**

## ۱۹) مسائل باقی‌مانده (Remaining Issues)

- **ردیف‌های تاریخیِ سپردهٔ مدت‌دار** با نوعِ قدیمی (`BANK_DEPOSIT`) می‌مانند؛
  ممیزیِ دادهٔ *پیش از این اصلاح* نمی‌تواند آن‌ها را از واریز به حسابِ خودِ
  بازیکن جدا کند. backfill عمداً انجام نشد چون تنها راهش تطبیقِ متنِ
  `reference` («افتتاح سپردهٔ …») است و ممیزیِ مالی نباید روی متن تکیه کند.
- `money-supply.ts` هنوز در پنل ادمین دیده نمی‌شود؛ مصرف‌کننده‌هایش ممیزِ
  آفلاین و مهارِ زندهٔ هم‌خوانی‌اند. ساختنِ داشبوردِ اقتصادیِ ادمین قابلیتِ
  تازه است و بیرون از دامنهٔ «بدون feature creep» ماند.
- اسکنرهای الگو (دور دهم و یازدهم) هنوز اسکریپتِ دائمیِ مخزن نشده‌اند؛ اسکنرِ
  «بازندهٔ رقابت» هم `return { ... }` را نمی‌بیند.
- تشخیصِ `P2002` همچنان در نُه جا از هشت فایل تکراری است.
- مهارِ هم‌خوانی برای درستیِ بندِ «داراییِ خصوصی = خروجیِ خالصِ بخش عمومی» فرض
  می‌کند بازیکنانِ جلسه از صفر شروع می‌کنند (که خودش تضمین می‌کند)؛ اگر روزی
  بازیکنِ موجودِ دیتابیس را هم ممیزی کند باید موجودیِ آغازین را از این تفریق کم
  کرد.

## ۲۰) موارد بیرون از دامنه (Out-of-Scope Items)

- backfill کردنِ ردیف‌های تاریخی دفتر کل؛ افزودنِ داشبوردِ اقتصادی به پنل ادمین.
- تغییرِ اسکیما یا مهاجرت؛ تایمر/کرون/صفِ پس‌زمینه.
- بازطراحیِ صورت‌حسابِ کیف پول (جهتِ ردیف‌ها عمداً از طرف‌ها خوانده می‌شود و
  دست نخورد).
- ساختِ PR یا merge (طبق دستور).

— پایان گزارش دور دوازدهم —

---

## یادداشتِ پایانیِ شاخه — Pull Request #19

به درخواستِ کاربر، پس از پایانِ دور دوازدهم کلِ کارِ این شاخه به‌صورتِ
**Pull Request #19** (`main` ← `arena/01a099ed-virtuallife`) باز شد:
<https://github.com/aminatkh0-ship-it/VirtualLife/pull/19> — ۱۴ کامیت، ۵۲ فایل
(+۱۲٬۴۲۲/−۳٬۹۸۷) و دو مهاجرت.

بندِ «بدون PR» در گزارشِ دورهای پیشین به دستورِ زمانیِ همان دورها برمی‌گردد و
از این نقطه به بعد معتبر نیست؛ **merge انجام نشد** و تصمیمِ ادغام با نگهبانِ
مخزن است.

همراه با آماده‌سازیِ PR یک باگِ دیگر هم پیدا و اصلاح شد (بند K در دور دوازدهم):
ابزارِ فقط‌خواندنیِ `offline-deps/scripts/economy-audit.js` که با بستهٔ آفلاین
منتشر می‌شود و برای پایش کد خروج ۱ می‌دهد، روی یک نشستِ سالم یک بازیکن را با
اختلافِ خیالیِ ۵٬۰۰۰٬۰۰۰ ریال خطا می‌داد (کامیت `aac91b4`).

وضعیتِ راستی‌آزمایی در لحظهٔ باز شدنِ PR: `tsc` پاک، `eslint src tests` پاک،
**۶۸ سوئیت / ۹۹۵ آزمون سبز**، و هر هشت راستی‌آزماییِ زنده سبز
(`economy-invariants`, `economy-race`, `reward-burn`, `marriage-live`,
`gameplay-live`, `admin-moderation`, `admin-ux`, `offline-bundle-verify`).
