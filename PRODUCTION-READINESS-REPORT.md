# گزارش آمادگی عملیاتی — ربات تلگرام «میراث» (Legacy Game)

تاریخ: ۱۴۰۵-۰۶-۲۳ (2026-09-14) · شاخه: `arena/01a09f3f-virtuallife` · Agent Mode (Arena.ai)

> این سند گزارش «عملیاتی‌سازی کامل» است: ممیزی‌های پیشین (AUDIT-REPORT.md و
> DEEP-RECONSTRUCTION-REPORT.md) مشکلات را پیدا کردند؛ این دوره آن‌ها را تا ریشه
> رفع کرد، با تست بازگشتی و زنجیرهٔ ساخت آفلاینِ قابل اثبات.

## وضعیت نهایی (همه سبز)

| بررسی | نتیجه |
|---|---|
| `tsc` (build کامل: prisma generate آفلاین + tsc) | ✅ پاک |
| `eslint src tests --max-warnings=0` | ✅ ۰ اخطار |
| `jest` | ✅ **۷۰ سوئیت / ۱٬۰۳۴ تست، همه سبز** |
| مهاجرت‌ها روی دیتابیس خالی (offline migrate.js) | ✅ ۲۴ مهاجرت، بدون خطا |
| economy-invariants-smoke (PostgreSQL زنده) | ✅ بیلانس بانکی بسته، آشتی دفتر کل ریال‌به‌ریال |
| economy-race-smoke (PostgreSQL زنده، burst کلیک) | ✅ هر claim دقیقاً یک‌بار پرداخت می‌شود |
| offline-bundle-verify (بستهٔ شسته‌شده در /tmp) | ✅ جریان واقعی moderation/erasure روی DB زنده، بدون اینترنت |
| زنجیرهٔ Source→Generate→Compile→dist→Archive→Install→Runtime | ✅ به‌تفصیل در بخش «ساخت آفلاین» |

---

## ۱) Fixed — باگ‌های پیدا و اصلاح‌شده (ریشه، نه سطح)

### ۱.۱ حراجی شهر: خرید آیتم «از خود برای خود» (بحرانی مالی)
`placeBid` فقط روی دکمهٔ UI تکیه داشت؛ رهبرِ فعلی می‌توانست با ۵٪ اختلاف، آیتمِ
اسطوره‌ای را **از خودش برای خودش** بخرد (آیتم از هیچ + پول کمتر).
- **اصلاح:** داخل تراکنش، روی رکورد تازهٔ بازمش‌دیدشده `fresh.highestBidderId === bidder.id` → `ConflictError('Self bid')`. حداقلِ پیشنهاد هم از `fresh.currentBid` حساب می‌شود (نه دادهٔ کهنهٔ قبل از تراکنش).
- **نمای پنل:** `getView(groupId, viewerTelegramUserId)` حالا `amILeader` درست می‌سازد و دکمهٔ «پیشنهاد» برای رهبر بسته می‌ماند.

### ۱.۲ قرض بازیکنی: نکول = پول رایگان (بحرانی اقتصادی)
قرض ۲۰ میلیونی بگیر، سررسید نده، پول را برای همیشه داشته باش — و وام‌دهنده
چیزی پس نگیرد؛ فقط یک وضعیت `DEFAULTED` در دیتابیس.
- **اصلاح (`settleDueLoans`/`markDefaulted`):** نکول در تراکنش: ثبت وضعیت →
  **وصول مجبورانه** تا سقف `totalRepay` از موجودی وام‌گیرنده؛ اول سهم ۱۰۸٪ِ
  وام‌دهنده، باقی کارمزد سیستم؛ هر حرکت، ردیف دفتر کل. اگر تسویهٔ همزمان برد،
  قفل CAS می‌شکند و **هیچ وصولی نمی‌شود** (نکول روی قراردادِ تسویه‌شده ثبت نمی‌شود).
- **جریمهٔ اعتباری:** `CreditService` حالا هر نکول = −۵۰ امتیاز (سقف −۲۰۰) — نکول هزینه دارد، نه پاداش.

### ۱.۳ پنل‌ها می‌مردند: خطای Markdown parser (بزرگ‌ترین باگ UX)
متنِ پویا (نامِ کاربر تلگرام با `*`، `_`، `[`) کل پیام را `can't parse entities`
می‌کرد و خطا فقط لاگ می‌شد — **کاربر هیچ پنلی نمی‌دید**.
- **اصلاح (`src/bot/panel.ts`):** `isParseError` (GrammyError ۴۰۰ + "can't parse") →
  همان ویرایش/ارسال **دوباره بدون `parse_mode`**؛ اگر ویرایش ساده هم شکست
  (مثلاً پیام حذف شده) → fallback به `reply` با همان قالبِ سالم؛ دفاع آخر:
  `reply` کاملاً بی‌قالب‌بندی. پنل دیگر هرگز از بین نمی‌رود. `isIgnorableEditError`
  هم محافظ `description ?? ''` دارد (بدنهٔ خطا بدون description، خودِ مدیریت خطا را نمی‌ترکاند).

### ۱.۴ middleware پنل: callbackِ گیرکردهٔ تلگرام
دکمهٔ `adm:*` با `ctx.from`ِ ناموجود (حساب حذف‌شده / کانال) → `TypeError`
→ callback هرگز ack نمی‌شد → اسپینر تلگرام تا ابد.
- **اصلاح:** `adm:` بدون actor → ack بدون پاسخ و بازگشت (نه خطا، نه افشای وضعیت).

### ۱.۵ دو رأیِ همزمان = دو انتخابات OPEN در یک گروه
مسابقهٔ ساخت election، هر دو `create` می‌شدند؛ `findFirst` قبل، هر دو را «هیچ» می‌دید.
- **اصلاح:** مهاجرت جدید + `getOrCreateOpenElection` با try/catch روی **P2002** →
  refetch انتخاباتِ موجود. (جایزهٔ امنیتی رایگان: یک index partial unique.)

### ۱.۶ کمک به پروژهٔ منطقه: پولِ بین خواندن و نوشتن
کسر از کیف **بیرون** از تراکنش بود؛ اگر وضعیت پروژه در میانه عوض می‌شد،
«جبران دستی» (refundِ جدا) اجرا می‌شد — و اگر refund خود خطا می‌داد،
پول اهداکننده برای همیشه از دست می‌رفت.
- **اصلاح:** کسر + افزایش صندوق + ردیف دفتر کل در **یک تراکنش**؛ بازندهٔ رقابت
  `throw` می‌کند و خودِ rollback پول را برمی‌گرداند. (تست‌های expansion هم
  بازنویسی شدند تا rollback واقعی را بستانند، نه جبران دستی.)

### ۱.۷ دو برابرزدن «پایان استراحت» = دو برابر ریکاوری
`finishRest` با `update` بی‌شرط می‌نوشت؛ هر کلیکِ اضافه، خستگی/سلامت را دوبار
جبران می‌کرد.
- **اصلاح:** `updateMany` شرطی روی `{activityState: RESTING, restStartedAt: {not: null}}` + `count === 1`؛ دومی صفر ردیف.
  (در همین تابع، clampِ سقفِ سلامت هم از ۱۰۰ ثابت به `resolveMaxHealth` — عضو باشگاه ۱۲۰ — اصلاح شد تا استراحت، سلامتِ او را «کاهش» نکند.)

### ۱.۸ آگهیِ شغلِ کسب‌وکار: دوبارزدن = دو آگهیِ یکسان
`postJob` بدون کلیدِ ضدتکرار بود؛ دو کلیکِ همزمانِ «ذخیره»، پیش‌نویس را دو بار
می‌خواندند و **دو آگهی** ثبت می‌شدند.
- **اصلاح:** `userStateRepository.claimContext` — ادعای اتمیک
  (`deleteMany` دقیق روی context + pendingSince، `count === 1`) در تراکنشِ
  ثبت؛ بازندهٔ رقابت پنل صادقانهٔ «در حال ثبت» می‌بیند. خطای ثبت، پیش‌نویس را
  برگردان می‌گذارد تا جریان ادامه یابد.

### ۱.۹ بیمهٔ کلینیک: تمدید از پایهٔ کهنه (race)
پایانِ بیمهٔ فعلی **قبل** از تراکنش خوانده می‌شد؛ خریدِ همزمان می‌توانست تمدید
را از پایهٔ قدیمی حساب کند (پوششِ از هیچ / کوتاه‌تر از آنچه پرداخت شد).
- **اصلاح:** همان الگوی «بازخوانی بعد از کسر در تراکنش» (سریالیزه با قفلِ
  conditional updateMany روی سطر بازیکن) — پایهٔ تمدید از وضعیتِ تازه است.

### ۱.۱۰ بذرِ فروشگاه، موجودیِ محدود را صفر می‌کرد
`upsert`ِ seeding در شاخهٔ `update`، `stock` را دوبار می‌نوشت — هر بار seed،
موجودیِ آیتم‌های محدود به مقدار اولیهٔ بذر برمی‌گشت (فروشِ قبلیِ آن‌ها بی‌اثر می‌شد).
- **اصلاح:** `stock` فقط در شاخهٔ `create`.

### ۱.۱۱ رفرش منطقه، صندوق منطقه (taxRevenue) را بازنویسی می‌کرد
`persist()` در شاخهٔ `update`، `taxRevenue` را با مقدارِ محاسبهٔ لحظه‌ای
جایگزین می‌کرد — صندوقی که بازیکن‌ها به آن کمک می‌کردند، با هر رفرش، با
مقدارِ محاسبه‌ایِ دیگری جابه‌جا می‌شد.
- **اصلاح:** `taxRevenue` فقط در شاخهٔ `create`؛ در `update` هرگز لمس نمی‌شود.

### ۱.۱۲ پاداشِ «هر سه» مأموریتِ روزانه: قابلِ دوباربردن بود
سه claimِ همزمان می‌توانستند پاداشِ تکمیل را دو بار بپردازند.
- **اصلاح:** مارکر `ALL_THREE_BONUS` با یکتا بودن؛ P2002 → پاداشِ تکمیل رد
  می‌شود ولی پاداشِ همان کارتِ سوم پرداخت می‌شود (بازیکن ضرر نمی‌کند، سیستم
  دوبار نمی‌پردازد).

### ۱.۱۳ رویدادِ اشتباهِ «هدیهٔ روزانهٔ حیوان»
هدیهٔ روزانه با `GameEventType.PET_ADOPTED` ثبت می‌شد — آمار و اخبار
**فرزندپذیری** را آلوده می‌کرد. → `PET_FED` (رویدادِ درستِ این مسیر).

### ۱.۱۴ دکمهٔ خرید سهام با تعدادِ نامعتبر: ackِ بی‌صدا
`qty` نامعتبر → `return` بی‌ack → اسپینر تلگرام تا ابد. → ack با پیامِ
واضح («تعداد سهم عدد صحیح و مثبت باشد»).

### ۱.۱۵ شات‌داونِ نرمِ ربات
`app.ts`: تایمرها پاک → `bot.stop()` (در try/catch تا خطای توقف سر راه
پاک شدن DB نماند) → `prisma.$disconnect()`.

### ۱.۱۶ باگِ مستندات
`life-core.ts`: ضریبِ سقف ۲٫۹۶ در کامنت بود ولی واقعی `MAX_WORK_MULTIPLIER`
(~۲٫۸۷) است — پنل حالا عددی نشان می‌دهد که با تابعِ واقعی یکی است.

---

## ۲) Improved — تقویت‌ها (رفتارِ درست، نه پچ)

- **پنلِ «در حال ثبت» صادقانه** در همهٔ مسابقاتِ claim (آگهی شغل، مأموریت، …):
  بازندهٔ رقابت به‌جای تکرارِ عمل یا crash، وضعیتِ واقعی را می‌بیند.
- **کاهش وابستگی به JSDoc:** همهٔ مسابقاتِ مالی بر `count !== 1 → throw` داخل
  تراکنس‌ها تکیه دارند؛ هیچ «refund دستی» جداگانه‌ای در مسیرِ پول نیست.
- **ack-before-work** در پاک‌سازی state: حتی اگر پاک‌سازی خطا دهد، اسپینر
  روشن نمی‌ماند.
- ۱۱ شکستِ mockِ قدیمیٔ تست‌ها ریشه‌یابی و اصلاح شد (سوئیت‌های
  finance / daily-loop / employment / expansion / health-cap / region-buffs):
  `mockResolvedValue(undefined)` روی `findUniqueOrThrow` (که خودش throw می‌کند و
  `undefined` نمی‌گردد)، `$transaction`ِ عبوری، `playerLoan.count` — پوشش ضعیف‌تر
  نشد، فقط mock‌ها با واقعیتِ API هماهنگ شدند.

## ۳) Telegram UX

- **هیچ پنلی دیگر از بین نمی‌رود** (بخش ۱.۳): متن‌های پویاِ شکسته، بدون
  قالب‌بندی همان‌جا ویرایش/ارسال می‌شوند — کاربر پنلِ سالم را می‌بیند.
- دکمهٔ «پیشنهاد» حراجی، برای رهبرِ فعلی بسته است (۱.۱).
- هیچ callbackی دیگر اسپینرِ تلگرام را گیر نمی‌اندازد: actorِ ناموجود (۱.۴)،
  تعدادِ نامعتبر سهام (۱.۱۴)، پاک‌سازی state (۲).
- پیامِ خطای مسابقات، صادقانه است: «وضعیت پروژه تغییر کرده است؛ دوباره
  تلاش کن» (دیگر ادعای «مبلغت برگردانده شد» بدون تراکنش نیست — ۱.۶).

## ۴) Database

- **مهاجرت جدید** `20260914010000_one_open_election_per_group` (idempotent، سه گام):
  ۱) ددوپِ انتخابات‌های OPENِ تکراری در هر گروه — قدیمی‌ترین می‌ماند، بقیه
  بسته می‌شوند و **سودِ ۳۰۰۰۰۰ِ** داوطلبانِ ردشده + `players.balance`
  اصلاح می‌شود؛ ۲) index partial unique روی `(group_id) WHERE status='OPEN'`.
  روی دیتابیسِ با دابلِ موجود هم idempotent است (با خودِ شرطِ WHERE).
- کل زنجیرهٔ ۲۴ مهاجرت روی دیتابیسِ خالی با `offline-deps/scripts/migrate.js`
  (بدون موتور باینری Prisma) اعمال و تأیید شد.
- کپسول‌های مالی: تمام مسیرهای نوشته‌ی پول، `updateMany` شرطی + `count === 1`
  داخل `$transaction` هستند؛ دفتر کل و کیف در یک تراکنس‌اند (آشتیِ ریال‌به‌ریال
  در smoke تأیید شد).

## ۵) ساخت آفلاین (CRITICAL — زنجیرهٔ کامل، اثبات‌شده)

| مرحله | ابزار | نتیجه |
|---|---|---|
| Source | ۲۵ فایلِ اصلاح‌شده | — |
| Generate | `prisma generate` با `PRISMA_SCHEMA_ENGINE_BINARY` از `prisma-engines/` (موتور در خودِ مخزن) | ✅ بدون دانلود |
| Compile | `tsc` + کامپایل seed | ✅ |
| Archive | `offline-deps/packaging.sh` (npm ci تازه → prune production → sanity `require` → tar) | ✅ |
| Artifact | `dist.tar.gz` **643٬565 B** · `node_modules-linux.tar.gz` **18٬844٬108 B** | ✅ |
| Install | `offline-bundle-verify.cjs` — بسته در `/tmp` استخراج شد | ✅ |
| Runtime | جریان واقعی moderation + erasure روی PostgreSQL زنده، **بدون دسترسی به شبکه** | ✅ PASS |

- sanity check بسته: `require('@prisma/client')` و … در node_modulesِ
  production-only (بدون prisma CLI/typescript، بدون موتورهای WASM غیر-postgres،
  بدون هیچ `.so.node`) — سرور مقصد **هیچ‌وقت** به اینترنت نیاز ندارد.
- وجودِ کدهای جدید در آرشیو تأیید شد (مارکرهای parse-fallback، clawback،
  `amILeader`، `getOrCreateOpenElection` در `dist.tar.gz`).
- `install.sh` بدون تغییر، همان تاربَال‌ها را مصرف می‌کند (نسخه‌های جدید
  جایگزینِ دقیقِ قبلی‌اند؛ ساختار باندل دست نخورده است).

## ۶) Tests

- **۷۰ سوئیت / ۱٬۰۳۴ تست — همه سبز** (قبلاً: ۶۹ سوئیت / ۱٬۰۰۵ تست که ۱۱ مورد
  در ۶ سوئیت شکست داشت؛ حالا ۰).
- **سوئیت جدید `tests/production-hardening.test.ts` (۲۸ تست بازگشتی)** —
  هر یک از اصلاحاتِ بالا قفل شد: self-bid (سرور + نمای amILeader)، clawback
  نکول (کل/سقفِ موجودی/تقسیمِ کارمزد/کیفِ خالی/رقابتِ تسویه/تسویهٔ خودکار)،
  جریمهٔ اعتباریِ نکول، مارکرِ پاداشِ هر-سه + P2002، تمدید بیمه از پایهٔ
  تراکنشی، بذرِ بدون stock، رفرشِ بدون taxRevenue، P2002ِ انتخابات،
  parse-fallbackِ پنل (۴ سناریو)، guardِ adm: بدون actor (۴ سناریو)،
  حراجیِ بسته، viewerِ ثبت‌نشده.
- ESLint: `src` + `tests` با `--max-warnings=0` پاک.

## ۷) Remaining — باقی‌مانده (صادقانه)

1. **پوششِ واقعیِ Telegram API:** fallbackِ parse در برابر `GrammyError`ِ
   شبیه‌سازی‌شده (با همان رشتهٔ `can't parse entities` که تلگرام می‌فرستد)
   تست شده است؛ تست زندهٔ token واقعی از این سنده ممکن نبود (سبک‌سازی:
   `isParseError` فقط ۴۰۰+parse را می‌شناسد — بقیهٔ خطاها همان رفتارِ
   پیشینِ log دارند).
2. **وصولِ نکول best-effort است:** اگر وام‌گیرنده قبل از سررسید کیفش را خالی
   کند، وام‌دهنده ضرر می‌کند (بازیکن جریمهٔ اعتباری می‌گیرد؛ انتخابِ
   تعادلِ بازی است، نه باگ — طراحیِ «اعتبار اجتماعی، نه موجودی منفی»).
3. `PlayerLoan.status` رشتهٔ آزاد است (CAS-guard شده)؛ enum شدنِ آن یک
   مهاجرتِ بعدیِ اختیاری است. `InsurancePolicy`/`GymMembership` بدون unique
   هستند — **عمدی** (ردیف‌های تاریخ) و سرویس‌ها در تراکنس‌اند guard شده‌اند.
4. `prisma-engines/` (موتورهای ساخت) در مخزن می‌ماند — فقط برای build روی
   دستگاهِ سازنده استفاده می‌شود و به باندلِ سرور راه ندارد.

---

## اعلام نهایی

**ربات «میراث» در حالتِ عملیاتی (production-ready) است.**
تمام باگ‌های کشف‌شده (از تایپوگرافی تا دو سیستمِ پولِ ناسازگار) در ریشه
اصلاح و با تست بازگشتی قفل شدند؛ ۱٬۰۳۴ تست، lint، زنجیرهٔ مهاجرت،
اینوارانت‌های اقتصادی روی PostgreSQL زنده و بستهٔ آفلاینِ قابل اثبات،
همه سبز هستند. باندلِ شسته‌شده (`offline-deps/`) سروری می‌سازد که
**بدون هیچ دسترسی به اینترنت** بالا می‌آید، مهاجرت می‌شود و می‌چرخد.
