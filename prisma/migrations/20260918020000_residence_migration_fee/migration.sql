-- ثبتِ هزینهٔ مهاجرت در دفتر کل.
--
-- پیش‌تر `ResidenceService.migrate` مبلغ مهاجرت (پایه ۲ میلیون تومان، ضربدر
-- ضریب سطح منطقه) را مستقیماً از `balance` بازیکن کم می‌کرد و هیچ ردیفِ
-- `financial_transactions` نمی‌ساخت. نتیجه: دفتر کلِ بازیکن با کیف پولش
-- نمی‌خواند (ممیزی اقتصاد آن را «کسرِ بدون ثبت» می‌دید)، بازیکن در تاریخچه
-- نمی‌فهمید پولش کجا رفت، و آن پول هم به هیچ صندوقی نمی‌رسید.
--
-- این مهاجرت فقط یک مقدار به enum اضافه می‌کند؛ هیچ ستون/ردیفی حذف نمی‌شود.

-- AlterEnum
ALTER TYPE "TransactionType" ADD VALUE IF NOT EXISTS 'RESIDENCE_MIGRATION_FEE';
