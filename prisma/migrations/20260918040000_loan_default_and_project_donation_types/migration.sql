-- ═══════════════════════════════════════════════════════════════════════════
-- دو نوعِ تراکنشِ تازه — هر دو فقط «افزودنِ مقدار» به enum هستند.
--
-- ۱) LOAN_COLLATERAL_SEIZED
--    وامِ سررسیدگذشته تا امروز هیچ عاقبتی نداشت: `dueAt` نوشته می‌شد ولی
--    هرگز خوانده نمی‌شد، وثیقه (خزانهٔ شرکت) هرگز تملک نمی‌شد و وام تا ابد
--    `ACTIVE` می‌ماند. حالا تملکِ وثیقهٔ شرکتی، خزانه را به صندوق بانک
--    منتقل می‌کند؛ آن جابه‌جایی باید ردیفِ دفتری داشته باشد تا ممیزیِ عرضهٔ
--    پول آن را «پولِ بدون ثبت» نبیند.
--
-- ۲) PROJECT_DONATION
--    کمک به پروژهٔ شهری پیش‌تر با نوعِ `TRANSFER` ثبت می‌شد. `TRANSFER`
--    معنایش «پول از یک بازیکن به بازیکنِ دیگر» است؛ پس ممیزی اقتصاد این
--    کمک‌ها را جریانِ خصوصی-به-خصوصی می‌دید، در حالی که پول واقعاً از بخش
--    خصوصی بیرون می‌رفت و خرج پروژه می‌شد. نتیجه: ترازِ بخش عمومی دروغ
--    می‌شد. نوعِ اختصاصی، جهتِ واقعیِ پول را ثبت می‌کند.
--
-- هیچ ستون/جدول/ردیفی تغییر نمی‌کند؛ فقط enum گسترده می‌شود.
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TYPE "TransactionType" ADD VALUE IF NOT EXISTS 'LOAN_COLLATERAL_SEIZED';
ALTER TYPE "TransactionType" ADD VALUE IF NOT EXISTS 'PROJECT_DONATION';
