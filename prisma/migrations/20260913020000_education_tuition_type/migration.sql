-- نوع تراکنش اختصاصی شهریهٔ تحصیل.
--
-- پیش از این، شهریه با نوع `WITHDRAWAL` (برداشت) ثبت می‌شد؛ در دفتر کل
-- بازیکن «برداشت» دیده می‌شد، نه هزینهٔ تحصیل. مقدار جدید اجازه می‌دهد
-- دفتر کل و برچسب تراکنش‌ها معنای واقعی داشته باشند.
ALTER TYPE "TransactionType" ADD VALUE IF NOT EXISTS 'EDUCATION_TUITION';
