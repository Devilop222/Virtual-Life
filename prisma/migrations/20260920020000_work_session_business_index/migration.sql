-- ═══════════════════════════════════════════════════════════
-- Performance: کوئریِ «کارکردِ یک کسب‌وکار در یک بازه».
--
-- هر تسویهٔ حقوق (و هر پیش‌نمایشِ آن) شیفت‌های تمام‌شدهٔ یک کسب‌وکار را
-- می‌خواند تا کارکردِ هر کارمند را از آن بسازد. بدون ایندکس، این کوئری روی
-- کلِ جدولِ کارکردها پیمایش می‌کرد — جدولی که به‌ازای هر شیفتِ هر بازیکنِ
-- هر کسب‌وکار یک ردیف می‌گیرد.
--
-- ستون‌ها به ترتیبِ همان شرط‌های کوئری‌اند: کسب‌وکار، وضعیت، و بازهٔ زمانی.
-- (شاخصِ یکتای «یک شیفتِ فعال برای هر بازیکن» روی player_id است و این
-- کوئری را پوشش نمی‌دهد.)
-- ═══════════════════════════════════════════════════════════

CREATE INDEX IF NOT EXISTS "work_sessions_business_work"
  ON "work_sessions"("business_id", "status", "ended_at");
