-- ═══════════════════════════════════════════════════════════
-- رخدادهای مراقبت از حیوان خانگی: بازی و هدیه.
--
-- پیش‌تر فقط دو نوع رخداد برای حیوان وجود داشت و همین باعث دو ناهمخوانی
-- در داده و متنِ بازیکن می‌شد:
--   • هدیهٔ روزانهٔ حیوان با نوعِ PET_FED ثبت می‌شد؛ پس در «تاریخچهٔ زندگی»
--     کنارِ عنوانِ «… برایت هدیه آورد!» برچسبِ «غذای حیوان» می‌آمد.
--   • «بازی و وقت گذراندن با حیوان» هیچ نوعِ رخدادی نداشت؛ پس نه
--     یک‌باردرروز بودنش قابل تضمین بود و نه در تاریخچه دیده می‌شد.
--
-- PET_GIFT و PET_PLAY این دو را از هم جدا می‌کنند. پیوندِ حیوان (Bond) از
-- تعدادِ رخدادهای مراقبت (pet-feed:* و pet-play:*) محاسبه می‌شود؛ پس
-- dedupeKey هر رخداد باید پایدار و یکتا بماند.
-- ═══════════════════════════════════════════════════════════

ALTER TYPE "GameEventType" ADD VALUE IF NOT EXISTS 'PET_PLAY';
ALTER TYPE "GameEventType" ADD VALUE IF NOT EXISTS 'PET_GIFT';
