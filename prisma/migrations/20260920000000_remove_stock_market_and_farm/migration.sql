-- حذفِ کاملِ «بورس شهر» و «مزرعه» — دو سیستمی که اثرِ معناداری بر پیشرفت نداشتند.
--
-- ─────────────────────────────────────────────────────────────────────────────
-- چرا این دو حذف شدند؟ (سنجش، نه سلیقه)
--
--  ۱) در «خالص دارایی» هیچ‌کدام حساب نمی‌شدند. معیارِ رسمیِ پروgression
--     (`StatisticsService`) این است: کیف پول + بانک + (ملک + کسب‌وکار + انبار)
--     − بدهی. نه سبد سهام در آن بود و نه زمینِ کشت. پس نه رتبهٔ ثروت، نه
--     نشانِ دارایی و نه هیچ مأموریتی به آن‌ها وابسته نبود.
--
--  ۲) بازدهِ واقعیِ مزرعه ناچیز بود. برداشت‌ها با `cycleAmount` هم‌تراز شده‌اند
--     (تقسیم بر ۳۰)، یعنی سودِ خالصِ هر دوره: گندم ۲٬۳۳۳ روی ۳۰۰٬۰۰۰ (۰٫۷۸٪ در
--     ۵٫۶ ساعت واقعی)، گوجه ۶٬۰۰۰ روی ۷۰۰٬۰۰۰ (۰٫۸۶٪ در ۱۱٫۲ ساعت)، زعفران
--     ۲۰٬۰۰۰ روی ۲ میلیون (۱٪ در ۲۴ ساعت) — حدود ۰٫۰۴٪ تا ۰٫۱۴٪ در ساعتِ واقعی.
--     در برابرِ یک شعبهٔ کسب‌وکار که در همان ساعت ~۱۶٬۶۶۶ می‌دهد. ظرفیتِ باغچه
--     هم به تعدادِ ملک گره خورده بود، پس انگیزهٔ خرید خانه نمی‌ساخت.
--     (راهنمای بازیکن سود «۲۳ تا ۳۰ درصد» وعده می‌داد که با عددِ واقعیِ کد
--     نمی‌خواند — یک وعدهٔ دروغین، دلیلِ دومِ حذف.)
--
--  ۳) بورس یک قمارِ بی‌طرفِ نبود: قیمت‌ها قطعی و کُرَندار در بازهٔ ۸۵٪ تا ۱۱۵٪
--     قیمتِ پایه‌اند و نگه‌داشتن هزینهٔ حمل ندارد. بازیکنِ صبور می‌تواند در روزِ
--     ارزان بخرد و در روزِ گران بفروشد و ~۳۰٪ خالص ببرد — یعنی یک شیرِ تورمیِ
--     بی‌ریسک، بدون هیچ مهارت یا تعاملی با بازیکنِ دیگر (قیمت از hash می‌آید).
--
-- ─────────────────────────────────────────────────────────────────────────────
-- ایمنی (Impact Analysis)
--
--  • هیچ پولی جابه‌جا یا ساخته نمی‌شود. پولِ سهام و بذر قبلاً و در همان لحظهٔ
--    خرید/کاشت واقعاً خرج شده بود (Sink). پسوداندنش یک MINT بی‌سند می‌ساخت و
--    دفتر کل را از اینورینتِ «هر ریال منبع دارد» بیرون می‌برد؛ پس فقط سندِ
--    دارایی حذف می‌شود و موجودیِ کل پول دست‌نخورده می‌ماند.
--  • این دو جدول هیچ کلید خارجیِ دیگری را تغذیه نمی‌کردند (تنها وابستگی،
--    `players` بود)؛ پس حذفشان ردیفِ یتیم نمی‌سازد.
--  • ارث: انتقالِ سهام و زمین از پروندهٔ میراث برداشته شد؛ سایر دارایی‌ها
--    (سپرده، انبار، ملک، کسب‌وکار، حیوان) بی‌تغییر منتقل می‌شوند.
--
-- ─────────────────────────────────────────────────────────────────────────────
-- چه چیزی *حذف نمی‌شود* (تاریخچه حفظ می‌شود)
--
--  • ردیف‌های دفتر کل با نوعِ `STOCK_BUY` / `STOCK_SELL` / `FARM_PLANT` /
--    `FARM_HARVEST` و رخدادهای `STOCK_TRANSACTION` / `FARM_HARVESTED` می‌مانند
--    تا ممیزیِ گذشته بازسازی‌شدنی بماند.
--  • مقادیرِ enum دست‌نخورده‌اند: Postgres مقدارِ enum را نمی‌اندازد و خواندنِ
--    ردیف‌های تاریخی به آن‌ها نیاز دارد (همان قاعدهٔ migrationِ کارگاه).

-- ۱) سبد سهام
DROP TABLE IF EXISTS "stock_holdings";

-- ۲) زمین‌های کشت
DROP TABLE IF EXISTS "farm_plots";
