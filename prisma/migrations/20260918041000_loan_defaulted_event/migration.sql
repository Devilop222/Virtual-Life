-- ═══════════════════════════════════════════════════════════════════════════
-- رخدادِ «نکولِ وام بانکی» برای سرگذشت و اعلان.
--
-- پیش‌تر عمرِ وام بانکی هیچ نقطهٔ پایانی نداشت؛ پس نه رخدادی برای ثبت بود و نه
-- دلیلی برای خبر دادن. حالا نکول یک لحظهٔ واقعی در زندگیِ شخصیت است (وثیقه
-- تملک می‌شود و اعتبار پایین می‌آید) و باید در تاریخچه بماند.
--
-- مهاجرت افزودنی است؛ هیچ جدول یا ستونی تغییر نمی‌کند.
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TYPE "GameEventType" ADD VALUE IF NOT EXISTS 'LOAN_DEFAULTED';
