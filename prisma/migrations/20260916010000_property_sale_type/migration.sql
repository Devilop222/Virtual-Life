-- ═══════════════════════════════════════════════════════════
-- فروش ملک به شهر: نوع تراکنشِ جداگانه.
--
-- پیش‌تر هیچ راهی برای خروج از املاک وجود نداشت و حتی یک جملهٔ دروغ
-- در صفحهٔ تأیید خرید نوشته شده بود («فروش ملک با قیمت خرید انجام
-- نمی‌شود» — درحالی‌که اصلاً فروش وجود نداشت). حالا:
--
--  • ملک در شرایط منطقی (بدون قرارداد اجارهٔ فعال و بدون وثیقهٔ وامِ
--    باز) به شهر فروخته می‌شود.
--  • قیمت، ۶۰٪ ارزش پایهٔ دارایی است — کمتر از قیمت خرید تا چرخهٔ
--    خرید→فروش ماشینِ پول‌سازی نشود (Sink خالص در هر چرخه).
--  • ردیف دفتر کل فقط مقصد (فروشنده) دارد: پول از ترازنامهٔ عمومی
--    می‌آید (MINT)، درست مثل REWARD_PAYOUT؛ ممیزی عرضهٔ پول این را
--    جدا از اجاره (TRANSFER) و خرید (BURN) می‌شمارد.
-- ═══════════════════════════════════════════════════════════

ALTER TYPE "TransactionType" ADD VALUE IF NOT EXISTS 'PROPERTY_SALE';
