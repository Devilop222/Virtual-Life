-- ═══════════════════════════════════════════════════════════
-- Integrity: همزمان تنها یک وامِ فعال برای هر بازیکن — در سطح
-- دیتابیس، نه فقط با گارد شمارشی در سرویس (ضد دابل‌کلیکِ همزمان)
-- ═══════════════════════════════════════════════════════════
CREATE UNIQUE INDEX IF NOT EXISTS "one_active_loan_per_player" ON "loans"("player_id") WHERE "status" = 'ACTIVE';
