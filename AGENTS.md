# قوانین پروژهٔ Legacy Game — میراث

## قانون طلایی
- بدون تأیید صریح کاربر Feature جدید ساخته نشود؛ کاربر شمارهٔ قابلیت را از کاتالوگ نقشهٔ راه اعلام می‌کند.
- همیشه Root Cause پیدا شود؛ workaround ممنوع.
- سیستم‌های Clan/جنگ/Mini-game ممنوع.

## هویت برند
- نام رسمی محصول: **Legacy Game** / در متن فارسی: **میراث**. تنها منبع نام: `src/config/brand.ts`.
- نام بازی فقط در نقاط معرفی (خوش‌آمد، راهنما، درِ گروه، توضیح ربات) می‌آید؛
  در پیام‌های عملیاتی، مالی و پنل‌های روزمره تکرار نشود (Brand ≠ Keyword Stuffing).
- لحن: انسانی و روان؛ در لحظهٔ رویشِ شخصیت و مناسبت‌های خاص، ادبی و کوتاه‌آیه؛
  در خطا، تراکنش و عدد: کوتاه، دقیق و کاملاً مطابق رفتار Backend.

## قانون ثابت پنل راهنما
- بعد از هر Update، فصل مربوطه در `src/bot/help-content.ts` بازبینی شود:
  تمیزتر، کوتاه‌تر، دقیق‌تر و کامل‌تر — متن اضافه حذف، عدد واقعی جای «تقریبی»،
  و هر مسیر تازه‌سازی‌شده (keyword/callback) دقیقاً مطابق رفتار فعلی توضیح داده شود.

## اصول UI
- ONE ACTION = ONE PANEL ، ONE PANEL = ONE MESSAGE؛ همیشه `editPanel` نه پیام جدید.
- همهٔ متن‌ها فارسی؛ اعداد با `fa()`/`money()` از ui-kit.
- خطای AppError فقط `persianMessage`؛ خطای ناشناخته از `handleCallbackError/handleCommandError`.

## الگوهای اجباری مالی
- هر کسر/افزایش پول: `updateMany` شرطی با بررسی `count === 1` داخل `$transaction`.
- مقدارِ وابسته به ردیفی که تراکنش قفل می‌کند (مثل تاریخ انقضا) *داخل* تراکنش و پس از
  نوشتنِ قفل حساب شود؛ خواندنِ پیش از تراکنش یعنی چند بار پول گرفتن برای یک تحویل.
- بازندهٔ رقابت داخل `$transaction` باید **پرتاب** کند نه `return`؛ `return` تراکنش را
  commit می‌کند و نوشتارهای قبلی‌اش (مثل کسرِ صندوق بانک) باقی می‌ماند.
- نشانِ «گرفته‌شده» را همان تراکنشی بنویسد که پول را می‌دهد؛ وگرنه شکستِ پرداخت، پاداش را
  برای همیشه می‌سوزاند. فقط `P2002` به بازیکن «قبلاً گرفته‌ای» بگوید، نه هر خطای دیگر.
- رخدادِ خوراک (`recordPlayerEvent`) *پس از* تراکنشِ پرداخت ثبت شود، نه داخلِ آن: پول که قطعی
  شد خبر هم می‌آید، ولی خبر نباید وسطِ تراکنش کانکسیون دومی اشغال کند یا از تراکنشِ
  برگشتی ردیفِ بازمانده به جا بگذارد.
- نوعِ ردیف دفتر کل باید جهتِ واقعیِ پول را بگوید: `BANK_DEPOSIT`/`BANK_WITHDRAWAL` فقط برای
  کیف پول ↔ حسابِ بانکیِ خودِ بازیکن (جابه‌جاییِ داخلیِ بخش خصوصی)، و سپردهٔ مدت‌دار `DEPOSIT`
  (پول به ترازنامهٔ بانک می‌رود). یک نوع برای دو جهتِ متفاوت = ممیزیِ مالیِ نابینا.
- هر تغییرِ پولی باید با `scripts/economy-invariants-smoke.cjs` آشتی بدهد: داراییِ خصوصیِ
  بازیکن (کیف پول + حساب بانکی) تا ریال برابرِ خالصِ دفتر کلِ اوست.
- Idempotency با Unique Constraint یا `dedupeKey` روی GameEvent.
- هیچ Timer جدیدی برای منطق بازی؛ تسویهٔ Lazy (الگوی elections/payroll).

## تعریف واحد «شاغل»
- منبع حقیقت: `src/modules/occupation/employment.ts` (نوبت فعال / استخدام / کسب‌وکار / کار هفتهٔ اخیر).
- هیچ کدی مستقیماً `occupationId` را به‌عنوان معیار اشتغال نخواند؛ آن فیلد فقط با فعال شدن
  نردبان شغلی رسمی نوشته خواهد شد و در همان لحظه باید به فیلتر اشتغال اضافه شود.

## انتشار خبر
- رخدادهای اولویت ≥۴ از طریق `NewsService.claimForChat` در مسیر پیام گروه منتشر می‌شوند؛
  رخداد جدید مهم باید priority و در صورت نیاز dedupeKey درست بگیرد.

## Verify
```
node node_modules/typescript/bin/tsc --noEmit
node node_modules/eslint/bin/eslint.js src tests
node node_modules/jest/bin/jest.js
node node_modules/typescript/bin/tsc          # build
$env:DATABASE_URL="..."; node node_modules/prisma/build/index.js validate
```
